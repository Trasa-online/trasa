# CLAUDE.md — Trasa.travel

> Przeczytaj to przed każdą sesją. Są tu decyzje projektowe, brand guidelines i lista rzeczy których NIE ruszać.

---

## Czym jest Trasa

Trasa to aplikacja do planowania podróży - zarówno **grupowo**, jak i **solo**. Użytkownicy przeglądają miejsca, dopasowują je (samodzielnie lub wspólnie z grupą - ale nie nazywamy tego "swipe" ani "match", to zakazane słowa), tworzą trasy i prowadzą dziennik podróży. Tryb grupowy jest jednym z kluczowych use case'ów, ale nie wyklucza solo tripów - cały flow działa też dla pojedynczego użytkownika. Firmy mogą dodać swój lokal jako wizytówkę i zarządzać wizerunkiem.

### Źródło danych miejsc (KRYTYCZNE)

Aplikacja pokazuje **wyłącznie miejsca z bazy Supabase** (tabela `places`). Nie ma żadnego trybu mock ani fallbacku do lokalnych danych.

- `src/lib/mockPlaces.ts` — **USUNIĘTY**, nie przywracać
- Jeśli miasto nie ma miejsc w DB → pokazujemy pusty stan, NIE generujemy fake danych
- `MOCK_MODE`, `getMockPlaces`, `MOCK_PLACE_DETAIL` — nie istnieją, nie używać

---

## Brand Guidelines

### Kolory

```
Primary (akcent):     gradient #F4A259 → #F9662B (orb, fill primary buttons)
Akcent żółty (2026-08-12): #FDF184 (żółty) + #FDCD84 (złoty), gradient #FDF184 → #FDCD84
Tło / biel:           #FEFEFE (złamana biel — NIE czyste #FFFFFF)
Typografia główna:    #0E0E0E (niemal-czarna)
Typografia secondary: #979797
Typografia tertiary:  #CFCFCF (niedostępne/placeholder)
```

**Akcenty żółte (rebrand ikony 2026-08-12):** `#FDF184` i `#FDCD84` to kolory AKCENTOWE (z nowej ikony aplikacji, gradient żółty→złoty). **Pomarańczowy zostaje PRIMARY** - żółte to tylko akcenty (tła, wyróżnienia, dekoracje), NIE zastępują pomarańczu na guzikach primary. W Tailwind: `bg-trasa-yellow` (DEFAULT #FDF184) / `bg-trasa-gold` (#FDCD84), `text-trasa-yellow-ink` / `text-trasa-gold-ink`, gradient `bg-trasa-yellow` (util backgroundImage) lub `bg-gradient-to-r from-[#FDF184] to-[#FDCD84]`. Zakaz gradientu na guzikach dotyczy też żółtego.

W Tailwind odpowiedniki klas:
- Primary fill = **`bg-primary`** (= `#EE5307`). ⛔ NIE `bg-orange-600` - patrz reguła jednego pomarańczu niżej.
- Secondary (guziki akcji) = **szary fill** `bg-secondary text-secondary-foreground` (styl YouTube), NIE biały+pomarańczowy stroke
- Tekst główny = `text-foreground` (mapuje na #0E0E0E)
- Tekst secondary = `text-muted-foreground`

**Paleta landingu spontaway (2026-09-02).** Marketingowa strona B2C pod `spontaway.com`
(route `/`, [SpontawayLanding.tsx](src/pages/SpontawayLanding.tsx)) ma własną, węższą
paletę z Figmy („Landing B2C ... high-fi copy"), w Tailwindzie jako `spontaway.*`:

```
Primary (marka):   #EE5307  bg-spontaway-orange   (znak, guziki, duże nagłówki)
Secondary (tło):   #FDF184  bg-spontaway-yellow   (karty hero/CTA, pasek statystyk)
Accent 1 (tekst):  #5B2C06  text-spontaway-brown  (tekst na żółtym + guzik secondary)
```

**Brązowy `#5B2C06` to nowy kolor marki** (nie mieszać z `text-foreground` w apce). Pomarańcz
na żółtym ma kontrast 3.08:1, więc tej pary używaj **tylko do dużych nagłówków**, nigdy do
zwykłego tekstu; treść na żółtym pisz brązowym (10:1).

**Landing B2C po ANGIELSKU ma WŁASNE grafiki (Nat 2026-09-18):** `public/grafika_hero_en.png` (hero, ten sam kadr 963×1032 co PL - gwiazdki jako osobne elementy w procentach siedzą na nim bez zmian), `public/mockup_odkrywaj_en.png` (karty miejsc z angielskimi kategoriami) i `public/dziel_sie_en.png` (kreator krajów po angielsku + wyjazd z przetłumaczoną notką). Ścieżki idą z `COPY.en` (`hero.heroImg`, `features[].img`), więc PL zostaje przy `grafika_hero.png` / `mockup_odkrywaj.png` / `dziel_sie.png`. Grafika kolekcji (`kolekcje_grafika.png`) NIE ma jeszcze wersji EN - w obu językach ta sama. Nowa grafika EN = plik `*_en.png` + wpis w `COPY.en`, nie warunek w JSX. **Hero landingu B2C na MOBILE (2026-09-18): zolta karta ma 32 px marginesu z obu stron (`px-8` na sekcji), a mockup wychodzi POZA karte na cala szerokosc ekranu** (`-mx-[52px] w-[calc(100%+104px)]` = 32 px sekcji + 20 px paddingu karty; zmierzone w WebKit: karta 32-361 px, grafika 0-393 px, zero przepelnienia poziomego). ⛔ Karta bez `overflow-hidden`, inaczej przytnie telefony. **Hero landingu B2C = `public/grafika_hero.png` + TRZY gwiazdki jako OSOBNE elementy** (2026-09-17).
Gwiazdki (`public/star.svg`) nie są częścią PNG, bo tylko wtedy mogą się ruszać; pozycja i rozmiar
w PROCENTACH kontenera (`left/top` + `w-[8%]`), więc trzymają się grafiki na każdej szerokości.
Układ to TRÓJKĄT: **górna** `left-[45%] top-[6%]` wciśnięta w „V" między telefonami (nachodzi
na krawędź lewego - na prośbę Nat zeszła z `top-[2%]`, gdzie tylko musnęła róg), **prawa**
`left-[80%] top-[67%]` przy prawej krawędzi prawego telefonu, **dolna lewa** `left-[3%] top-[82%]`.
⛔ Prawej nie przenoś pod górę ekranu: przy `top-[4%]` stała tuż obok górnej i obie zlewały się
w parę, a przy `top-[11%]` siadała na czarnej ramce obok wyspy, między godziną a ikonami zasięgu
(oba sprawdzone renderem A/B w WebKit).
⚠️ Każda ma INNĄ animację (`animate-star-twinkle` / `-alt` / `-slow`, czasy 5,5 / 7,5 / 6,4 s
z różnymi przesunięciami fazy) - przy wspólnej dwie sąsiadujące pulsują równocześnie i całe hero
zaczyna mrugać jak jeden element. Dokładasz gwiazdkę → dołóż też wariant animacji.

### Stan FOCUS = żółty marki (2026-09-16)

`--ring` to od teraz **żółty marki `#FDF184`** (`54 96.8% 75.5%`), a nie pomarańcz primary. Poza tokenem trzeba było dołożyć trzy rzeczy, których **żaden token Tailwinda nie dotyka**, a iOS maluje je systemowym niebieskim: kursor w polu (`caret-color`), podświetlenie zaznaczonego tekstu (`::selection`) i natywne kontrolki - checkbox, radio, suwak (`accent-color`). To właśnie ten niebieski widać na telefonie po wejściu w pole; wszystkie trzy siedzą teraz w [index.css](src/index.css).

⚠️ **Nie wszystko mogło być żółte** - `#FDF184` na bieli ma ~1,1:1:
- **zaznaczenie tekstu** - żółty JEST właściwy: to tło, a tekst zostaje prawie czarny, więc czyta się jak zakreślacz;
- **kursor i ptaszek w checkboksie** - żółte byłyby niewidoczne (2 px kreska na bieli), więc idą pomarańczem marki. To nadal koniec z niebieskim, tylko czytelny;
- **pierścień focusu** sam na bieli też by znikał, dlatego jego OFFSET (wewnętrzna obwódka Tailwinda) jest **brązowy i ma 1 px** - przy 2 px brąz przeważał i całość czytała się jako brązowy z żółtym halo, a ma być odwrotnie. Sprawdzone renderem w WebKit na zbudowanym CSS.

⚠️ Reguła `*:focus-visible` stoi **POZA `@layer`** - tylko dzięki temu wygrywa z `ring-offset-background` z warstwy utilities (style niewarstwowane biją warstwowane). Przenosząc ją do `@layer`, przestanie działać.

⛔ **Niebieski pierścień WebKita to OSOBNA sprawa i była zgłaszana trzy razy** (ostatnio 2026-09-17). Token `--ring` i offset dotyczą WYŁĄCZNIE elementów z klasami `focus-visible:ring-*`, czyli komponentów shadcn. Zwykły `<button>` - pigułki zakładek, wiersze ustawień, guziki w arkuszach - nie ma ich wcale, więc WebKit malował mu systemowy `outline: auto -webkit-focus-ring-color`. Dlatego w [index.css](src/index.css) stoi teraz `@layer base { *:focus { outline: none } *:focus-visible { outline: 2px solid #5B2C06; outline-offset: 2px } }`. ⚠️ `:focus` gasimy, bo **iOS ustawia go po zwykłym TAPNIĘCIU**; `:focus-visible` zostaje z brązowym obrysem marki - brązowy działa na bieli, na żółtym (10:1) i na peachy, czyli na wszystkich tłach naszych akcji. ⚠️ Ta reguła MUSI siedzieć w `@layer base`, a NIE poza warstwami jak wiersz z offsetem: dzięki temu `focus-visible:outline-none` z komponentów shadcn (warstwa utilities) nadal wygrywa i te komponenty zostają przy swoim żółtym pierścieniu, bez drugiego obrysu na wierzchu.

⛔ W kodzie nie ma już ANI JEDNEGO `ring-blue-*`. Zostało natomiast ~30 pól z własnym, miękkim pomarańczowym focusem (`focus:ring-orange-500/30` i podobne) - to nie jest niebieski, więc świadomie ich nie ruszano.

### ⛔ Zakaz ciemnych teł na stronach publicznych

**NIGDY nie używaj czarnego ani ciemnoszarego tła (`#0E0E0E`, `bg-slate-900`, `bg-black`, dark mode)** na stronach widocznych dla użytkowników (landing, waitlist, one-pager, itp.). Zawsze tło = `#FEFEFE` (złamana biel) lub bardzo jasny odcień (np. `bg-slate-50`). Ciemne tła są zarezerwowane wyłącznie dla nakładek wideo/overlay wewnątrz komponentów (np. phone mockup).

### Ekran powitalny / logowania B2C (2026-09-14)

[Auth.tsx](src/pages/Auth.tsx) (natywka, gałąź B2C): **żółte tło marki `#FDF184`, brązowy tekst `#5B2C06`**, nagłówek „Odkrywaj i zwiedzaj" w Sigmar (`font-brand`) BEZ claimu pod spodem (zdjęty 2026-09-14; tagline zostaje w marketingu), u góry **tasująca się talia okładek** ([WelcomeDeck](src/components/auth/WelcomeDeck.tsx), przebudowa 2026-09-18: karty **9:16** 176 × 313 px z **PRAWDZIWYMI kafelkami z Eksploracji** - `TripTile` i `ListTile` z [FeedTiles](src/components/home/FeedTiles.tsx) w wariancie `grid`, renderowane w naturalnej szerokości 224 px i przeskalowane transformem do karty; wyjazd wypełnia kartę co do piksela, kolekcja stoi wyśrodkowana na swoim kolorze. Dzięki temu talia ma pigułkę autora z nakładką awatara, mini-mapę, chipy miasto/miejsca/dni - i każda zmiana kafelka w apce jest od razu tutaj. ⛔ Nie wracaj do własnych uproszczonych kart (do 18.09: zdjęcie miejsca + nazwa / kolor + 3 miniatury) - Nat: „brakuje stylowania takiego jak w apce". Źródła: opublikowane wyjazdy z `list_cover_url`, publiczne kolekcje `visited`, `pins`, `discovery_items`, `profiles` - wszystko z publicznym odczytem, sprawdzone kluczem anon; co 2,4 s wierzchnia karta wysuwa się w bok i CHOWA POD spód talii - bez zanikania, z-index spada w połowie lotu, gdy karta jest już poza talią), karuzela USP w brązie, Apple (czarny) + Google (biały) na dole. Copy w `auth:welcome.*`. Poprzednia wersja (biel + dryfujące pomarańczowe plamy) wycofana. Ekran B2B (`?business=true`) bez zmian - niebieski.

### Identyfikacja B2B (panel biznesowy) - niebieski branding

**⚠️ ZMIANA 2026-09-14 (Nat): PANEL (`BusinessDashboard`) NIE jest już niebieski** - „cała aplikacja jest żółto-pomarańczowa, więc panel powinien być również w takich kolorach". W dashboardzie: aktywna pozycja nawigacji `bg-primary/10 text-primary`, przełącznik języka i akcje `text-primary`, baner powitalny = **gradient marki** `#FDF184 → #FDCD84` z brązowym tekstem `#5B2C06`, baner „czeka na weryfikację" w złocie, wykresy/statystyki `#EE5307`. Zero `*-blue-*` w [BusinessDashboard.tsx](src/pages/BusinessDashboard.tsx) - przy edycji NIE przywracaj niebieskiego. **Ekran logowania/rejestracji B2B i „Ustaw hasło" też zeszły z niebieskiego (2026-09-14)** - patrz „Wejście dla lokali" niżej. `set-password-biznes` ([SetPassword.tsx](src/pages/SetPassword.tsx), gałąź `isBusiness`) ma teraz tę samą skórę co karta logowania: białe tło `#FEFEFE` (bez szarych kropek), znak „spontaway biznes", wąska karta 420 px, szare pola, pomarańczowe CTA, wskaźnik siły hasła złoto→pomarańcz. **Niebieski został już TYLKO w onboardingu lokalu** (`BusinessOnboarding`) - Nat świadomie go nie ruszamy, bo idzie razem z redesignem panelu.

**Wejście dla lokali = LANDING Z REJESTRACJĄ (`/auth?business=true`, wariant C2 z Figmy, wybór Nat 2026-09-14).** Research portali partnerskich (Refero: DoorDash Merchants, Faire, Robinhood, Boords, Squarespace) dał jeden wniosek: logowanie i rejestracja to NIE są równorzędne zakładki - powrót do panelu ma być szybki, a założenie konta dostaje przestrzeń i argumenty. Dlatego:
- **JEDNA strona, zmienia się tylko treść formularza (prośba Nat 2026-09-14).** Nad formularzem stoi **przełącznik `[Zarejestruj lokal | Zaloguj się]`** (aktywny = brąz marki `#5B2C06`, żeby nie konkurował z pomarańczowym CTA pod spodem), a hero - nagłówek, grafika, układ - zostaje na miejscu. ⛔ Logowanie NIE ma osobnego ekranu: wcześniej „wróć do logowania" wyrzucało na pustą białą stronę z samotną kartą i lokal tracił kontekst. Przy logowaniu zmienia się nagłówek („Witaj ponownie"), podtytuł i pola; rząd dowodów znika (to argumenty za rejestracją). Guzik „Mam już konto / Dodaj lokal" w belce ZDJĘTY - dublował przełącznik pod inną nazwą.
- **Domyślnie** (i po akcji „zaloguj się", i po „zarejestruj się") widać **żółte hero** `#FDF184`: nagłówek w Sigmarze („Dodaj swój lokal do spontaway"), obietnica, pole email + pomarańczowe CTA, rząd dowodów, a po prawej **grafika `public/B2B_mockup.png`, która unosi się góra-dół** (`animate-biz-float`, keyframes w [index.css](src/index.css), 6 s, ±14 px na desktopie / ±8 px poniżej `lg`, wyłączona przy `prefers-reduced-motion`). Pod hero **sekcja USP w stylu landingu B2C** (prośba Nat 2026-09-14): trzy **żółte karty** `#FDF184` z promieniem 32 px, nagłówek w Sigmarze na pomarańczowo `#EE5307` + zdanie w brązie; we WSZYSTKICH kartach kolejnosc jest ta sama: najpierw grafika, pod nia tytul i copy (prosba Nat 2026-09-15; wczesniej srodkowa karta miala tytul NAD grafika za makieta marketingowa - w rzedzie trzech kart czytalo sie to jak blad skladu, bo wzrok skakal gora-dol-gora). Grafiki = mockupy z landingu B2C (`mockup_odkrywaj` / `mockup_listy` / `mockup_dziel_sie`) pokazane od strony „co podróżny robi z Twoim lokalem"; kadr ma stałą wysokość, bo trzy pliki mają różne proporcje. Copy w `auth:bizland.usp*`.
- **Rejestracja jest DWUKROKOWA:** hero zbiera sam email (tak jest w makiecie), a po „Dalej" dosypuje nazwę lokalu i telefon - `register-business` wymaga nazwy, a trzy pola w hero zabijały lekkość. Guzik kroku 2 to „Załóż konto".
- **Logowanie** = ten sam hero, pola w kształcie pigułki na białym, CTA „Zaloguj się do panelu" + „Nie pamiętasz hasła?".
- ⛔ **Wyjątek od domyślnego hero:** gdy w adresie jest `?return=` (odbiliśmy kogoś z chronionego adresu panelu), pokazujemy od razu logowanie - to wracający właściciel, nie lead.
- Responsywność: `lg` = dwie kolumny, poniżej stos (grafika pod treścią, węższa), guzik pełnej szerokości na telefonie. ⛔ Pole email w hero ma `w-full shrink-0 sm:flex-1`, NIE samo `flex-1`: kontener jest na telefonie KOLUMNA, a w kolumnie `flex-1` steruje wysokoscia - `h-14` przegrywalo z `flex-basis: 0%` i pole zgniatalo sie do 20 px przy guziku 56 px (zgloszenie Nat 2026-09-15).

**Reguła historyczna (auth B2B, nadal obowiązuje tam):** kontekst dla firm = niebieska identyfikacja na ekranach logowania (`/auth?business=true`), ustawiania hasła (`/set-password-biznes`) i onboardingu. Niebieski to **kolor akcentu** (guziki, toggle, badge, linki, focus) - nie tło.

**Maile do lokali = TA SAMA skora, co panel i landing B2B (2026-09-15).** Trzy transakcyjne maile (`register-business/activation.ts` - aktywacja, `send-business-welcome/welcome.ts` - powitanie po zatwierdzeniu, `send-business-password-reset/index.ts` - reset hasla) zeszly z niebieskiego razem z reszta B2B. Uklad: biala belka ze znakiem i wordmarkiem „spontaway **biznes**", pod nia ZOLTE hero `#FDF184` z naglowkiem w brazie `#5B2C06`, nizej pomaranczowe CTA `#EE5307` w pigulce. Wspolna funkcja `bizEmailShell` jest SKOPIOWANA do kazdego z plikow (funkcje brzegowe wdraza sie osobno, wiec nie ma tu wspolnego importu; od 2026-09-21 takze `business-nudges/emails.ts`) - **zmieniasz skore w jednym, zmien w pozostalych trzech**. ⛔ Sigmar w mailu NIE dziala (Gmail i Outlook wycinaja `@font-face`) - naglowki ida ciezkim stosem systemowym. ⛔ Na zoltym tle piszemy WYLACZNIE brazem: pomarancz na zoltym ma 3,08:1. Reset hasla przestal tez mowic „TRASA BIZNES".

**KODY QR NA WIZYTOWKACH DRUKOWANYCH (decyzja Nat 2026-09-21, migracja `20260921g`, na razie JEDEN kod testowy `vcf9brfx`):** tabela `place_qr_codes (token, place_id, claimed_by_profile_id, scans)`, bez polityk - klient czyta tylko przez `resolve_qr_code(token)` (anon, zlicza skan), nowy kod = `create_qr_code(label, place_id)` (admin, 8 znakow bez 0/1/l/o), przypisanie miejsca do kodu = zwykly UPDATE `place_id` w SQL. Jeden link `spontaway.com/q/<token>` robi dwie rzeczy: **podrozny** dostaje strone miejsca (`api/share.ts`, `placePage()` wspolne z `/p/<id>`; rewrite w `vercel.json`), a z zainstalowana apka **universal link** otwiera trase `/q/:token` ([QrPlace](src/pages/QrPlace.tsx): pelna wizytowka + „Zapisz to miejsce", skan zalogowanego = odwiedziny bez GPS przez `mark_qr_visit`, `place_visits.source='qr'`); **lokal** ma pod karta „To moj lokal" → `/auth?business=true&qr=<token>`, a `register-business` przypina token do wizytowki (`claimed_by_profile_id`) i od razu podpina `place_id` z tokenu (bez dopasowywania po nazwie); `admin-moderate-business` przy zatwierdzeniu dopisuje `place_id` do kodu, ktory go nie mial. Universal links: `public/.well-known/apple-app-site-association` (`J33M8H3SGZ.travel.trasa.app`, sciezki `/q/*` ORAZ od 2026-09-21 wieczor `/p/*`, `/l/*`, `/r/*` = linki udostepniania miejsca / kolekcji / planu, zeby link z relacji na Instagramie otwieral apke; `/p/<id>` ma w apce trase `/p/:id` = `SharedPlace` w [QrPlace.tsx](src/pages/QrPlace.tsx), ten sam ekran co kod QR, tylko BEZ odwiedzin - id to `places` albo migawka `shared_places`; naglowek `Content-Type: application/json` w `vercel.json` - Vercel serwuje pliki z `public/` PRZED przepisaniami) + `applinks:spontaway.com` w `App.entitlements`. ⚠️ Po dodaniu entitlementu Xcode musi odswiezyc profil (Associated Domains w App ID - automatic signing robi to samo), a iOS pobiera AASA przy INSTALACJI apki (cache CDN Apple do 24 h) - kod otwarty w Safari zamiast w apce zaraz po wdrozeniu to nie blad. `appUrlOpen` w `NativeDeepLinkHandler` mapuje `https://spontaway.com/q/<token>` na hash `#/q/<token>`. PNG kodu: `npx qrcode -o <plik> -w 1200 "https://spontaway.com/q/<token>"` (plik dla Nat w `scratchpad/qr/`, w repo).

**PRZYPOMNIENIA DLA LOKALI, KTORE UTKNELY (decyzja Nat 2026-09-21, migracja `20260921f`, funkcja `business-nudges`, cron `business-nudges` 8:00 UTC):** trzy maile w tej samej skorze `bizEmailShell` (skopiowanej do `business-nudges/emails.ts` - to juz CZWARTY plik ze skora), kazdy najwyzej RAZ na wizytowke (tabela `business_nudges`, UNIQUE (wizytowka, rodzaj), bez polityk - pisze tylko service_role): `activation_1` (>= 48 h od rejestracji - Nat zeszla z 24 h tego samego dnia; email niepotwierdzony i zero logowan - z NOWYM linkiem, bo stary 24-godzinny wygasl), `activation_2` (>= 4 dni, po pierwszym, „ostatnie przypomnienie"), `complete_profile` (>= 2 dni od aktywacji, brak ktoregos z: kategoria / adres / zdjecie glowne / opis >= 20 znakow, wizytowka NIEZATWIERDZONA - z lista brakow jak checklista w panelu i linkiem `/biznes/<place_id ?? id>`). Pomijane: szkice (`is_draft`, leady od nas), odrzucone, zatwierdzone. Testy: `{"dry_run":true}` = sam plan; `{"test_to":"adres","ignore_delays":true}` = wysylka wszystkich zaplanowanych na jeden adres z `[TEST]`, BEZ zapisu do tabeli i z ATRAPA linku aktywacyjnego (prawdziwy token do cudzego konta nie moze isc na adres testera). Wolanie przez `net.http_post` z sekretem z Vault (klucz service_role z `.env` daje 401), odpowiedz w `net._http_response`. `verify_jwt = false` w `config.toml` (gate w funkcji: sekret / service_role / admin po `has_role`).

⛔ **`supabase functions deploy --no-verify-jwt` ZAPAMIETUJE przelacznik po stronie platformy** i ponowny deploy BEZ flagi go NIE cofa - trzeba PATCH-nac `/v1/projects/{ref}/functions/{slug}` z `{"verify_jwt": true}`. Jedno nieuwazne wdrozenie z ta flaga zdjelo autoryzacje z `send-business-welcome` (2026-09-15, zlapane i cofniete tego samego dnia). Dlatego `verify_jwt` dla wszystkich trzech funkcji mailowych jest teraz WPISANY JAWNIE w `supabase/config.toml` - stan ma zyc w repo, a nie w tym, co ktos ostatnio wpisal w terminalu.

**Flow B2B zweryfikowany end-to-end (2026-09-14):** rejestracja (`register-business`) → mail aktywacyjny → „Ustaw hasło" (`SetPassword`, gałąź `isBusiness`) → logowanie → onboarding (`BusinessOnboarding`) → panel (`BusinessDashboard`) → zapis danych → reset hasła (`send-business-password-reset`) → moderacja admina (`admin-moderate-business` approve = `is_active=true` + nadanie `place_id`). Wszystko działa. **Dwie łaty przy okazji audytu:** (1) `register-business` dla ISTNIEJĄCEGO konta zakłada teraz wizytówkę jako `is_draft=true` (SetPassword zdejmuje szkic po kliknięciu linku z maila) - wcześniej `is_draft=false` pozwalał wpisać cudzy email + nazwę i przypiąć wizytówkę do cudzego konta, a `BusinessGuard` od tej chwili wpychał ofiarę do panelu przy każdej nawigacji (griefing); (2) `upgrade-business-account` straciło „metodę 2" (decode `sub` z niepodpisanego tokenu) - zostaje wyłącznie `getUser`, który weryfikuje podpis. **Panel ładuje wiersz przez `business_profile_for_dashboard(p_key, p_token)`** (owner / admin / `?t=<preview_token>`), bo `business_profiles` ma teraz kolumnowe granty SELECT (audyt).

**Layout ekranów auth B2B (2026-07-16, aktualny):** jasny, w stylu SaaS (referencja: aaply). Tło jasnoszare z kropkowanym wzorem, logo Trasy (pomarańczowe, w białym kółku) w lewym-górnym rogu + wordmark „trasa biznes", biała karta wycentrowana z formularzem, toggle „Zaloguj się / Zarejestruj lokal". Guzik szybkiego przełączenia w prawym-górnym rogu. **NIE** wracaj do ciemnego granatu (`bg-blue-950`) - to spójne z regułą „żadnych ciemnych teł na stronach publicznych". Reference: `Auth.tsx` (early-return `if (businessMode)`) i `SetPassword.tsx` (branch `isBusiness`).

**Paleta B2B (jasny layout):**
- Tło ekranu: `bg-[#F4F4F5]` + kropki `radial-gradient(rgba(15,23,42,0.06) 1px, transparent 1px)` / `background-size: 22px 22px`
- Karta: `bg-white rounded-3xl shadow-xl shadow-slate-900/[0.06] border border-slate-100`
- Logo: `TrasaLogo` (pomarańczowe w białym kółku - patrz reguła Logo)
- Badge "Panel Biznesowy": `bg-blue-50 border-blue-100 text-blue-600`
- Inputs: `bg-white border-slate-200 text-slate-900 placeholder:text-slate-400 focus-visible:ring-blue-500`
- Labels: `text-slate-700`; tekst muted: `text-slate-500` / `text-slate-400`
- Toggle aktywny + Primary button: `bg-blue-600 hover:bg-blue-700 text-white`
- Linki / akcje secondary: `text-blue-600`

**Pomarańczowy (gradient `#F4A259 → #F9662B`) jest zarezerwowany WYŁĄCZNIE dla B2C** (użytkownicy końcowi: solo + grupowo). Wyjątek: samo **logo Trasy** zawsze pomarańczowe (w białym kółku), nawet w kontekście B2B. Poza logo nie mieszaj brandingu - akcent biznesowy = niebieski.

### JEDEN pomarańcz primary: `#EE5307` (decyzja Nat 2026-09-08)

Do 8 września w aplikacji żyły **trzy różne pomarańcze**, wszystkie podając się za primary:
`--primary` = `#D25014` (171 użyć `bg-primary`), Tailwind `orange-600` = `#EA580C`
(150 użyć) i `spontaway.orange` = `#EE5307` (landing). Ta strona traktowała pierwsze dwa
jak synonimy, a różnica `--primary` wobec docelowego to **ΔE 13,2** - widać ją od razu,
bez porównywania obok siebie.

Teraz jedna wartość: **`#EE5307`** = `--primary` (`hsl(19.7 94.3% 48%)`, round-trip co do
bajtu) = `spontaway.orange`.

- ✅ Używaj **`bg-primary` / `text-primary` / `border-primary` / `fill-primary`**.
- ⛔ **NIE używaj `*-orange-600`** jako primary - te 150 użyć zostało przepiętych na token.
- Reszta skali `orange-*` (50-500, 700-900) zostaje jako **paleta dekoracyjna** (tła, obwódki,
  gradienty) i NIE udaje primary.
- Wyjątki, które zostały świadomie: `from-orange-600/20`, `to-orange-600`, `shadow-orange-600/30`
  - tam kolor jest częścią efektu przy niskiej przezroczystości, nie wypełnieniem guzika.

### Claim / tagline

Oficjalny tagline aplikacji: **"speed dating z miastem"** (wszystkie litery małe, bez kropek na końcu). Używaj go w headerach stron marketingowych. Nie zastępuj innymi sformułowaniami bez wyraźnej prośby.

### Typografia

- **Główna:** Inter (wszystkie wagi)
- **Nagłówki marki / marketing (landing spontaway):** **Sigmar** (`font-brand`) - zastąpił
  Baloo 2 w warstwie marketingowej (decyzja Nat 2026-09-02). Sigmar ma jedną wagę i pełne
  polskie znaki (subset latin-ext). Ten sam font jest już w apce na liczniku „+N".
- **Akcenty nagłówkowe w apce** (nagłówki sekcji w dzienniku, karty tras): Baloo 2 (`font-display`)
- NIE używaj innych fontów bez wyraźnej prośby

### Przyciski

- **Primary:** **SOLIDNY pomarańczowy fill** (`bg-primary`, jedyny poprawny token), zaokrąglenie **16px** (`rounded-2xl`). ⛔ **ZAKAZ gradientu na guzikach** (`linear-gradient(#F4A259 → #F9662B)` itp.) - domyślny guzik MUSI być jednolicie pomarańczowy. Gradientowy guzik tylko gdy Nat wyraźnie napisze, żeby go wprowadzić (decyzja 2026-08-04). Gradient zostaje wyłącznie dla logo/orba/akcentów tła, NIE dla guzików.
- **Secondary:** **szary fill** `bg-secondary text-secondary-foreground` (styl YouTube - jasny szary, ciemny tekst). NIE biały+pomarańczowy stroke. Dotyczy wszystkich guzików akcji secondary oraz komponentów "paper" (karty sugerujące klik, np. karty miejsc we wpisie dziennika = `bg-secondary`).
- **Destrukcyjne:** `bg-destructive` (czerwony), tylko dla nieodwracalnych akcji
- Wszystkie przyciski obłe, `rounded-2xl` minimum
- NIE używaj prostokątnych buttonów bez zaokrągleń

### Karty i sekcje

- Zaokrąglenia kart powinny być **komplementarne** do zaokrągleń przycisków (nie identyczne)
- Karty: `rounded-2xl` lub `rounded-3xl`
- Sekcje z podkładem: subtelne `bg-muted` lub `bg-background` z `border border-border/30`
- Cienie: subtelne, `shadow-sm` lub `shadow-md` — NIE ciężkie cienie

### Logo / Orb

- Logo Trasy = sama orba (gradient pomarańczowy, kula)
- **NIE** dodawaj białego tła do orby
- **NIE** dodawaj napisu "trasa" obok orby bez wyraźnej prośby
- CSS orby: `radial-gradient(circle at 35% 35%, #fb923c, #ea580c 60%, #c2410c)`

### Znak spontaway (rebrand 2026-08-04) - sam pomarańczowy symbol, BEZ kółka

**Marka: spontaway** (tylko logo/znak - nazewnictwo „trasa/trasy" w UI zostaje jako zwykłe słowo = route/routes). Znak = faliste „S" (plik `/spontaway-symbol.png`, pomarańczowy `#F75708` na przezroczystym tle).

- **Sam symbol, NIE w kółku.** Kolor przewodni pomarańcz, ale symbol nie jest już zamknięty w krążku (poprzednia reguła „znak zawsze w pomarańczowym kółku" = NIEAKTUALNA).
- **Używaj komponentu `TrasaLogo`** ([src/components/TrasaLogo.tsx](src/components/TrasaLogo.tsx)). Prop `size` = rozmiar boxu w px (symbol `object-contain`). Prop `tone`: `"orange"` (domyślnie, symbol pomarańczowy na jasnym tle) lub `"white"` (biały symbol na pomarańczu/ciemnym tle, np. loading/splash - przez filtr `brightness(0) invert(1)`).
- **Warianty kolorów wg tła:** jasne tło (`#FEFEFE`) → pomarańczowy symbol. Pomarańczowy kafelek (ikona aplikacji) → BIAŁY symbol.
- **Ikona aplikacji (home screen) = NOWE LOGO od Nat (2026-09-15):** jeden plik `public/Logo_Spontaway.jpg` (1024) jest ŹRÓDŁEM PRAWDY - **płaskie żółte tło `#FDF184`** + **pomarańczowy znak `#F75708`**: faliste „S" z **GWIAZDKĄ** w prawym górnym rogu (wcześniej stała tam pinezka). Zastąpiło to żółto-złoty gradient z 2026-08-12 - ikona jest teraz PŁASKA, bez gradientu. **Wszystko generuje `python3 scripts/gen_app_icon.py`**: master `public/App icon IOS.png`, PWA `icon-192/512`, `apple-touch-icon` 180, `favicon.png` 48 + `favicon.ico`, **`Avatar_Trasa.png`** (domyślny awatar = ta sama ikona, `DEFAULT_AVATAR`) oraz `ios/.../AppIcon-512@2x.png`. Zmienia się logo → podmień TEN JEDEN plik i odpal skrypt, nie edytuj pochodnych ręcznie. ⛔ To NIE dotyczy **presetów awatarów** (`public/avatars/preset-*`) - tam są same kolory, bez znaku (decyzja Nat). **Ikona powstaje z PIKSELI Nat, nie z mojego obrysu - i Xcode jej NIE skaluje (2026-09-15).** Trzy osobne przyczyny fałszywego cieniowania, wszystkie usunięte: (1) **próg maski był przesunięty** - `alpha_from_yellow` miał rampę `LO=40 HI=140`, więc alfa 128 wypadała przy odległości 90 z 285, czyli na **32 %** drogi żółty→pomarańcz zamiast 50 %. Każdy kształt wychodził rozdęty o szerokość rozmycia JPEG: grubsze ramiona gwiazdki i płytsze wcięcia („gwiazdka wygląda inaczej, jakby była przerobiona"). Teraz `LO=110 HI=174`, środek dokładnie na 142. (2) **ikonę składałam z mojego obrysu wektorowego** - obrys to przybliżenie i na ikonie było je widać; teraz robi to `icon_from_mask()` z maski liczonej ze źródła, czyli z oryginalnych pikseli. Pomniejszamy MASKĘ (krycie) filtrem `Image.BOX` (uśrednienie pola), a kolor składamy dopiero potem - dzięki temu każdy piksel leży na odcinku `#FDF184`→`#F75708` i nie ma ringingu. ⛔ Nie skaluj gotowej ikony `LANCZOS`-em - to filtr wyostrzający, przestrzeliwuje na granicy dwóch kolorów i zostawia ciemny rant wzdłuż znaku. (3) **katalog miał JEDEN master 1024**, a resztę dorabiał `actool` swoim resamplerem - w ikonie 120 px, którą dostawał telefon, było **899 pikseli poza kolorami marki**. Teraz `write_ios_iconset()` kładzie **komplet 13 rozmiarów** (iPhone + iPad + marketing), więc `actool` tylko kopiuje. Zweryfikowane `actool --compile`: 0 pikseli poza kolorami marki, zero ostrzeżeń, żadna ikona nie ma kanału alfa (1024 z alfą = odrzucenie buildu przez App Store). ⚠️ Po zmianie ikony iOS trzyma starą w cache springboarda - **skasuj apkę z telefonu i zbuduj od nowa**, sam Cmd+R nie wystarczy. ⚠️ Ikony nie wgrywa się w App Store Connect: jedzie w środku buildu (także ta 1024 na stronę sklepu), więc podmiana = nowy numer builda + Clean Build Folder + Archive + Upload.
- **Splash / ekran ładowania = ZNAK SKŁADA SIĘ, gwiazdka na końcu (prośba Nat 2026-09-15):** natywny ekran startowy zostaje **jednolitym `#FEFEFE` BEZ znaku** (decyzja 2026-09-01, `capacitor.config` `backgroundColor: '#FEFEFE'`, `Splash.imageset` = czysty kolor) - znak rysuje React, więc natywny ekran tylko trzyma kolor, zanim WebView odmaluje pierwszą klatkę. [SplashDraw](src/components/layout/SplashDraw.tsx): „S" **odsłania się od lewej** (przesłona w kolorze tła zjeżdża w prawo - sam `transform`, więc animacja idzie po GPU w najcięższym momencie startu), a na jego końcu **ZAPALA SIĘ GWIAZDKA** (pop z przestrzeleniem skali), potem oddycha, dopóki apka się ładuje. ⛔ Zero `-webkit-mask-image`: WebKit na iOS gubi maski w animowanych warstwach (ta sama reguła co przy nakładkach awatara), a maska z pliku to dodatkowe zapytanie PRZED pierwszą klatką. Ścieżki „S" i gwiazdki mieszkają w **`src/components/spontawayMarkPaths.ts`** (GENEROWANE z logo przez `gen_app_icon.py` + `trace_mark_svg.py`, jeden układ współrzędnych `MARK_W`×`MARK_H`, więc warstwy stoją względem siebie jak w logo); rysuje je [SpontawayMark](src/components/SpontawayMark.tsx) (`SpontawayMark` = cały znak, `MarkStar` = sama gwiazdka). ⚠️ Przesłona działa, bo tło jest nieprzezroczyste i znane (`SPLASH_BG`) - zmieniasz tło splashu, zmień oba naraz. ⚠️ **Splash trwa o ~1 s dłużej, żeby dało się przeczytać podpis** (prośba Nat 2026-09-17): `HOLD_MS` w `SplashDraw` to 1240 ms, a `minElapsed` w `App.tsx` 1500 ms. Dwie stałe, bo `SplashPulse` nie ma animacji do odczekania i bez tej drugiej mrugał na pół sekundy. ⚠️ Obie są DOLNĄ granicą, nie górną - ekran i tak znika dopiero, gdy boot jest gotowy. [SplashPulse](src/components/layout/SplashPulse.tsx) (kolejne starty w ciągu 12 h) pokazuje TEN SAM znak z gwiazdką, nie `TrasaLogo` - inaczej dwa warianty ekranu startowego pokazywałyby dwa różne znaki. **U dołu obu wariantów stoi podpis** [SplashCredit](src/components/layout/SplashCredit.tsx): „Stworzone w Polsce" / „Made in Poland, Shared Worldwide" z pełnym brandowym sercem w pomarańczu (prośba Nat 2026-09-16). ⚠️ To NIE jest kalka: po polsku mówimy krócej, bo dla polskiego odbiorcy druga połowa jest oczywista, a po angielsku niesie cały sens - dwa różne teksty pod jednym kluczem `common:splash.made_in`. **Podpis jest złożony WERSALIKAMI** (`uppercase` + `tracking-[0.07em]`; decyzja Nat 2026-09-16). ⛔ Kapitaliki (`font-variant-caps: small-caps`) były próbowane tego samego dnia i odrzucone („nie pasują") - nie proponuj ich ponownie. Wielkie litery robi `text-transform`, więc treść w locale zostaje pisana normalnie. ⚠️ **Podpis jest MAŁY (12 px, waga 500, krycie 75 %) i tak ma zostać** - to AKCENT, nie komunikat (prośba Nat 2026-09-17). Podniesienie go do 15 px tego samego dnia było błędną diagnozą: problemem nie była wielkość.

⛔ **TRZY razy wracało zgłoszenie, że to kapitaliki, a to były wersaliki.** `font-variant-caps: small-caps` zniknęło już przy pierwszym zgłoszeniu (commit `d05f0701`) i nie ma go ANI w źródle, ANI w zbudowanym CSS, ANI w paczce natywnej - computed style w WebKit oddaje `uppercase` / `fontVariantCaps: normal`. Skoro nie dało się tego odtworzyć, zamknięte zostały WSZYSTKIE furtki naraz:
1. **tekst w locale jest już zapisany WIELKIMI LITERAMI** (`STWORZONE W POLSCE`), więc wielkość liter nie zależy od tego, czy `text-transform` gdziekolwiek zadziała. ⛔ To świadomie łamie regułę „treść w locale pisana normalnie, wielkie litery robi CSS" - po trzech podejściach pewność jest ważniejsza od elegancji. Klasa `uppercase` ZOSTAJE jako zabezpieczenie przed tłumaczem piszącym minuskami;
2. `fontVariantCaps: normal` i `fontFeatureSettings: normal` wprost - gaszą kapitaliki nawet gdyby szły z dziedziczenia albo z funkcji OpenType fontu;
3. **odstęp międzyliterowy zszedł z .07em na .035em** - szeroko rozstrzelone małe wersaliki to jest dokładnie wizualny podpis kapitalików, więc przy tej wielkości litery czytały się jak kapitaliki, będąc wersalikami.

⛔ Dawny `clamp(12px, 3.4vw, 14px)` nie wraca: miał chronić angielską wersję przed wyjechaniem poza ekran iPhone'a SE, a nie chronił przed niczym - przy .07em zdanie zawijało się na dwie linie także przy 12 px. Po zejściu z odstępem mieści się w jednej linii nawet na SE. Zmierzone w WebKit na zbudowanym bundlu: przepełnienie 0 px na SE, 16 i Pro Max, w obu językach. ⛔ **ZERO `opacity` w animacjach znaku** (prośba Nat 2026-09-15: „chcę żeby S było w jednolitym kolorze pomarańczowym `#F75708`"). Do 15.09 `splash-pulse` chodził **0,72 → 1 krycia**, a `#F75708` przy 72 % na białym to `(249,134,77)` - bladszy pomarańcz wracający w kółko do pełnego. Żaden gradient nie był nigdzie zadeklarowany (źródłowe logo, ikona i ścieżki SVG są płaskie `#F75708` - sprawdzone pikselowo), ale **animowane krycie wygląda dokładnie jak gradient**. Znak ma być ZAWSZE kryjący, ruch niesie sama skala - dotyczy `splash-pulse` i `splash-star-breath`. Jedyny wyjątek to WEJŚCIE gwiazdki (`splash-star-pop`): krycie idzie 0 → 1 raz, pełne już na 18 % animacji, i zostaje na 1.
- **Landing B2C też ma znak Z GWIAZDKĄ (2026-09-15):** `public/logo.svg` (znak w modalu pobrania, kafelku baneru instalacyjnego i arkuszu zapytania od lokalu) oraz lockup `public/wordmark_spontaway.svg` (belka + karta CTA w stopce) są przegenerowane z **tych samych ścieżek co apka natywna** (`src/components/spontawayMarkPaths.ts`), tylko w pomarańczu landingu `#EE5307`. W lockupie „S" zostało w dawnym rozmiarze, a gwiazdka **wystaje ponad napis**, więc ramka pliku urosła z `240x43` na `240x47,3` - klasy wysokości przy każdym `<Wordmark>` są przeskalowane o te same ~10 %, żeby sam napis nie zmienił rozmiaru. Oba pliki są używane WYŁĄCZNIE przez landing, więc podmiana nie rusza apki. **Obrazek do udostępniania `public/baner-ios.png`** (og:image landingu i stron `/r/`, `/p/`) też jest nowy: żółte tło + znak z gwiazdką + napis, zamiast dawnego przyciętego „S" bez gwiazdki; adresy w `index.html` i `api/share.ts` mają `?v=2`, bo komunikatory cache'ują podglądy po URL (favicon analogicznie `?v=7`). ⛔ `TrasaLogo` (`/spontaway-symbol.png`) NADAL jest bez gwiazdki i zostaje tak w całej apce - gwiazdka żyje w ikonie, na splashu i na landingu.
- **Ikona panelu ops (`admin.spontaway.com`) = ODWRÓCONE kolory ikony aplikacji** (2026-09-04): kafelek **gradient pomarańczowy** (`#F75708` → `#F9662B`) + **ŻÓŁTE „S"** (`#FDF184`). Celowa różnica, nie niespójność: zakładka panelu zapisana na ekranie domowym telefonu wyglądała identycznie jak apka. NIE ujednolicaj jej z ikoną apki. Pliki `public/ops-icon-1024/180.png` + `ops-favicon.png` żyją **tylko na gałęzi `admin`** (panel buduje się wyłącznie z niej); generator `scripts/gen_ops_icon.py`. `OpsLogo` rysuje ten sam plik co `apple-touch-icon` w `admin.html`, żeby nagłówek panelu i skrót na telefonie się nie rozjechały.
- Wyjątek: **orba** (kula z gradientem) to osobny element - jej NIE ruszamy.
- **B2B branding + logo:** logo marki (pomarańczowy symbol) pojawia się nawet w niebieskim kontekście biznesowym (nagłówki auth, SetPassword, onboarding). Świadomy wyjątek od „B2B = tylko niebiesko".
- **Historia:** do 2026-08-04 znak = „T" w pomarańczowym kółku (wariant reverse, biały znak na gradiencie). Rebrand na spontaway: sam pomarańczowy symbol bez kółka.

---

## MVP Scope

### B2C (Użytkownicy)

1. **Wybieranie miejsc** — sesja solo lub grupowa, wybór miasta i kategorii
2. **Dopasowania** — miejsca wybrane przez użytkownika (solo) lub przez wszystkich członków grupy
   - ⛔ ZAKAZ: słowa "swipe", "match" (jak w Tinder)
   - ✅ Używaj: "przeglądanie", "eksploracja", "dopasowania", "dodanie do trasy"
3. **Tworzenie trasy** — z wybranych/dopasowanych miejsc
4. **Podsumowanie podróży** — plan vs rzeczywistość, **notki** o miejscach (solo lub przez grupę)
   - ⛔ **ZAKAZ ocen gwiazdkowych miejsc:** Użytkownik NIE wystawia żadnych ocen (gwiazdek/punktów) miejscom. Bazujemy WYŁĄCZNIE na wartościowych **notkach** userów. Nie dodawaj inputu oceny w podsumowaniu, dzienniku, wizytówce ani nigdzie indziej. (Gwiazdki Google na kartach to zewnętrzny rating do wyświetlania — to co innego, zostaje.)
5. **Wyjazdy** (dawny „Dziennik") — trasy usera z „pocztówkami"/wpisami (Baloo font na nagłówkach kart). Mieszkają w zakładce **Wyjazdy** na profilu (`/moj-profil`); osobny ekran `/dziennik` USUNIĘTY 2026-08-20 (patrz sekcja „Nawigacja i architektura informacji").

### B2B (Firmy)

1. **Profil biznesowy** — wizytówka lokalu widoczna w trasach użytkowników
2. **Feed / wydarzenia** — aktualizacje, promocje (tylko pakiet Premium)
3. **Galeria zdjęć** — zarządzanie bazą zdjęć lokalu
4. **Analityka** — kliknięcia w szczegóły, dodania do trasy, oceny

---

## Nawigacja i architektura informacji (IA) — aktualne (2026-09-11)

**BottomNav (native) = 4 pozycje (decyzja Nat 2026-09-13):** pill z ikonami `Eksploracja` · `Miejsca` · `Profil` + OSOBNE kółko `+` po prawej (wzór: nawigacja COSMOS; tworzenie to akcja, nie zakładka). **Ikona + PODPIS pod nią** (10 px; same ikony okazały się za mało czytelne - prośba Nat, wieczór 2026-09-13). Pill jasny (szkło, `NAV_PILL` w [BottomNav.tsx](src/components/layout/BottomNav.tsx)) - wzór jest ciemny, ale ciemne tła są poza marką; target 68 × 52 px, pill h-16, `+` = `Ikona_Dodaj_orange.svg` na całą wysokość (64 px). Ikony z brandowego zestawu SVG (`Ikona_Eksploracja` = kompas, `Ikona_Miejsca`, `Ikona_Profil`, przez `NavIcon` = CSS mask + currentColor). **Ikona ma 19 px** (`NAV_ICON`, zejscie z 22 px na prosbe Nat 2026-09-15) - pasek i target 68 x 52 px BEZ ZMIAN, caly zapas poszedl w oddech wokol ikony. **Eksploracja jest pierwsza i STARTOWA** (route `/eksploruj`). **Zakładka „Główna" (siatka 2 kolumny, `ExploreGrid`) i osobny feed obserwowanych (`/feed`, `Feed.tsx`) USUNIĘTE 2026-09-13** - został jeden widok odkrywania; `/feed` przekierowuje na `/eksploruj`, flaga `followingOnly` w `DiscoveryFeed` zostaje nieużywana. **Zasada „jedno zadanie na widok":**
- **Eksploracja** → `/eksploruj` ([Explore.tsx](src/pages/Explore.tsx)) = **jedna kolumna kafelków** wyjazdów i list od WSZYSTKICH (`DiscoveryFeed city="all"`; **najpierw treści od obserwowanych, pod nimi reszta świata** - dwa koszyki po `following-ids`, w każdym przeplot wyjazd/lista po dacie publikacji), karta po karcie ze snapem (`snap-y snap-mandatory` na scrollerze tylko poza szukaniem). Kafelki = [FeedTiles.tsx](src/components/home/FeedTiles.tsx) (`GridTile size="feed"`, makieta Nat 2026-09-13): **WYJAZD** = okładka ZAWSZE **9:16** (`aspect-[9/16]`, `object-cover`; bez karuzeli zdjęć - usunięta 2026-09-13), pomarańczowa pigułka autora (awatar z ramką + @nazwa) w lewym górnym rogu, kwadratowa mini-mapa **108 px** (`buildTripStaticMapUrl(pins, "200x200")`) w prawym; **tytuł 36 px, chipy 30 px / tekst 14 px** (wymiary z makiety „Majówka 2025", wieczór 2026-09-13; wcześniej 24 / 26 / 72 px) w **Inter Black (`font-black`, waga 900 dociagnieta w `index.html`)** na **gradiencie ciemniejszym o 20 %** (`from-black/90 via-black/40`, prosba Nat 2026-09-15: tytul lezy na gradiencie, nie na zdjeciu) - **tapnięcie ROZWIJA ją** do żywej `RouteMap` na 62 % okładki (te same markery co w wyjeździe; instancja Google montuje się DOPIERO po rozwinięciu, `Minimize2` zwija, pigułka autora chowa się na ten czas; przywrócone z `TrasaBigCard` na prośbę Nat 2026-09-13), tytuł + chipy u dołu (pinezka + liczba miejsc, miasto, dni); **LISTA** (układ od 2026-09-15: NAJPIERW podpis, pod nim okładki) = kolorowe tło z palety ([listThemes.ts](src/lib/listThemes.ts), `discovery_collections.theme`, NULL = stały kolor z id; autor wybiera w menu „…" listy → [ListThemeSheet](src/components/lists/ListThemeSheet.tsx); **18 kolorow + KOLOR WLASNY z pipety od 2026-09-15**) + mini-siatka miejsc 3 × 2 (przy > 6 „+N", chip kategorii = napis w feedzie / ikona w wariancie grid), **kafelki miejsc maja lekki cien** `shadow-[0_2px_8px_rgba(0,0,0,0.18)]` (2026-09-15, odkleja je od kolorowego tla; siedzi na warstwie PRZYCINAJACEJ, nie na zewnetrznej - ta musi zostac czysta dla gwiazdki „nowe miejsce") + pigułka autora + tytuł + chipy (**„odwiedzone przez AUTORA / wszystkie"** np. `9/13` z RPC `list_author_visit_counts(uuid[])`, migracja 20260913c, `fetchListVisitCounts` w [placeVisits.ts](src/lib/placeVisits.ts); **ZERO TEZ pokazujemy - `0/7`**, prosba Nat 2026-09-15: do tego dnia przy zerze zostawala sama liczba miejsc i kafelek wygladal, jakby licznika nie mial wcale, choc widok samej kolekcji (`HighlightChips`) pokazywal `0 / 7` od poczatku. Sama liczba zostaje WYLACZNIE, gdy `visitedCount` nie zostal podany - wtedy nie wiemy, ile autor odwiedzil, a `0` twierdziloby, ze nic; miasto). Bez bookmarka na kafelkach (zapis z widoku wyjazdu/listy). `TrasaBigCard` zostaje na profilach i w podglądzie udostępniania; przełącznik `FEED_TRIPS_AS_TILES` w [DiscoveryFeed.tsx](src/components/home/DiscoveryFeed.tsx) (`false` = stara pełnoekranowa karta). Przypięta wyszukiwarka zostaje (wyniki nadal renderuje `DiscoveryFeed` w trybie `searchOnly`). Flaga `SHOW_ZESTAWIENIA` = `true`.
- **`+` (FAB)** → `CreateFlowSheet` [Lista|Wyjazd] (bez zmian).
- **Skrót do wyjazdu = PIGUŁKA POD GÓRNĄ BELKĄ** ([ActiveTripBanner.tsx](src/components/home/ActiveTripBanner.tsx); makiety w Figmie: `[NEW] Ekrany` → „Skrót do wyjazdu na Eksploracji"). Wraca jednym tapnięciem do wyjazdu **w trakcie**, a gdy takiego nie ma - do najświeższego **roboczego**. ⚠️ **Miejsce wracało dwa razy**: do 16.09 nakładka na feedzie, 16.09 → 17.09 pigułka nad dolną nawigacją (kierunek A z eksploracji), od 2026-09-17 znów góra - na prośbę Nat. Wróciło MIEJSCE, nie tamten układ: kolory marki, biała obwódka i wąska pigułka zostają, bo rozwiązywały realne problemy. Stan aktualny:
  - **Szerokość: pigułka HUGUJE treść i stoi na środku** (zgłoszenie Nat 2026-09-17: rozciągnięta na całą szerokość „nachodziła brzydko na okładki"). Guzik ma `max-w-[min(300px,100%)] min-w-0`, kolumna tekstu `min-w-0 flex-1` + `truncate`. ⛔ Nie wracaj do `w-full` - kontener zostaje pełnej szerokości (niesie gest), wąskie są guziki w środku. ⚠️ `min-w-0` na pigułce jest konieczne: na iPhonie SE pigułka + krzyżyk nie mieszczą się w 296 px i bez tego rząd wyjeżdża poza ekran zamiast skrócić tytuł.
  - **Miejsce: W UKŁADZIE, między `TabTopBar` a scrollerem feedu** - zwykły blok, ZERO `fixed`/`absolute`. To kasuje u źródła powód, dla którego wersja sprzed 16.09 miała `pr-28`: skrót nie leży na okładce, tylko stoi nad nią, więc nie ma czego omijać (ani mini-mapy, ani pigułki autora), i nie trzeba liczyć odstępu od wysokości `BottomNav`. Siedzi POZA `PullToRefresh`, więc feed przewija się pod nim, a on zostaje.
  - **Jest widoczny ZAWSZE** (poza otwartą wyszukiwarką). Chowanie po przewinięciu feedu zdjęte razem ze stanem `feedScrolled` w `Explore.tsx` - skrót, którego trzeba szukać, przestaje być skrótem.
  - **Kolory z marki:** tło `#FDF184`, tekst i chevron `#5B2C06`, pomarańcz `#EE5307` **wyłącznie** jako kropka 6 px + wersaliki `W TRAKCIE` (na żółtym ma 3,08:1, więc nigdy nie niesie dłuższego tekstu). ⛔ Zero `bg-card` / `bg-muted` - to były jedyne szarości shadcn w Eksploracji.
  - **Separacja przez BIAŁĄ OBWÓDKĘ 2 px, nie cień.** To nie jest dekoracja: żółty `#FDF184` siedzi też w palecie kafelków kolekcji, więc bez obwódki żółty skrót na żółtej kolekcji znika (sprawdzone w makiecie i w harnessie).
  - **KRZYŻYK obok pigułki** (prośba Nat 2026-09-17: „żeby userzy mogli w prosty sposób zamknąć ten baner") - osobny guzik, to samo żółte tło i ta sama biała obwódka, **pełne 44 × 44 px**. ⚠️ Zdjęliśmy go 16.09 nie dlatego, że pomysł był zły, tylko dlatego, że siedział WEWNĄTRZ pigułki na całą szerokość: zjadał tytuł i miał target 24 px. Osobny guzik obok tego nie powtarza. **Gest zostaje jako skrót i idzie teraz W GÓRĘ** (próg 36 px; ruch w dół i w bok oddajemy feedowi) - element stoi przy górnej krawędzi, więc „odsuń to z drogi" znaczy „schowaj pod belkę". ⛔ Znikanie to ZWINIĘCIE `max-height` (`STRIP_PX` = 64), nie wyjazd w górę: `TabTopBar` nie ma ani tła, ani `z-index`, a skrót stoi po nim w DOM, więc wyjeżdżająca pigułka przeorałaby belkę. Zwinięcie chowa ją pod własną krawędzią i przy okazji płynnie dosuwa feed. **Schowanie żyje TYLKO do zamknięcia aplikacji** (prośba Nat 2026-09-16): klucz `(id wyjazdu : etap)` siedzi w **pamięci modułu** (`dismissedThisRun`), nie w `localStorage` - przeżywa wyjście z Eksploracji i skakanie po zakładkach, ginie razem z procesem apki, więc **przy każdym nowym otwarciu skrót wraca**. ⛔ Nie przenoś tego z powrotem do `localStorage`: jedno machnięcie palcem kasowało wtedy skrót do niedokończonego wyjazdu na stałe. Stary klucz `trasa_trip_shortcut_hidden` jest czyszczony przy starcie modułu.
  - Nazwa zapasowa wyjazdu bez tytułu idzie przez `tripName()` z [placeNaming.ts](src/lib/placeNaming.ts) (polska odmiana), nie przez sklejanie `Wyjazd do {miasto}`.
- **Miejsca** → `/miejsca` ([Miejsca.tsx](src/pages/Miejsca.tsx)) = wizytówki miejsc (`ExploreSwiper`, dawny widok „browse" spod przełącznika). **W wierszu meta na okladce stoi ULICA · MIASTO** (`metaPlace` w `SwipeCard`, 2026-09-15) - wczesniej byl sam pierwszy czlon adresu, czyli ulica, a przy globalnej zakladce (`city="all"`) karty z Warszawy, Gdanska i Rzymu leza jedna na drugiej i po ulicy nie dalo sie poznac, gdzie sie jest. Dotyczy tak samo wizytowek premium (adres z panelu lokalu), jak i stanu zero. ⛔ Zrodlem miasta jest `place.city`, NIE prop `city` - ten bywa sentynelem `"all"`. W belce **przypięte pole wyszukiwarki + dzwonek** (jak w Eksploracji; 2026-09-14 - tytuł „Wizytówki" i sama lupka odrzucone), segment `Wydarzenia (wkrótce)` USUNIĘTY 2026-09-13 (wraca razem z realnym widokiem wydarzeń od klientów biznesowych). ⛔ Nad swiperem NIE może stanąć nic poza `TabTopBar` (zamrożony sizing karty 9:16 liczy się ze stałego chrome).
- **Profil** → `/moj-profil` (bez zmian). ⚠️ **W USTAWIENIACH górna belka ([TopBar](src/components/layout/TopBar.tsx)) ma CHEVRON WSTECZ zamiast awatara** (prośba Nat 2026-09-17): awatar prowadził na własny profil, czyli tam, skąd user właśnie przyszedł, a przy domyślnym zdjęciu (`Avatar_Trasa.png` = ikona aplikacji) wyglądał jak przypadkowy placeholder z logo. Przy okazji ZDJĘTA druga strzałka wstecz z `SettingsScreen` - byłyby dwa wejścia do tej samej czynności w odległości 40 px. ⚠️ Cel powrotu podstrony nie zginął: `SettingsScreen` publikuje go atrybutem **`data-settings-back`**, a belka pyta o niego DOM - ten sam wzorzec, co `data-scroll-main`, bo belka stoi NAD ekranem i nie zna jego propsów. ⛔ Warunek w `TopBar` to `location.pathname.startsWith("/settings")` - **trasa nazywa się `/settings`, a NIE `/ustawienia`**. Pierwsza wersja celowała w `/ustawienia` i nigdy nie była prawdziwa (hub wjechał pod `/settings` tego samego dnia), więc awatar wrócił na belkę i Nat zgłosiła to drugi raz.
- **Wyszukiwarka na KAŻDEJ zakładce (2026-09-11):** wspólny moduł [TabSearch.tsx](src/components/home/TabSearch.tsx) (`useTabSearch` + `TabSearchField` + `TabSearchButton` + `SearchPane`/`TabSearchResults`; wyniki = `DiscoveryFeed searchOnly`). **Bez frazy = lista kategorii JEDNA POD DRUGĄ** (`SearchCategoryList`: Listy / Wyjazdy / Miejsca / Ludzie, ikona + nazwa + podtytuł + strzałka, wzór FYI) zamiast rzędu folderów; tapnięcie w kategorię = wyniki tylko z niej (każda jako lista pionowa, nie siatka); strzałka w belce najpierw wraca do listy kategorii, potem zamyka szukanie. `SearchCategoryRow` (rząd folderów) nie jest już używany w wyszukiwarce. Feed: pole na całą szerokość belki (bez logo i tytułu). Eksploruj: własna belka `ExploreTopBar` (bez zmian). Miejsca: pole przypięte obok dzwonka (od 2026-09-14); w trybie wyników belka pokazuje tylko strzałkę + pole (wysokość belki stała = 52 px, swiper zostaje zamontowany pod spodem). **Wyszukiwarka Miejsc ma PASEK ZASIEGU: kraj + miasto** (`SearchScopeBar`, prosba Nat 2026-09-15 - „sama wyszukiwarka nie spelnia swojej funkcji", bo trzeba bylo znac nazwe lokalu z gory). Lista krajow i miast budowana z BAZY ([placeCities.ts](src/lib/placeCities.ts): distinct `places.city` dla aktywnych + przypisanie kraju z `TRIP_COUNTRIES`; miasto spoza tabeli laduje w „Pozostałe"), wiec nie da sie wybrac miasta bez wizytowek. Rzad miast pojawia sie PO wybraniu kraju; kraj z jednym miastem zaznacza je od razu. **Wybrane miasto samo jest zapytaniem**: bez frazy `SearchPane` pokazuje od razu miejsca tego miasta zamiast listy kategorii. Strzalka w belce cofa po jednym kroku: miasto → kraj → kategoria → zamkniecie. Pod spodem `DiscoveryFeed` dostaje `searchCity`, ktore zasila `cityFilter` - ⛔ to NIE jest powrot arkusza filtrow zdjetego 2026-09-10, tylko jedno wejscie z wyszukiwarki. `expandCity` dostaje tu doklejona sama wybrana nazwe, bo „Trójmiasto" to meta-miasto i piec wizytowek oznaczonych nim literalnie bylo inaczej nieosiagalne. Tryb wyników chowa BottomNav. **Dzwonek i lupka w belkach = OKRĄGŁE `h-9 w-9 rounded-full bg-muted/70 border border-border/50`** (2026-09-13, ten sam styl co pigułka szukania; wcześniej kwadraty `rounded-xl`).
- **Karta zaproszeń na profilu ([ReferralCard](src/components/profile/ReferralCard.tsx)):** **plaskie ZOLTE tlo marki `#FDF184`, promien 24 px, tekst brazowy `#5B2C06`** (powrot 2026-09-15). Ikona w bialym kwadracie 40 px, eyebrow, guzik primary pelnej szerokosci + kwadrat z kodem QR obok (oba `rounded-2xl`), „Nie teraz" pod spodem. ⛔ **NIE wracaj do wariantu „kupon"** (kreskowany szary obrys, OSTRE rogi, wzor „Get credits" z FYI) - probowany 2026-09-11 i odrzucony: na profilu pelnym zaokraglonych kafelkow ostra ramka czytala sie jak obcy element, a szarosc gubila baner w tle. ⛔ Nie wracaj tez do gradientu (odrzucony 2026-09-10 jako najglosniejszy element profilu).
- **Wyróżnione miejsca na profilu (2026-09-13):** trzecia statystyka w rzędzie Obserwujący / Obserwowani = **gwiazdka „topki" + licznik** miejsc, które user wyróżnił w SWOICH wyjazdach (`pins.is_top`) i listach (`discovery_items.is_top`, migracja 20260913b; toggle w menu wiersza w [SharedList](src/pages/SharedList.tsx)). **Gwiazdek jest BEZ LIMITU - w wyjazdach i kolekcjach** (decyzja Nat 2026-09-14; do tego dnia wyjazd miał `TOP_LIMIT = 1` i kolejny wybór przenosił gwiazdkę - stała nie istnieje, [topPlaces.ts](src/lib/topPlaces.ts) trzyma tylko historię decyzji). Tap → [StarredPlacesSheet](src/components/profile/StarredPlacesSheet.tsx) (lista miejsc + skąd gwiazdka, tap = wyjazd/lista). **Filtry: kraj · miasto · typ miejsca** (2026-09-15) - rzad chipow pojawia sie TYLKO dla wymiaru, ktory ma co najmniej dwie rozne wartosci (przy jednym miescie „Wszystkie / Warszawa" to sam halas). Kraj i miasto biora sie ZE ZRODLA (wyjazd / kolekcja), bo pin ich nie niesie; pozycja kolekcji ma wlasne `city` i ono wygrywa. **Także na profilu publicznym** (`own={false}` = opisy w trzeciej osobie), gdzie guzik „Obserwuj" to od 2026-09-13 **sama ikona** (`FollowButton iconOnly`: pomarańczowe kółko z `UserPlus`, po zaobserwowaniu szare z `UserCheck`) - trzy statystyki w rzędzie nie zostawiały miejsca na pigułkę z tekstem. **Zgłoszenie profilu = sama flaga w górnej belce po prawej**, obok „⋮" z blokadą (oba przeniesione z rzędu statystyk, który przy trzech licznikach wystawał poza ekran). Dane: [starredPlaces.ts](src/lib/starredPlaces.ts), klucz `["starred-places", userId]` - inwaliduj po każdym toggle'u gwiazdki. **Jedno miejsce = jeden wiersz (prośba Nat 2026-09-21):** `dedupeStarred` scala gwiazdki po znormalizowanej nazwie (ta sama tożsamość co `place_note_key`), źródła sumuje w `sources` (wiersz pokazuje pierwsze + „· i jeszcze N"), licznik na profilu liczy MIEJSCA, nie gwiazdki.
- **ZNAJOMY = WZAJEMNA OBSERWACJA (decyzja Nat 2026-09-17, migracja `20260917a`).** Licznik **„Znajomi"** wchodzi w rzedzie statystyk na miejsce „Obserwowanych" (rzad miesci TRZY pozycje - przy trzeciej trzeba bylo wyprowadzic flage zgloszenia i „⋮" do belki, czwarta sie nie miesci), a „Obserwowani" przenosza sie do PRZELACZNIKA wewnatrz arkusza ludzi (Obserwujacy | Znajomi | Obserwowani). Zrodlem prawdy jest BAZA: `friend_ids(p_user)` i `are_friends(u1,u2)` - tej samej funkcji uzywaja polityki RLS bramkujace tresc, wiec klient NIE MOZE liczyc znajomych sam (predzej czy pozniej pokazalby liste inna niz ta, ktora naprawde otwiera zdjecia). Warstwa danych: [friends.ts](src/lib/friends.ts).
  - ⛔ To NIE jest `useFriends` z [useFriends.ts](src/hooks/useFriends.ts) - tamten czyta STARA tabele `friendships` (model zaproszenie + akceptacja, porzucony 2026-07-26, 4 wiersze na prodzie). Zostaje wylacznie dla linku zapraszajacego `/dodaj/:code`. Podpowiedzi przy zapraszaniu (`AddPeoplePicker`, `InviteFriendsSheet`) zeszly na nowa definicje - inaczej profil pokazywalby 22 znajomych, a arkusz zaproszen 4.
  - Dlaczego nie model z AKCEPTACJA: w dniu wdrozenia bylo 22 pary wzajemne wobec 4 zaakceptowanych znajomosci, wiec tresc „tylko dla znajomych" nie mialaby kogo ogladac; na cudzym profilu stanelyby DWA guziki relacji (Obserwuj + Dodaj do znajomych); i bylby to powrot do modelu porzuconego dwa miesiace wczesniej.
  - ⚠️ **`friend_excludes` zatyka dziure tego modelu.** Odwzajemnienie obserwacji z grzecznosci dawaloby komus dostep do tresci prywatnej, a jedynym odcieciem byloby odobserwowanie - czyli sygnal PUBLICZNY (znikasz z jego listy obserwujacych) i spolecznie kosztowny. Teraz z zakladki „Znajomi" wypisujesz kogos JEDNOSTRONNIE i po cichu; dziala od razu, z „Cofnij" w toascie.
  - ⚠️ **Wykluczenie zrywa znajomosc W OBIE STRONY** - nazwa funkcji mowi „czy sa znajomymi", wiec musi byc symetryczna, inaczej ten sam kod bramkujacy dawalby rozne odpowiedzi zaleznie od tego, kto pyta. Kto kogos wypisuje, sam tez przestaje widziec jego tresc dla znajomych.
  - ⛔ **Wykluczenia stosuje sie WYLACZNIE do WLASNEJ listy.** `followers` jest publiczna, wiec wzajemne obserwacje kazdy policzy sam; gdyby cudza lista je uwzgledniala, z roznicy dalo by sie odczytac, KOGO ta osoba wypisala. Publicznie relacja wyglada dalej na znajomosc, zmienia sie tylko to, co `are_friends` wpuszcza do tresci.
- **Awatary brandowe (2026-09-13):** 12 presetów = **same kolory** z palety marki (bez znaku „S" - wersja ze znakiem odrzucona przez Nat), generator `scripts/gen_avatar_presets.py` → `public/avatars/preset-<id>.png` + kopie w buckecie `avatars/presets/v2/` (wersja w ścieżce, bo pliki idą z cache na rok; profil zapisuje PEŁNY URL jak przy własnym zdjęciu). Lista id + URL: [avatarPresets.ts](src/lib/avatarPresets.ts); rząd do wyboru [AvatarPresetRow](src/components/profile/AvatarPresetRow.tsx) w kroku „zdjęcie" onboardingu i **w arkuszu „Customizuj" NA DOLE, pod nakładkami i kolorem** (prop `flush`, zapis od razu do `profiles.avatar_url` - prośba Nat 2026-09-13; wcześniej rząd siedział w formularzu Ustawień, tam go już NIE ma). Nowy preset = wpis w `AVATAR_PRESET_IDS` + wygenerowany i wgrany plik.
- **Ramki awatara (2026-09-11):** `profiles.avatar_frame` (`stars` | `hearts` | `clouds` | null) + `profiles.avatar_frame_color` (`#RRGGBB` | null = pomarańcz marki `#EE5307`; migracje 20260911f/g - kolumny PUBLICZNE jak `avatar_url`, dopisane do kolumnowych grantów SELECT/UPDATE). Wejście: Ustawienia, guzik „Customizuj" **bezpośrednio pod awatarem** (ikona = żywa miniatura nakładki, nie statyczny sparkle) → [AvatarFrameSheet](src/components/profile/AvatarFrameSheet.tsx): podgląd na własnym zdjęciu, zapis od razu, kolor = szybkie kolory + pipeta (`<input type="color">`, dowolny kolor). **Wszystkie nakładki domyślnie pomarańczowe**, jeden kolor na całą ramkę (`currentColor`). **NAKŁADKI-PREZENTY (2026-09-16, migracje `20260916e` i `20260916g`):** `moonstars` (księżyc i gwiazdki) oraz `banana` NIE DA SIĘ zdobyć - odblokowuje je wyłącznie imienny wpis w `frame_grants` (moonstars: założycielka + `msrebellefleur`, banana: `maria`). Arkusz **CHOWA** prezent przed każdym, kto go nie dostał - kłódka bez żadnej drogi do odblokowania to sama frustracja; tym różnią się od `rainbow`, która stoi na liście z postępem „0 z 3", bo JEST do zdobycia. Obdarowanego wita RAZ [FrameGiftSheet](src/components/profile/FrameGiftSheet.tsx) (żółty arkusz, nakładka na jego zdjęciu w 104 px, guzik od razu ją zakłada). ⚠️ „Widziane" trzyma BAZA (`frame_grants.gift_seen_at` + RPC `mark_frame_gift_seen`), nie localStorage: prezent ma być jednorazowy także po reinstalacji i na drugim telefonie. ⛔ Lista prezentów żyje w DWÓCH miejscach i muszą się zgadzać: `gift: true` w `AVATAR_FRAMES` oraz SQL (`frame_unlocked` i `my_frame_gift`). Do `my_frame_gift` NIE wpisuj `rainbow` - to nagroda, a nie prezent, i modal z podziękowaniem wyskoczyłby za nią założycielce.

**Nakładka-NAGRODA `rainbow`** (tęczowy świecący PIERŚCIEŃ - kółko, nie kwadrat; kolor usera bez znaczenia) odblokowuje się po zaproszeniu `REFERRAL_GOAL` (3) osób albo z grantu w `frame_grants` (Nat ma grant). Blokadę egzekwuje **baza**: trigger `guard_avatar_frame` → `frame_unlocked(uid, frame)` odrzuca zapis (`frame_locked`); arkusz czyta stan z RPC `my_frame_unlocks()` i pokazuje kłódkę + postęp „0 z 3". Baner zaproszeń na profilu obiecuje właśnie tę nakładkę (migracja 20260911h). Nowa nagroda = wiersz w `AVATAR_FRAMES` z `reward: true` + gałąź w `frame_unlocked`. Render: [AvatarFrame](src/components/profile/AvatarFrame.tsx) = nakładka `pointer-events-none` obok `<Avatar>` w elemencie `relative`; znaczki LEŻĄ na krawędzi zdjęcia (orbita 0,94 × średnica), 14 s/obrót, kontr-rotacja, `motion-reduce`. ⛔ Gwiazdka w nakładce = **INLINE svg** (`StarGlyph`, ścieżka z `Ikona_Gwiazdka.svg`), NIE `BrandIcon`/CSS mask - WebKit na iOS gubił `-webkit-mask-image` wewnątrz animowanej warstwy (gwiazdki znikały w arkuszu udostępniania i na kafelkach eksploracji, 2026-09-13). Pipeta koloru w arkuszu = SAM natywny `<input type="color">` rozciągnięty na kropkę (bez guzika pod spodem - podwójne otwarcie palety) + podgląd na żywo z `draft`, zapis odroczony 600 ms / na `change` (iOS strzela `input` przy każdym ruchu palca). Małe awatary w nagłówkach wyjazdu i listy przez [FramedAvatar](src/components/profile/FramedAvatar.tsx). Lista wariantów = [avatarFrames.ts](src/lib/avatarFrames.ts). **Od 2026-09-16 doszły trzy darmowe: `moon` (księżyc), `moonstars` (księżyc i gwiazdki NAPRZEMIAN - 4 pozycje na orbicie nie pomieszczą obu kompletów, a przeplot dopiero czyta się jak nocne niebo) i `banana`.** ⚠️ **`banana` ma WŁASNE kolory** (`fixedColor` w `AVATAR_FRAMES`: żółty marki `#FDF184` + brązowy obrys `#5B2C06`) i ignoruje pipetę - banan musi być żółty. Arkusz chowa wtedy cały wybór koloru, żeby nie obiecywać zmiany, która nie nastąpi; drugą taką nakładką jest `rainbow`. Brązowy obrys nie jest ozdobą: przy 9 px w nagłówkach płaska żółta plama na jasnym tle nie ma żadnej krawędzi (ta sama sztuczka co biały obrys chmurki). ⚠️ Banan jest **obrócony o -115°**, bo z natury jest półksiężycem i w pionie miał DOKŁADNIE ten sam zarys co nakładka „księżyc" - obie stoją obok siebie na liście i różniłby je sam kolor; położony poziomo ma inną oś i inną sylwetkę (sprawdzone renderem w WebKit przy 76 i 24 px). **Widoczna WSZĘDZIE, gdzie jest awatar osoby (prośba Nat 2026-09-11):** własny profil (76 px, BEZ plakietki aparatu - zasłaniała nakładkę; tapnięcie w awatar prowadzi do Ustawień, gdzie żyje zmiana zdjęcia i „Customizuj"), profil publiczny, karty list i wyjazdów na obu profilach (własne + zapisane od innych), autor na kartach feedu/wyszukiwarki (`AuthorChip`, `TrasaBigCard`), wiersze ludzi (wyszukiwarka, polecani), nagłówki wyjazdu i listy, **pigułka autora w lewym górnym rogu kafelka w Eksploracji** (`FeedTiles` `AuthorPill`, awatar 24 px; ukryta dla wyjazdów anonimowych) oraz **arkusz udostępniania** (awatar autora z nakładką po prawej od „udostępnij", `ShareSheet author`). Ramkę cudzego usera bierz przez [avatarFrameLoader](src/lib/avatarFrameLoader.ts) (`useAvatarFrame(userId)` - zbiera pytania z jednego renderu w JEDNO zapytanie `in(...)`, cache 5 min, klucz `["avatar-frame", userId]`) albo gotowe komponenty w [FramedAvatar.tsx](src/components/profile/FramedAvatar.tsx): `FramedAvatar` (ramka podana wprost), `UserAvatar` (ramka po `userId`), `UserFrameRing` (sama ramka obok istniejącego `<Avatar>`). NIE dopisuj kolumn `avatar_frame*` do kolejnych zapytań o autorów - podaj `userId`. ⛔ **Nowy wariant = CZTERY miejsca, nie dwa:** wpis w `AVATAR_FRAMES` + glyph w `AvatarFrame` + **CHECK `profiles_avatar_frame_check`** + **funkcja `frame_unlocked`** (kończy się `return false`, więc ODRZUCA wszystko, czego nie wymieniono w środku - bez dopisania trigger `guard_avatar_frame` wywala zapis błędem `frame_locked`, co w apce wygląda jak losowa awaria zapisu) + etykiety `frames.<id>` w `settings.json` PL i EN. Wzór: migracja `20260916d`. Strażnik typu `isAvatarFrame` liczy się już Z LISTY (`FRAME_IDS`), więc jego nie trzeba ruszać.
- **Arkusz udostępniania KOLEKCJI pokazuje jej MINIATURĘ, czyli prawdziwy kafelek (prośba Nat 2026-09-15).** `ShareCardList` renderuje teraz `ListTile` z [FeedTiles](src/components/home/FeedTiles.tsx) - kolor przewodni kolekcji, pigułka autora, chipy i okładki miejsc, dokładnie to, co widać w Eksploracji i na profilu. Wcześniej była tu własna biała karta z siatką 3 × 3 (`ListShareTile`, usunięty): wyjazd pokazywał swoją kartę z eksploracji, a kolekcja coś, czego nie ma nigdzie indziej w produkcie. ⛔ Nie duplikuj tu układu kafelka - `ListTile` jest jeden i ma się zmieniać w jednym miejscu. ⚠️ Kafelek dostaje **białą oprawę** (`rounded-[28px] bg-white p-2`) i to nie jest ozdoba: arkusz ma tło `#FDF184`, a dokładnie ten kolor (oraz złoty `#FDCD84` i kremowy `#F7E8D0`) jest w palecie kolekcji - bez oprawy kolekcja w żółtym motywie znikałaby w tle bez żadnej krawędzi. **Strona linku do KOLEKCJI (`/l/<id>`, [api/share.ts](api/share.ts)) ma kolor kolekcji** (2026-09-16): biała oprawa zostaje, a w środku siedzi kafelek w motywie kolekcji - paleta idzie **importem z [listThemes.ts](src/lib/listThemes.ts)** (moduł nie ma żadnych zależności), więc apka i strona mają JEDNO źródło kolorów. ⚠️ Biała oprawa jest konieczna: tło strony to żółty marki, który jest też w palecie kolekcji. ⛔ **Nazwy klas CSS na tej stronie muszą być rozłączne między widokiem wyjazdu a widokiem kolekcji** - jeden `<style>` obsługuje oba. Kafelek „+N" w siatce kolekcji nazywał się `.more`, tak samo jak chip „Jeszcze N miejsc" z wyjazdu (`width:150px;height:104px;border:2px dashed`), i to on wygrywał: trzecia kolumna siatki rozciągała się do 150 px, kafelki w niej robiły się 150×200 zamiast 76×101 i cała siatka się rozjeżdżała (zgłoszenie Nat 2026-09-16, zmierzone w WebKit na żywej stronie). Teraz `.tl.plus`, a siatka ma `repeat(3,minmax(0,1fr))` - tor `1fr` rośnie do najszerszego dziecka, `minmax(0,…)` nie może. ⛔ **W komentarzach CSS na tej stronie NIE używaj backticków** - cały arkusz żyje w template literalu i pierwszy backtick zamyka string (tsc pokazuje wtedy błędy składni kilkanaście linii dalej).

**KAFELEK KOLEKCJI NA PROFILU POKAZUJE SAM SKLAD, NIE PIGULKE AUTORA (2026-09-17, wzor: klaster tworcow przy filmie na YouTube).** Na **wlasnym profilu i na profilach publicznych** `GridTile` dostaje `people="avatars"` i rysuje `PeopleCluster` w [FeedTiles](src/components/home/FeedTiles.tsx): do **czterech awatarow, BEZ nakladek, bez nazwy i bez handle autora**, autor pierwszy i na wierzchu. W **EKSPLORACJI i w udostepnianiu pigulka z nazwa ZOSTAJE** (`people="pill"`, domyslne) - tam autor jest informacja, a na profilu jego nazwa stoi juz w naglowku ekranu i pigulka powtarzala to samo, zabierajac pol rzedu chipow.
- **Uklad zalezy od liczby osob** (`CLUSTER_POS`): sztywna siatka 2x2 przy dwoch osobach zostawiala pusty dolny rzad i klaster czytal sie jako obciety, przy trzech - jako brakujacy czwarty.
- **Kolekcja JEDNOOSOBOWA dostaje MNIEJSZE pudelko** (32 px zamiast 44): awatar na cale 44 px byl ciezszy niz cztery twarze w klastrze, a to wlasnie kolekcja jednoosobowa jest przypadkiem domyslnym - w „Moje kolekcje" kazdy kafelek pokazywalby moje wlasne zdjecie wieksze niz cokolwiek innego na tym ekranie.
- Obwodka awatara w kolorze **KAFELKA**, nie biala - to ona rozdziela sasiadujace twarze, a biel na kremowym czy piaskowym motywie nie rozdzielalaby niczego.
- Piata i dalsze osoby ida w licznik `+N` obok klastra: bez niego kolekcja dwunastu osob wygladalaby dokladnie tak samo jak czteroosobowa.
- Nakladki (`avatar_frame`) sa tu ZDJETE swiadomie (prosba Nat): cztery ramki po 24 px, kazda z wlasnymi gwiazdkami i kolorem, zlewaly sie w plame i to one rzucaly sie w oczy zamiast twarzy. Wszedzie indziej, gdzie awatar stoi sam i ma na nia rozmiar, nakladka zostaje.
- Dotyczy takze podzakladki **Zapisane** (kolekcje od innych), gdzie autor NIE jest wlascicielem profilu - dwa rozne traktowania kafelka na jednym ekranie czytalyby sie jak blad. Sprawdzone renderem w WebKit przy 1/2/3/4/7 osobach na trzech motywach kolekcji.

⛔ Okładkę miejsca bierz z `photo_url` (widok kolekcji ma je już przeliczone przez `pinCover`) i dopiero potem z `rowOwnPhotos`. **NIE czytaj `item._cover`** - to pole ustawiają wyłącznie zapytania feedu i profilu, w widoku kolekcji go NIE MA, a stara karta udostępniania właśnie je czytała, przez co WSZYSTKIE miniatury miejsc były puste (zgłoszenie Nat 2026-09-15).
**KARTA UDOSTEPNIANIA JAKO OBRAZ: Instagram + „Pobierz PNG / JPG" (prosba Nat 2026-09-21).** W rzedzie kanalow arkusza ([ShareCard](src/components/share/ShareCard.tsx), [shareTargets.tsx](src/components/share/shareTargets.tsx)) dla planu, kolekcji i miejsca doszly trzy kafelki obrazowe: **Instagram** (nie przyjmuje linkow - dostaje JPG karty), **Pobierz PNG** i **Pobierz JPG** (pod Pinteresta, Stories, wszystko, co chce pliku). Render = [shareImage.ts](src/lib/shareImage.ts): `modern-screenshot` (`domToCanvas`) na DOKLADNIE tym DOM-ie, ktory user widzi w podgladzie (`exportRef` w `ShareSheet`), ~1080 px szerokosci, zolte tlo marki z marginesem 28 px domalowanym NA PLOTNIE. Natywka: `@capacitor/filesystem` (katalog Cache) + `Share.share({ files })` = systemowy arkusz z plikiem (tam Instagram, Pinterest, „Zapisz obraz"); web: `navigator.share` z plikiem albo `<a download>`. Sprawdzone renderem w WebKit dla wszystkich trzech kart (harness z podstawionymi pluginami Filesystem/Share).
- ⚠️ Kadr = TRESC, nie pudelko (`contentBounds`): kontener podgladu ma `h-full`, a kafelek kolekcji jest nizszy - liczymy sume prostokatow potomkow, ktore cos MALUJA (`paints()`: tlo/obrys/cien/obraz/tekst). Przezroczysty wrapper flex nie liczy sie, inaczej pudelko 9:16 miejsca dostawalo z boku pas pustego tla.
- ⚠️ Margines tla domalowujemy na plotnie, NIE `style.transform` na klonie - biblioteka maluje `backgroundColor` pod klonem, nie pod calym plotnem, i przesuniety klon zostawial czarny pas u gory i po lewej.
- ⚠️ W klonie GASIMY animacje (`onCloneEachNode`: `animation: none`): nakladka awatara (orbita serc/gwiazdek) raz byla w obrazie, raz nie, bo animacja w svg startowala od zera. Z `animation: none` zostaje `transform` skopiowany z chwili zrzutu.
- Kontrolki karty (kolumna rozwin/zapisz w `TrasaBigCard`) maja `data-share-image-skip="true"` i nie wchodza do obrazu. Nowa kontrolka na karcie = ten sam atrybut.
- ⛔ Zdjecie spoza naszego Storage (bez CORS) zostawi pusta plame BEZ bledu - dzis takich zrodel na kartach nie ma (zero Google od 2026-09-15); `api.spontaway.com` i `spontaway.com` oddaja `access-control-allow-origin: *` (sprawdzone curl-em z originem `capacitor://localhost`).
- **INSTAGRAM STORIES NA WPROST (prosba Nat 2026-09-21, „naklejka ma linkowac do lokalu"):** kafelek „Instagram" otwiera EDYTOR RELACJI z nakladka juz nalozona - miejsce: TLO = okladka z podgladu (`storyBackgroundFromPhoto`, kadr 1080 x 1920) + NAKLEJKA = gwiazdki + pigulka `@handle` (`stickerPng(handle, "full")`); plan / kolekcja: naklejka = karta z podgladu (`renderShareImage` z `background: "transparent"`) na gradiencie marki zolty → zloty. ⛔ **Obraz w relacji NIE MOZE byc klikalny** - sprawdzone w dokumentacji Meta „Sharing to Stories": apka zewnetrzna przekazuje WYLACZNIE obraz tla, obraz naklejki i dwa kolory (`contentURL` wycofany), a jedyne klikalne elementy relacji to naklejki Instagrama (Link / Wzmianka / Lokalizacja). Dlatego LINK (`/p/`, `/l/`, `/r/`) jedzie w TYM SAMYM wpisie schowka jako tekst - user tapa naklejke „Link" i wkleja; po 4 s plugin zostawia w schowku sam tekst (zadanie w tle). Przed pierwszymi TRZEMA przelaczeniami stoi instruktaz `StoriesPrimer` (panel reczny z-[97], licznik w `localStorage` `spontaway_ig_stories_primer_v1`), potem od razu Instagram + toast „link skopiowany". Most: [InstagramStoriesPlugin.swift](ios/App/App/InstagramStoriesPlugin.swift) (`UIPasteboard` + `instagram-stories://share?source_application=<fb app id>`; `instagram-stories` w `LSApplicationQueriesSchemes`) ↔ [instagramStories.ts](src/lib/instagramStories.ts). **Facebook App ID = `1405978694380412`** (apka „spontaway" na koncie Nat w Meta for Developers, zalozona 2026-09-21; identyfikator publiczny, wpisany na stale w `instagramStories.ts`, `VITE_FACEBOOK_APP_ID` tylko nadpisuje; bez App ID nowe wersje Instagrama ignoruja wywolanie). Bez Instagrama na telefonie / na webie kafelek dziala jak dotad (JPG przez systemowy arkusz), ale link i tak laduje w schowku. ⛔ Nie linkuj do App Store: universal link `spontaway.com/p/<id>` u kogos z apka otwiera wizytowke, u reszty strone miejsca z guzikiem pobrania - App Store gubi kontekst lokalu. Sprawdzone w harnessie WebKit (podstawiony plugin): miejsce oddaje sticker 1080 x 1920 + tlo JPG + link, kolekcja i plan - przezroczysta karte + kolory tla.
- ⚠️ **LOKALNE pluginy natywne rejestruje [MainViewController.swift](ios/App/App/MainViewController.swift)** (`CAPBridgeViewController.capacitorDidLoad` → `registerPluginInstance`), wskazany w `Main.storyboard`. `packageClassList` w `capacitor.config.json` generuje `cap sync` WYLACZNIE z paczek npm, wiec klasa z `ios/App/App` bez tej rejestracji nigdy nie trafia do mostu - tak `ScreenshotPlugin` (2026-09-01) lezal martwy do 2026-09-21: nie byl nawet dodany do projektu Xcode (`project.pbxproj` kompilowal sam `AppDelegate.swift`). Nowy plik Swift = wpis w `project.pbxproj` (PBXFileReference + PBXBuildFile + grupa + Sources) i linijka w `MainViewController`; `xcodebuild -project ios/App/App.xcodeproj -scheme App -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO build` sprawdza kompilacje bez Xcode.

**NAKLADKA MIEJSCA NA STORIES (prosba Nat 2026-09-21):** w arkuszu udostepniania WIZYTOWKI (tylko miejsce) kafelek **„Nakladka"** (zolty, brandowa gwiazdka) otwiera [PlaceStickerSheet](src/components/share/PlaceStickerSheet.tsx) (panel reczny `fixed z-[97]` - `Sheet` z shadcn ma z-50 i schowalby sie pod arkuszem udostepniania na z-[95]). Nakladka = CALA relacja 9:16 (1080 x 1920) z przezroczystym tlem: trzy gwiazdki `Ikona_Gwiazdka` z kolorowymi obwodkami (rozowa `#FF5FA2`, zolta marki, zielona `#3CC46F`) rozrzucone jak wlepki + zolta pigulka `@handle` (biala obwodka, BEZ gwiazdki w srodku) w prawym dolnym rogu - uklad z referencji Nat (relacja z jedzeniem). Handle = instagram lokalu z `business_profiles.social_links`, a bez niego nazwa miejsca bez spacji (`stickerHandle`). Dwa zestawy: **Gwiazdki + nazwa** (cala relacja) i **Sama nazwa** (`variant: "pill"` - tylko zolta pigulka z handle, plotno przyciete do niej, ~1080 px szer., wylacznie PNG, bo nie ma czego animowac). Dla zestawu z gwiazdkami dwa tryby: **Statyczna** = PNG 1080 x 1920, **Animowana** = GIF 540 x 960, 30 klatek co 60 ms, gwiazdki obracaja sie (±8-11°) i pulsuja (±5-9 %), kazda z INNA faza, pigulka stoi. Wszystko rysuje JEDNA funkcja `drawSticker(ctx, handle, t)` w [placeSticker.ts](src/lib/placeSticker.ts) na canvasie - ta sama dla podgladu na zywo (rAF), PNG (t = 0) i klatek GIF-a, wiec podglad = plik. ⛔ Nie renderuj tego z DOM-u przez modern-screenshot: 30 klatek po ~300 ms to 10 s czekania. GIF: `gifenc` (5 KB), paleta `rgba4444` liczona RAZ z pierwszej klatki, piksele o alfie <= 127 recznie na indeks przezroczysty, `dispose: 2` (bez tego poprzednie pozycje gwiazdek zostawaly pod spodem). ⚠️ `document.fonts.load("400 96px Sigmar")` PRZED rysowaniem - canvas nie czeka na font i bralby zapasowy kroj. Sprawdzone w harnessie: GIF 30 klatek, `transparency: 0`, `loop: 0`, obrot widoczny miedzy klatkami.

**Udostępnianie miejsca (2026-09-11):** „Udostępnij to miejsce" w [SavePlaceSheet](src/components/plan-wizard/SavePlaceSheet.tsx) → arkusz `ShareCardPlace` (ta sama karta co w Miejscach, `SwipeCard scrollMode`) + link **`spontaway.com/p/<places.id>`** (serwer: [api/share.ts](api/share.ts) `t=place`, OG + karta + „Zobacz miejsce" + pasek przedpremierowy). Działa dla **KAŻDEGO** miejsca: wizytówka z bazy → `/p/<places.id>` z pełnymi danymi; miejsce spoza bazy (z listy, wyjazdu, Google) → **migawka** w `shared_places` (migracja 20260911e; jedna na usera i miejsce, klucz = google_place_id albo nazwa; public read, insert/update tylko autora) → `/p/<shared_places.id>`, a serwer próbuje najpierw `places`, potem migawki. `buildShareUrl("/miejsce/<id>")` → `/p/<id>`. Link do Google Maps zostaje wyłącznie jako awaria (błąd zapisu migawki). **Logika udostępniania miejsca żyje w hooku [usePlaceShare](src/hooks/usePlaceShare.tsx)** (zdjęcia, migawka, link, arkusz) - używają go SavePlaceSheet i **wizytówka** (`PlaceSwiperDetail`: żółte kółko z brązową ikoną obok „Zapisz to miejsce", 2026-09-13). **Zdjęcia:** karta w arkuszu renderuje się ze `skipGoogleFetch`, więc NIC sama nie dociąga - hook zbiera zdjęcia ze WSZYSTKICH źródeł wizytówki (wiersz, `fetchEnrichedPlace`, `fetchPlaceUserPhotos` = piny + `place_photos`), pierwsze = okładka, a **tapnięcie w kartę przełącza na kolejne zdjęcie** (w kółko, z delikatną haptyką `haptics.light()`; BEZ pigułki „Zmień zdjęcie" i bez osobnego arkusza wyboru - oba odrzucone 2026-09-13). **Karta w arkuszu = `SwipeCard shareMode`**: samo zdjęcie + plakietka kategorii + nazwa + adres; bez chipa dystansu, cen, tagów i kolumny zapisz/rozwiń (prośba Nat 2026-09-13 - odbiorca linku i tak ich nie dostaje). Wybór jedzie do `shared_places.photo_url` (wizytówka z bazy dostaje wtedy migawkę z `place_id` i link `/p/<migawka>`), a serwer bierze zdjęcie z migawki PRZED własnymi źródłami; ostatni fallback serwera = zdjęcia userów z opublikowanych wyjazdów (`pins`). **Udostępnianie WYJAZDU pokazuje miejsca po dniach (`pins.day_index`) w JEDNYM przewijanym rzędzie z sekcjami dni: nagłówek dnia jest PRZYKLEJONY u góry (`position: sticky; left`) w obrębie swojej sekcji - „Dzień 1" stoi, dopóki przewijają się jego miejsca, przy ostatnim zostaje wypchnięty i od pierwszego miejsca dnia 2 stoi „Dzień 2"; na końcu kafelek „Jeszcze N miejsc zobaczysz w aplikacji", dalsze dni tylko w aplikacji** (decyzja Nat 2026-09-14; osobne rzędy na dzień i pionowy divider w rzędzie odrzucone) - tak samo w arkuszu w apce (`ShareSheet stripDays`), na zapowiedzi linku w apce i na stronie `/r/<id>` w `api/share.ts` (`.sec .day{position:sticky}`). ⛔ **Arkusz udostępniania renderuje się WEWNĄTRZ gospodarza** (drawer vaul wizytówki / `Sheet` zapisu), więc jego korzeń ma `data-vaul-no-drag data-no-drag` (przeciągnięcie po żółtym arkuszu zamykało gospodarza), a hook dostaje `{ hostOpen }` i **zeruje stan, gdy gospodarz się zamyka** - bez tego stary arkusz wracał na wierzch przy następnej wizytówce („nie mogę wejść w wizytówkę innego miejsca", zgłoszenie Nat 2026-09-13).
- Stare wejścia: `/plan` z `exploreMode` i `/eksploruj` ze `state.view === "browse"` przekierowują na `/miejsca`; event `trasa:explore-nearby` nawiguje na `/miejsca` ze `state.nearby`.
- **Onboarding (2026-09-13) = DOKŁADNIE 5 kroków** ([OnboardingFlow](src/components/onboarding/OnboardingFlow.tsx), stepper zostaje): welcome (regulamin) → „Skąd znasz" → „W jakim celu" (multi, z opcją „Jeszcze nie wiem") → **PROFIL na jednym ekranie** (zdjęcie/awatar bazowy + imię + nazwa użytkownika + **płeć** - pigułki, wymagana, z „Wolę nie podawać"; `onboarding_responses.gender`, migracja `20260913g`, NIE `profiles`) → zgoda na analitykę (copy bez „nie sprzedajemy danych"). Kroki „miasto zamieszkania", „powiadomienia" i „lokalizacja" USUNIĘTE - o zgody systemowe pytamy w chwili użycia (patrz „Zgody systemowe w kontekście" niżej). Po 5. kroku user ląduje w Eksploracji i **coach-marki ([OnboardingGuide](src/components/OnboardingGuide.tsx)) prowadzą go po KAŻDEJ zakładce**: Eksploracja → Miejsca → Profil → „+" (cele `data-ob="nav-eksploruj|nav-miejsca|nav-profil|nav-fab"`, każdy krok nawiguje na swój ekran przez `route`); dawny krok „zapis" bez celu zniknął.
- **Zgody systemowe W KONTEKŚCIE (2026-09-14, decyzja Nat):** o push i lokalizację pytamy tam, gdzie user bez zgody „jest jak bez ręki", NIGDY przy starcie apki. Brama = [permissionPrompts.ts](src/lib/permissionPrompts.ts) (`askPermission(kind, ctx, { explicit })`, `askPermissionSoon`), arkusz = [PermissionPrimerSheet](src/components/permissions/PermissionPrimerSheet.tsx) (zamontowany raz w `App.tsx`; styl jak potwierdzenia: pływający panel, peachy kółko z ikoną, akcja NAD „Nie teraz"). **Dwa tryby:** `explicit` (user sam tapnął coś, co wymaga zgody) = systemowy alert OD RAZU, bez pośrednich pytań; gdy zgoda była już odrzucona - arkusz „X jest wyłączona → Otwórz Ustawienia" (`window.location.assign("app-settings:")`, Capacitor oddaje to `UIApplication.open`; toast „włącz w ustawieniach" nikogo tam nie prowadził). Kontekstowy (bez `explicit`) = najpierw NASZ arkusz z wyjaśnieniem, systemowy alert dopiero po „Włącz"; „Nie teraz" nic nie psuje (iOS pokazuje alert tylko RAZ na instalację, więc chronimy go). Limity kontekstowych: raz na kontekst, nie częściej niż co 3 dni, max 3 razy, nigdy w trakcie coach-marków, nigdy nad innym arkuszem zgody (localStorage `spontaway_perm_asks_v1`). **Push, konteksty:** `list_created` (kreator, po wejściu w nową kolekcję), `trip_created` / `invite_sent` (kreator wyjazdu; przy zaproszonych osobach copy o odpowiedziach), `trip_published` (po zejściu toastu „Cofnij"), `invite_sent` z arkusza zaproszeń, `bell` (żółta karta na górze szuflady powiadomień - jawne, znika po zgodzie, przy odmowie prowadzi do Ustawień), `settings` (wiersz „Powiadomienia push" w Ustawieniach: Włącz / Włączone / Wyłączone). ⛔ `useNativePush` przy starcie TYLKO rejestruje token, gdy zgoda już jest - do 2026-09-14 po ukończonym onboardingu odpalał systemowy alert zaraz po zalogowaniu. **Lokalizacja, konteksty:** `browse` = miękkie pytanie „Jesteś w mieście?" przy TRZECIEJ wizytówce w Miejscach (kto przewija miejsca, pewnie jest na miejscu; po „Włącz" chip „od Ciebie" pojawia się sam), a jawne: chip „Pokaż dystans" na karcie i **w nagłówku wizytówki** (`PlaceSwiperDetail`, nowe), „Bieżące położenie" w wyszukiwarce, „Moja lokalizacja" na mapie dodawania miejsca. Copy w `common:permissions.*` (PL/EN). Nowy kontekst = wpis w typach `PushContext`/`LocationContext` + klucze `permissions.<kind>.<ctx>.{title,desc}`. Dev: `window.__askPermission(kind, ctx)` z konsoli.
- Web/PWA (stary flow, `!PLANNING_DISABLED`): bez Eksploracji/Miejsc; w pillu `Wyjazdy` (`/home`) · `Profil` + `+`, start = `/eksploruj` (waitlista).

**Historia:** 2026-08-20 → 2026-09-11 pasek miał 3 pozycje (`Eksploruj` · `+` · `Profil`), a wizytówki miejsc siedziały pod przełącznikiem Trasy|Miejsca w eksploracji. 2026-09-11 → 2026-09-13: 5 pozycji z podpisami (`Główna` (siatka) · `Eksploracja` (feed obserwowanych) · `+` · `Miejsca` · `Profil`).


**POWRÓT NA TO SAMO MIEJSCE w Eksploracji i w Miejscach (prośba Nat 2026-09-15).** Wejście w wyjazd, kolekcję czy wizytówkę zmienia trasę, więc React odmontowuje ekran razem ze scrollerem i po powrocie user lądował na początku widoku. Wspólny hook [useScrollRestore](src/hooks/useScrollRestore.ts) (pamięć MODUŁOWA, nie `sessionStorage` - ma żyć tyle, co uruchomienie apki; po restarcie user oczekuje świeżego widoku). **Dwa różne mechanizmy, bo to dwa różne problemy:**
- **Eksploracja** = pozycja w PIKSELACH (`useScrollRestore("/eksploruj", ...)`). Kolejność kafelków jest stabilna, więc offset wystarcza. Scroller siedzi w `PullToRefresh`, który wystawia go propem `scrollRef`. Hook **śpi przy otwartej wyszukiwarce** (`enabled: !searchOpen`) - ten sam kontener pokazuje wtedy wyniki, a pozycja feedu nie ma się do nich nijak; przy zamknięciu szukania feed sam wraca na swoje.
- **Miejsca** = IDENTYFIKATOR KARTY (`rememberItem` / `recallItem`, klucz `/miejsca`), NIE piksele. ⛔ Nie zamieniaj tego na offset: `interleaveByCategory` układa kolejkę od nowa przy każdym montowaniu (przeplot kategorii z losowym jitterem), więc ten sam offset trafiłby w INNĄ wizytówkę. Szukamy zapamiętanej karty w nowej kolejności; gdy wypadła poza okno leniwego doładowania, najpierw rośnie `exploreVisible`, scroll idzie w kolejnym przebiegu.

Pułapki, na które ten kod już odpowiada (nie usuwaj zabezpieczeń):
- **Treść dochodzi asynchronicznie**, więc `scrollTop = X` na pustym jeszcze feedzie zostaje przycięte do zera. Hook próbuje przez `RESTORE_MS`, a `onNeedMore` woła TYLKO gdy kontener przestał rosnąć (inaczej leniwa lista załadowałaby się cała w kilka klatek).
- **Przy odmontowaniu element bywa już odpięty** i oddaje `scrollTop === 0` - zapisujemy więc wyłącznie wartość dodatnią, żeby nie wyzerować dobrej pozycji.
- **`rememberItem(key, null)` KASUJE wpis**, a przy montowaniu `activeCardId` to jeszcze null - zapis jest więc pod warunkiem, że karta jest znana.
- **Przywrócenie to PROGRAMOWY scroll**: gasi je pierwszy ruch palca usera, a `programmaticScroll` blokuje przy nim pytanie o lokalizację (próg „trzecia karta" - powrót do widoku to nie przeglądanie).

**ARKUSZ LUDZI = JEDEN ARKUSZ, TRZY ZAKLADKI (kierunek A z eksploracji, wybor Nat 2026-09-17).** [PeopleSheet.tsx](src/components/profile/PeopleSheet.tsx) - ten sam komponent na WLASNYM i CUDZYM profilu (`TravelerProfile` i `PublicProfile`), wiec wiersz osoby jest JEDEN, a nie dwa prawie takie same. Makiety: Figma `[NEW] Ekrany` -> „Obserwujacy i obserwowani - eksploracja kierunkow” (rzad P = profil publiczny).
- **Zakladki:** `Obserwujacy | Znajomi | Obserwowani`, trzy ROWNE pigulki BEZ liczb - polskie dopelniacze („124 Obserwujacych”) lamia sie w nich na dwie linie. Liczba stoi POD zakladkami jako etykieta listy i zmienia sie razem z nia.
- **Wysokosc 82 dvh, nie 72:** przy otwartej klawiaturze na 72 zostawaly trzy wiersze.
- **Wiersz:** awatar 44 px **z nakladka** (`UserAvatar` - do 17.09 to byla jedyna lista ludzi w apce bez `FramedAvatar`), imie, nick, plakietka „Znajomi” przy relacji wzajemnej i akcja po prawej. ⛔ Wiersz i guzik to DWA rozne cele dotyku: wiersz otwiera profil, guzik zmienia relacje.
- **Relacje licza sie zawsze wzgledem MNIE**, nie wlasciciela listy - takze gdy ogladam liste kogos innego. Przy sobie samej zamiast guzika stoi „to Ty”.
- **Szukanie** filtruje po imieniu i nicku po stronie klienta (listy sa juz w pamieci). ⚠️ Przy tysiacach obserwujacych trzeba bedzie dolozyc `range()` i szukanie w bazie - dzis `useFollowList` robi jedno zapytanie bez paginacji.
- **Wypisanie ze znajomych tylko na WLASNEJ liscie** (zakladka Znajomi, ikona `UserMinus`): dziala od razu, z „Cofnij” w toascie. Na cudzej liscie tej akcji nie ma - nie ma czego wypisywac.
- **Profil publiczny ma teraz TEN SAM rzad licznikow co wlasny:** Obserwujacy / **Znajomi** / Wyroznione, a obserwowani maja zakladke w arkuszu. ⛔ `friend_ids` dla CUDZEGO uzytkownika nie odejmuje wykluczen - z roznicy dalo by sie odczytac, kogo ta osoba wypisala.

**USTAWIENIA = HUB Z PODSTRONAMI (kierunek B z eksploracji, wybor Nat 2026-09-17).** ⚠️ **JEDNA skora wierszy (2026-09-18): szary fill `bg-muted/60` + promien 20 px** - starsze sekcje (zgoda na analityke, push, „Zglos blad", spolecznosc) mialy biale tlo z szarym obrysem sprzed hubu i Nat zobaczyla dwa style na jednym ekranie; nowy wiersz = `SettingsRow`/`SettingsGroup` albo ta sama klasa. Kontakt = `support@spontaway.com` (alias w Gmailu jeszcze do zalozenia). Klucze `permissions.location.status_*` doszly 2026-09-18 - wczesniej wiersz „Lokalizacja" pokazywal surowy klucz. [Settings.tsx](src/pages/Settings.tsx) + prymitywy w [SettingsUI.tsx](src/components/settings/SettingsUI.tsx); makiety: Figma `[NEW] Ekrany` -> „Ustawienia konta - eksploracja kierunkow”. Do 17.09 byl to JEDEN przewijany strumien pietnastu pozycji z dwoma przypadkowymi naglowkami `h3` („Kosz” sasiadowal ze zgoda na analityke, „Zmien haslo” lezalo pod „Zglos blad”).
- **Ekran glowny to SPIS TRESCI:** zolta karta profilu (-> `/settings/profil`) + szesc wierszy: `Konto i bezpieczenstwo` - `Powiadomienia` - `Prywatnosc i dane` - `Kosz` - `Pomoc i kontakt` - `O aplikacji`, a pod nimi **osobne karty** `Wyloguj sie` i `Usun konto` (wzor PayPal: akcja nieodwracalna nie mieszka w tej samej szufladzie co przelacznik analityki). Admin dostaje JEDEN dodatkowy wiersz `Narzedzia zespolu`, nie trzy luzne pozycje - dol ekranu ma wygladac tak samo na kazdym koncie.
- **Routing:** `/settings` = hub, `/settings/:section` = podstrona (`profil`, `konto`, `powiadomienia`, `prywatnosc`, `zablokowani`, `pomoc`, `o-aplikacji`, `zespol`, `usun-konto`). Kazda ma wlasny adres, wiec gest wstecz i historia dzialaja bez dodatkowej logiki. ⚠️ Bramka „wlasciciel lokalu idzie do panelu” w `App.tsx` pomija `startsWith("/settings")`, nie samo `=== "/settings"`.
- ⛔ **Kosz ZOSTAJE na najwyzszym poziomie** (jedno tapniecie z huba), choc jest rzadki: to sciezka ratunkowa „gdzie to zniknelo”. Schowanie go o poziom glebiej bylo w eksploracji swiadomie odrzucone.
- **Koniec wiersza mowi, co sie stanie:** chevron = prowadzi dalej, stan/przelacznik = zmienia sie tu i teraz. To odpowiedz na dwa modele zapisu, ktore do 17.09 wygladaly identycznie. **Formularz profilu jest JEDYNYM miejscem z guzikiem `Zapisz zmiany`** i mowi to wprost pod guzikiem.
- **`Prywatnosc i dane` to POWIERZCHNIA, ktorej nie bylo:** zgoda na analityke, **`Zablokowani`** ([BlockedPeople.tsx](src/components/settings/BlockedPeople.tsx) - do 17.09 listy zablokowanych nie bylo NIGDZIE, odblokowac dalo sie tylko wchodzac na profil osoby, ktora sie przed chwila schowalo), zgody systemowe ze STANEM i przejsciem do Ustawien iOS oraz dokumenty. ⛔ To NIE jest miejsce pierwszego pytania o push i lokalizacje - o zgody pytamy w chwili uzycia (`permissionPrompts.ts`).
- **Usuwanie konta ma dwa kroki** ([DeleteAccountFlow.tsx](src/components/settings/DeleteAccountFlow.tsx), wzor Comet): ekran konsekwencji (co znika NA ZAWSZE i co zostaje u innych) -> **przepisanie wlasnej nazwy uzytkownika**, przy czym czerwony guzik jest nieaktywny, dopoki sie nie zgadza -> wylogowanie i toast. Samo kasowanie idzie tym samym RPC `delete_current_user_account` co wczesniej; zmienila sie DROGA do niego. Tytul ekranu zszedl z „Ustawienia profilu” na „Ustawienia” - polowa tresci profilu nie dotyczy.

**NAZEWNICTWO „PLANY" (decyzja Nat 2026-09-20): dawne „Wyjazdy" nazywają się w CAŁYM UI „Plany"** (PL: plan / planu / plany / planów / planie, EN: plan / plans). Tak jak przy „Kolekcjach": **kod, routy, tabele i klucze zostają** (`/route/:id`, `routes`, `?tab=wyjazdy`, klucze `trip_*`, `createWyjazd`, `SharedRoute`) - to tylko słowo w interfejsie. Zmienione: locale PL+EN (mechanicznie z zachowaniem odmiany, potem ręczny przegląd), strona linku `/r/<id>` w `api/share.ts`, copy pushy w `notify_push` (migracja `20260920f`). ⚠️ Tam, gdzie chodzi o FIZYCZNĄ podróż, a nie o byt w apce, zostaje „podróż"/„wyjazd" jako zwykłe słowo: „Zdjęcia z podróży", „w trakcie podróży", „Planuj podróże ze znajomymi", „planowania wyjazdów ze znajomymi" na stronie linku. Nazwa domyślna: **„Plan podróży do Polski"** (`naming.trip_to` = `Plan podróży {{place}}`, tabele odmiany w [placeNaming.ts](src/lib/placeNaming.ts) bez zmian), kilka krajów / miasto ręczne: „Plan: Polska · Czechy"; EN „Plan for Poland" / „Plan: Poland". Raport dzienny dla founderów (`daily-analytics-digest`) i panel ops zostały przy „wyjazdach" - to nie jest UI usera.

**KOSZTY API W PANELU OPS = PRAWDZIWY RACHUNEK GOOGLE W PLN (2026-09-20).** Do tego dnia strona „Koszty API" mnożyła nasz licznik Text Search przez cennik katalogowy ($32/1000) od pierwszego wywołania i pokazywała „$52", gdy faktura Google mówiła 23,85 zł - Google ma darmową pulę per SKU, a licznik nie widzi geokodowania, Maps JS ani zdjęć. ⛔ Nie wracaj do przeliczania wywołań na dolary. Źródło prawdy: **eksport rozliczeń konta „Moje konto rozliczeniowe" do BigQuery** (projekt `gentle-scene-474817-r1` - to „My First Project" Google, w nim siedzą klucze Maps; projekty „Trasa" nie mają rozliczeń; zbiór `billing`, `europe-central2`). Funkcja brzegowa `google-billing-sync` (konto usługi `spontaway-billing-reader`, sekrety `GOOGLE_BILLING_SA_JSON` + `GOOGLE_BILLING_PROJECT`, JWT RS256 → BigQuery `jobs.query`) upsertuje 40 dni wstecz do `google_billing_daily` (dzień w czasie polskim, `cost` katalogowy + `credits` dodatnie; do zapłaty = różnica). Cron `google-billing-sync` 4:20 UTC przez `cron_google_billing_sync()` z sekretem z Vault, ⚠️ `timeout_milliseconds := 90000` - domyślne 5 s pg_net ucinało połączenie w trakcie zapytań BigQuery. Panel ([CostsPage](src/admin/modules/costs/CostsPage.tsx)): kwota do zapłaty + prognoza z tempa + rozbicie na usługi + „Odśwież teraz" (funkcja przepuszcza admina po `has_role`); liczniki wywołań zostały WYŁĄCZNIE jako stan limitów proxy. Eksport nie działa wstecz - dane od dnia włączenia (20.09.2026). Dry-run: `net.http_post` z Vault + body `{"dry_run":true}` (klucz service_role z `.env` nie zgadza się z env funkcji → 401).

**JEDEN MECHANIZM: PLAN (decyzja Nat 2026-09-20, „usunąć przeszłe wyjazdy").** Kreator pod „+" nie pyta już „Przeszły / Przyszły" - krok `tripMode` USUNIĘTY z [CreateFlowSheet](src/components/create/CreateFlowSheet.tsx) (klucze `trip_kind.*` skasowane), każdy nowy plan startuje w etapie `planning`, a kalendarz ma `allowPast` - plan może dokumentować także to, co już było. Stare wiersze z `trip_type='completed'` bez publikacji (np. „Japan Honeymoon") zostają i działają jak dotąd. Podzakładki profilu (`Robocze | Opublikowane | Zapisane`) bez zmian. Konsekwencje w widoku planu tego samego dnia:
- **Gwiazdka „topki" NIE JEST już bramkowana publikacją** (`onToggleTop={canEdit ? …}`; do 20.09 `canEdit && isPublished` z 10.09 „wyróżnienie dla czytających"). Uczestnik i właściciel wyróżniają miejsca na każdym etapie - RLS `Group members can update pins of shared route` to przepuszcza.
- **„Dodaj notkę" WRACA do menu miejsca w planie** (etykieta zależy od tego, czy notka już jest). 18.09 zdjęłam ją przez nadinterpretację, a edytor pod wierszem ma `hideActions`, więc bez tej pozycji nikt nie mógł założyć notki przy miejscu bez notki (zgłoszenie Nat: „jako uczestnik nie mogę dodawać notek").
- **Uczestnik pisze WSPÓLNY OPIS planu, nie własną notkę** (Nat: „Add note usunąć i dodać opcje dodaj opis"). Guzik „Dodaj opis" pod tytułem i pozycja w menu „…" stoją za `canEdit`, zapis idzie przez SECDEF `set_trip_description(p_route, p_description)`; zmiana nazwy przez `set_trip_title` (migracja `20260920e`, właściciel LUB członek `group_session_members`, `REVOKE` dla anon). ⛔ Nie dawaj członkom polityki UPDATE na `routes` - dostaliby też status, daty, okładkę i kosz. ⚠️ Do 20.09 zmiana nazwy przez uczestnika kończyła się 0 wierszy BEZ błędu (toast „zapisano", po odświeżeniu stara nazwa) - dlatego nazwa też przeszła na RPC. Dawne notki uczestników o planie (`route_member_covers.note`) zostają widoczne w dymkach pod opisem, ale nie da się już pisać nowych; `setMyRouteNote` nie jest nigdzie wołane.

**KONTA LOKALI NIE SĄ LUDŹMI W APCE (zgłoszenie Nat 2026-09-21, migracja `20260921d`):** `profiles.is_business` = konto WYŁĄCZNIE lokalu. Ustawia trigger `trg_mark_business_owner_profile` przy przypięciu `owner_user_id` do wizytówki, ale TYLKO gdy profil nie ma śladu B2C (`profile_has_b2c_footprint`: plany, obserwacje, kolekcje poza „Ogólne", członkostwa) - Nat jest właścicielką testowego lokalu i zostaje zwykłym userem. Klient filtruje `.eq("is_business", false)` w wyszukiwarce ludzi (także szukanie planów po autorze), `PublicProfile` (lokal = „nie znaleziono") i `InviteFriendsSheet`. ⛔ Nie wracaj do filtra po `business_profiles_public.owner_user_id`: widok pokazuje tylko AKTYWNE wizytówki, więc lokal czekający na weryfikację („Oter Coffeebar") przechodził, a założycielka znikała. Kolumna jest w kolumnowym grancie SELECT; nikt poza triggerem jej nie pisze.

**WIZYTÓWKA BIZNESU PRZEJMUJE STAN ZERO (prośba Nat 2026-09-21):** `admin-moderate-business` przy zatwierdzeniu szuka ISTNIEJĄCEGO `places` po nazwie+mieście (bez wielkości liter), a gdy wizytówka nie ma miasta albo nic nie pasuje - po samej nazwie, ale tylko przy DOKŁADNIE jednym kandydacie (sieciówka w dwóch miastach = dwa lokale); odpadają wiersze podpięte już pod inną wizytówkę. Trafienie = `place_id` na TEN wiersz (uzupełniane tylko braki adresu/współrzędnych, okładka lokalu nadpisuje, `is_active=true`), więc `enrichWithBusinessProfile` nakłada profil wszędzie, gdzie ten `places.id` już wisi (piny po UUID, `resolvePlaceDbId`, zakładka Miejsca). Brak trafienia = nowy wiersz jak dotąd. Do 21.09 funkcja ZAWSZE wstawiała nowy wiersz i w bazie zostawał duplikat. Wynik w audycie: `metadata.linked_existing`. Rejestracja B2B nie zbiera `google_place_id`, więc dopasowanie idzie po nazwie.

**ODWIEDZINY DOPASOWANE PO OBU KLUCZACH (zgłoszenie Nat 2026-09-21, migracja `20260921c`):** to samo miejsce ma w różnych kolekcjach RÓŻNE klucze (`gpid:` z wyszukiwarki Google, `nm:nazwa` z planu/ręcznie), a `place_visits` zapisuje jeden z nich - miejsce odhaczone w jednej kolekcji wyglądało w nowej na nieodwiedzone. `item_visit_keys(google_place_id, place_name)` oddaje oba kandydaty; `list_author_visits`, `list_collection_visits` i `list_author_visit_counts` dopasowują `= any(...)`; w `SharedList` `canonVisitKeys` mapuje trafienie na klucz PIERWOTNY pozycji, więc reszta ekranu dalej pyta po `visitKeyOf(it)`.

**EKSPLORACJA: WŁASNE treści w pierwszym koszyku + natychmiastowe odświeżenie (zgłoszenie Nat 2026-09-21):** świeżo utworzona kolekcja lądowała „na samym dole", bo user nie obserwuje sam siebie i wpadał do „reszty świata"; teraz `followed` w `DiscoveryFeed` zawiera `user.id`. Tworzenie kolekcji/planu (`CreateFlowSheet`, `CreateRanking`) woła `invalidateContentLists()` (zamiast dwóch kluczy profilu), a `addPlaceToList` unieważnia `explore-rankings` (5-minutowy `staleTime`, sortowanie po `updated_at`).

**WYSZUKIWARKA = TE SAME KAFELKI CO RESZTA APKI (prośba Nat 2026-09-21):** kolekcje w wynikach renderuje `GridTile` przez wspólny mapper `collectionToGridItem` (ten sam, co feed Eksploracji; zapytanie dociąga `theme`, `countries` i `fetchListVisitCounts`) - `ProfileFeedCard` stamtąd zniknął. Miejsca = siatka 3 kolumn `PlaceTile showCity` (jak „Ogólne" na profilu), zamiast wierszy z miniaturką i chevronem. ⛔ Okładka miejsca w wynikach = zdjęcie USERA (`place_photos`) albo własna okładka lokalu z `business_profiles_public`, NIGDY `places.photo_url` - ta sama reguła co w zakładce Miejsca; do 21.09 `attachCovers` brał `photo_url` i kafelki pokazywały pusty kadr z gradientem.

**Nazewnictwo „Kolekcje" (decyzja Nat 2026-09-13):** w CAŁEJ warstwie UI dawne „Listy" nazywają się **„Kolekcje"** (PL: kolekcja / kolekcji / kolekcję / kolekcje, „w kolekcji", nie „na liście"; EN: collection / collections). Dotyczy locale (`*.json`), copy pushy w `notify_push` (migracja `20260913e`) i strony linku (`api/share.ts`). **Kod, routy i tabele ZOSTAJĄ po staremu** (`/lista/:id`, `discovery_collections`, `SharedList`, klucze `list_*`, `tab=listy`) - to tylko słowo w interfejsie. Generyczna „lista" (widok listy, lista oczekujących, cennik, „nie ma na liście") NIE jest kolekcją i zostaje. Landing spontaway („Twórz własne kolekcje tematyczne") też zmieniony. Ta dokumentacja używa obu słów zamiennie tam, gdzie mówi o kodzie.

**Imię i nazwa użytkownika (2026-09-14):** nazwa = max 20 znaków (`USERNAME_MAX`, reguły w [usernameRules.ts](src/lib/usernameRules.ts) + wyzwalacz `reject_banned_username`), **imię = max 30 znaków, same litery (myślnik/apostrof ok), bez wulgaryzmów** (`checkFirstName` + wyzwalacz `reject_banned_first_name`, migracja `20260914b`; do tego dnia imię nie było sprawdzane wcale - „Cipa" przechodziło). Lista słów = [profanity.ts](src/lib/profanity.ts) po stronie klienta i tabela `banned_usernames` w bazie - dopisując słowo, dopisz w OBU. Liczniki znaków przy obu polach w onboardingu i Ustawieniach.

**Profil (`/moj-profil`, [TravelerProfile.tsx](src/pages/TravelerProfile.tsx)) = hub z 2 zakładkami** (`?tab=listy|wyjazdy`). **Kolejność pigułek: Wyjazdy | Kolekcje** (w kodzie nadal `listy`), domyślnie otwiera się **Wyjazdy** (zmiana 2026-08-30) - tak samo na profilu publicznym. **Wejście bez parametrów (pasek, powrót) ZAWSZE ląduje na Wyjazdy → Opublikowane** (prośba Nat 2026-09-14; efekt od `searchParams` resetuje podzakładki, gdy `?sub=` nie ma - deep-linki `?tab=&sub=` działają jak dotąd). **Zaznaczona pigułka podzakładki = żółta `#FDF184` z brązowym tekstem `#5B2C06`** (`TabSelect`; pomarańcz odrzucony 2026-09-14). **Chipy podzakladek sa STICKY RAZEM z zakladkami** (2026-09-15): jedno pudelko `sticky top-0`, w nim pasek Wyjazdy|Kolekcje, a pod nim wiersz chipow (+ guzik ukladu przy Wspomnieniach). Wysokosc tego pudelka jest MIERZONA (`ResizeObserver` -> zmienna `--profile-sticky`) i zasila `scroll-pt` scrollera, zeby snap kolekcji zatrzymywal kafelek POD naglowkiem - stala wpisana na sztywno rozjechala sie przy pierwszej zmianie paska. **Zakładka „Zapisane" USUNIĘTA 2026-08-24** - zapisane miejsca żyją w LIŚCIE OGÓLNEJ (wishlista `to_visit`, patrz niżej), niewidocznej jako tab; pojawiają się przy tworzeniu listy/wyjazdu. Wewnątrz Listy i Wyjazdy są **podzakładki** (dropdown w stylu iOS, komponent lokalny `TabSelect`):
1. **Listy** — pigułki `[Moje kolekcje | Wspólne | Ogólne | Zapisane]` (domyślnie **Moje kolekcje**). **Podzakładka „Wspólne" doszła 2026-09-16** (prośba Nat: „kolekcja może być przypadkowo nie zapisana ALE wspólna") i zbiera kolekcje, w których user WSPÓŁTWORZY, ale nie jest autorem (`_shared` z zapytania `profile-list-feed`). ⚠️ To ZMIANA wobec 2026-09-15, kiedy współtworzone lądowały w „Moje kolekcje" - były tam, bo nie miały własnego miejsca, nie dlatego, że tam pasowały. Teraz „Moje kolekcje" = wyłącznie moje autorsko, „Wspólne" = cudze, w których dodaję miejsca. Oba renderuje JEDNA funkcja `renderCollectionCards` - skopiowany blok rozjechałby się przy pierwszej zmianie kafelka.:
   - **Moje listy** — kuratorskie **publiczne polecajki** usera (`discovery_collections`, `kind='ranking'`, `list_status='visited'`). Grupy miejsc do polecenia, NIE luźne zapisy.
   - **Zapisane** — listy zapisane **od innych** userów. Komponent [SavedCollections](src/components/home/DiscoveryFeed.tsx) (localStorage `trasa_saved_collections`). Karta zapisanej listy: licznik zapisów TYLKO w nagłówku przy dacie, BEZ zakładki w stopce (2026-09-13) - odpięcie listy żyje w jej widoku.
2. **Wyjazdy** (dawny Dziennik) — pigułki `[Robocze | Wspomnienia | Zapisane]` (domyślnie **Wspomnienia**):
   - **Robocze** — trasy usera `status != 'published'` (niepublikowane, badge „Robocze"). **Wspomnienia** — `status = 'published'`. Publikacja = „Zapisz trasę" (patrz model roboczy→przeszły). Karta renderowana wspólnym helperem `renderTripCard` (przekazuje `status/is_shared/trip_type` z zapytania `profile-trip-feed`).
   - **Zapisane** — trasy zapisane **od innych** userów (`saved_routes`). Komponent [SavedRoutes](src/components/home/DiscoveryFeed.tsx) z `city="all"`.
   - **Układy Wspomnień** ([TripLayout.tsx](src/components/profile/TripLayout.tsx)): lista | mozaika | siatka, wybór oglądającego w localStorage. **JEDEN guzik przelaczajacy w kolko** (2026-09-15; rzad trzech kwadratow najezdzal na chipy podzakladek) - ikona pokazuje uklad AKTUALNY, nastepny siedzi w `aria-label`. **Kafelki maja UJEDNOLICONE proporcje**: mozaika 3:4, siatka kwadrat, zdjecie kadrowane `object-cover`; zazebienie robi `MOSAIC_OFFSET` (`mt-7` na DRUGIEJ kolumnie), nie rozne wysokosci. ⛔ Nie wracaj do `natural` / `h-auto` - obok siebie stawaly kadry 9:16, 3:4 i panoramy, przez co uklad wygladal na przypadkowy. ⛔ Mozaika = DWIE kolumny flex z rozdziałem naprzemiennym (`mosaicColumns`), NIE CSS `columns-2` - WebKit gubi malowanie i trafianie palcem w drugiej kolumnie przy warstwach kompozytowanych (ten sam błąd co w ExploreGrid). Ten sam podział na własnym i publicznym profilu.
   - **„Przytrzymaj i przestaw" (2026-09-11):** na siatce i mozaice własnego profilu przytrzymanie kafelka (420 ms) podnosi go (duch w portalu, haptyka), przeciągnięcie przestawia okładki, puszczenie zapisuje kolejność w `profile_trip_order` (jedna tablica `route_ids` na usera, migracja `20260911i`; [tripOrder.ts](src/lib/tripOrder.ts): `applyTripOrder` = nieznane/nowe wyjazdy NA POCZĄTKU, usunięte pomijane). Kolejność jest WSPÓLNA dla wszystkich (profil publiczny czyta tę samą tablicę, bez edycji); lista pełnych kart tylko ją pokazuje. Gest = wspólny hook [useLongPressReorder](src/hooks/useLongPressReorder.tsx) (patrz „Gesty natywne"). Podpowiedź pod pigułkami (`tab_hints.trips_wspomnienia_reorder`) zamiast dymka.
**Kreator KOLEKCJI = kraj → miasto → nazwa → miejsca (2026-09-15).** Do tego dnia szedl kraj → miejsca, a nazwa powstawala automatycznie i nie dalo sie jej tknac przed utworzeniem; miasto dalo sie ustawic dopiero PO utworzeniu, w menu „…" → „Kraj i miasto" ([ListScopeSheet](src/components/lists/ListScopeSheet.tsx)). Teraz oba kroki sa w kreatorze: **miasto** (chipy miast pierwszego wybranego kraju + pole na wpisanie recznie, oba krokiem opcjonalnym - ten sam uklad co w `ListScopeSheet`, zeby tworzenie i edycja pytaly tak samo) i **nazwa** (pole startuje z propozycja, `titleTouched` przelacza je na tekst usera - bez tego cofniecie sie po miasto kasowaloby wpisana nazwe). Copy kroku kraju to teraz **„Wybierz kraj"** (bylo „Skąd są te miejsca?"). Propozycja nazwy bierze MIASTO przed krajem („Kolekcja miejsc w Krakowie" zamiast „... w Polsce").

**Domyślne nazwy nowej kolekcji i wyjazdu (2026-09-14):** arkusz tworzenia WYJAZDU nie ma pola na nazwę, więc tytuł powstaje z wybranego kraju (picker wybiera WYŁĄCZNIE kraje - miast tam nie ma): **„Kolekcja miejsc w Polsce"** / **„Wyjazd do Polski"** ([placeNaming.ts](src/lib/placeNaming.ts)). ⚠️ Polska odmiana jest nieregularna, więc moduł trzyma **jawne tabele całych fraz z przyimkiem** dla wszystkich 90 krajów z `TRIP_COUNTRIES` + polskich miast z pickera: sam rzeczownik nie wystarcza, bo obok „w Polsce" jest „we Włoszech" (zbitka spółgłosek) i „na Węgrzech" (Węgry, Słowacja, Litwa, Łotwa, Ukraina + wyspy: Cypr, Malta, Islandia, Kuba, Filipiny, Malediwy, Seszele, Mauritius, Dominikana, Fidżi). **Dopisujesz kraj do `TRIP_COUNTRIES` - dopisz go w OBU tabelach**, inaczej dostanie formę zapasową. **Nazwy krajów po ANGIELSKU (2026-09-18):** `name` w `TRIP_COUNTRIES` to KLUCZ DANYCH (tak siedzi w `routes.countries` / `discovery_collections.countries`, po nim idą tabele odmiany i `countryForCity`) - nie tłumacz go w danych. Na ekran idzie `countryLabel(name)` z [tripCountries.ts](src/lib/tripCountries.ts) (mapa `COUNTRY_NAME_EN`, 90 krajów): `CountryPicker` (wiersze, chipy, szukanie po PL i EN), selecty w `CityDrum` / `CountryCityPicker` / `CreateRanking`, `scopeLabel` (chipy na kafelkach i w nagłówkach) i gałąź EN w `placeNaming`. Zgłoszenie Nat: w angielskiej apce kreator pokazywał „Niemcy", „Włochy". Nowy kraj = wpis także w `COUNTRY_NAME_EN`. **Listy krajów są ALFABETYCZNE po nazwie wyświetlanej (prośba Nat 2026-09-21):** `countriesInRegion(region)` / `compareCountries` w [tripCountries.ts](src/lib/tripCountries.ts) (`localeCompare` z regułami PL/EN, więc „Łotwa" stoi po „Litwie") - używają ich `CountryPicker` (także wyniki szukania), `CityDrum`, `CountryCityPicker`, `CreateRanking`. ⛔ Nie renderuj `TRIP_COUNTRIES.filter(region)` wprost - kolejność w danych to „od najczęstszych" i na ekranie czytała się jak rozsypanka. Kilka krajów naraz → „Kolekcja miejsc: Polska · Czechy" (bez odmiany), nazwa spoza tabeli (miasto wpisane ręcznie) → ta sama forma z dwukropkiem. Tabele mają `i18n-ignore-start/end` - to dane językowe, nie copy. EN nie odmienia (`declines: false`), przyimek siedzi w szablonie (`Places in {{place}}` / `Trip to {{place}}`).

**Lista „Ogólne" (2026-08-24):** = **JEDYNA prywatna lista** każdego usera. JEDNA, globalna kolekcja (`kind='ranking'`, `list_status='to_visit'`, `is_public=false`, `city=null`, `title='Ogólne'`), get-or-create przez `ensureToVisitList` (`city` param IGNOROWANY - już NIE per-miasto; skonsolidowane migracją `20260824b`). **Nie podlega moderacji** (`guard_discovery_moderation`: `is_public=false` → `moderation_status='approved'`). Pozostałe kolekcje (`visited`) powstają jako PUBLICZNE, ale od 2026-09-17 autor może je schować (patrz „Prywatność kolekcji" niżej). Każde „Zapisz" (drawer [SavePlaceSheet](src/components/plan-wizard/SavePlaceSheet.tsx)) auto-zapisuje miejsce do „Ogólne" (`quickSavePlace`) + pozwala **dodatkowo** dodać do istniejącej/nowej listy (`visited`, publicznej). W drawerze „Ogólne" ZAWSZE na górze i zaznaczone (wiersz wirtualny). Na profilu: **opcja „Ogólne" w dropdownie zakładki Listy** (`[Moje listy | Ogólne | Zapisane]`, `listyTab='ogolne'`) → renderuje `SavedPlacesGrid` inline. Przy tworzeniu pokazuje `fetchSavedPlaces` (płaska, WSZYSTKIE miasta): [CreateFlowSheet](src/components/create/CreateFlowSheet.tsx), [CreateRanking](src/pages/CreateRanking.tsx), [ComposeWyjazd](src/pages/ComposeWyjazd.tsx) (wyjazd = też wszystkie, Ogólne jest globalna). `SavedListsRoutes` = martwy kod.

**Widok wyjazdu i listy - nagłówek (redesign Nat 2026-09-13, makieta „Łódzki citybreak"):** wspólny moduł [TripHeaderChips.tsx](src/components/route/TripHeaderChips.tsx). Belka: **wstecz = chevron w BIAŁYM kółku z szarym obrysem** (`bg-white border-border`; peachy odrzucone), **autor jako pigułka** (`AuthorPill`: peachy, CZARNY `@nick`, awatar z ramką - nakładka ZOSTAJE) + po prawej: gość = **flaga zgłoszenia** (`ReportContentSheet` z własnym triggerem; wcześniej link „Zgłoś" pod listą miejsc); właściciel/uczestnik = **menu „…" w BELCE** (białe kółko z delikatnym cieniem `shadow-[0_1px_5px_rgba(0,0,0,0.12)]`; w wyjeździe obok BIAŁEGO kółka udostępniania - było żółte do 2026-09-15, na żółtej belce znikało) - przy tytule nie ma już żadnych guzików poza polubieniem. Menu kolekcji: nazwa · **Kraj i miasto** ([ListScopeSheet](src/components/lists/ListScopeSheet.tsx), 2026-09-14: ten sam `CountryPicker` co przy tworzeniu + opcjonalne miasto z chipów pierwszego kraju albo wpisane; zapis `discovery_collections.countries/city`, chip pod nazwą = miasto albo kraje przez `scopeLabel`) · tło · usuń. **Belka widoku WYJAZDU jest ZAWSZE ŻÓŁTA `#FDF184`** (prośba Nat 2026-09-15; wcześniej `bg-background`) - ma wyglądać jak belka kolekcji, z tą różnicą, że kolekcja bierze swój kolor przewodni, a wyjazd ma jeden, stały. ⚠️ Kolorowa belka wymusza BIAŁE kółka na guzikach: kółko udostępniania było żółte i na żółtym tle znikało, flaga zgłoszenia była samą ikoną bez tła, a pigułka autora jest peachy i zlewa się z żółtym - wszystkie trzy dostały biel (`AuthorPill className="!bg-white"`), dokładnie tak jak rozwiązano to w kolekcji. Pasek „masz zaproszenie" nad belką zostaje peachy `#fcede3`: jest wyraźnie bledszy, więc nadal się odróżnia. Żółte kółko udostępniania ZOSTAJE tam, gdzie tło jest jasne, czyli przy dolnym CTA. **Belka widoku kolekcji ma KOLOR PRZEWODNI kolekcji** (2026-09-14): tło paska = `listTheme(theme, id).bg`, czyli ten sam kolor co kafelek w Eksploracji - wejście z siatki nie zmienia tożsamości. Guziki zostają białymi kółkami (czytelne na wszystkich 9 kolorach), a pigułka autora dostaje `!bg-white` zamiast peachy (na jasnych motywach peachy zlewało się z paskiem). **Każda nowa kolekcja dostaje LOSOWY kolor** (`randomListTheme()` w `createListFromSavedPlaces` i `CreateRanking`) - losowanie idzie z CIEPLEGO RDZENIA marki (pierwsze 9 kolorow), nie z calej palety: swieza kolekcja ma wygladac brandowo, a nie wylosowac granat - wcześniej `theme` zostawał NULL, a kolor był tylko wyliczany z id przy renderze. Prywatna „Ogólne" koloru nie dostaje (nie renderuje się jako kafelek). **Polubienie = BRANDOWE SERCE na wysokości tytułu** (`BrandHeart` w [BrandHeart.tsx](src/components/BrandHeart.tsx) = inline svg z `public/Ikona_serce.svg`, kontur = nie polubione / pełne = polubione, jak `BrandStar`; NIE lucide `Heart`; tylko wyjazd, tylko gość; licznik gdy > 0) - z animacją w [TripLikeButton](src/components/route/TripLikeButton.tsx) (2026-09-14): polubienie = serce pompuje się sprężyną, fala na zewnątrz, sześć małych serduszek na boki, licznik wjeżdża od dołu, haptyka średnia; cofnięcie = serce się sflacza z przechyłem, fala zapada do środka, haptyka lekka. Gra na tapnięciu (stan optymistyczny), nie na odpowiedzi serwera. Gwiazdka zostaje wyłącznie dla „topki" (`BrandStar` w [BrandStar.tsx](src/components/BrandStar.tsx) = brandowa gwiazdka z konturem, gdyby była potrzebna). **Daty wyjazdu pod chipami = sama informacja** (ikona kalendarza + zakres, bez ołówka); ustawianie/zmiana dat żyje w menu „…" w belce („Dodaj daty wyjazdu" / „Zmień daty wyjazdu", tylko właściciel; wiersz „Dodaj daty" pod tytułem usunięty 2026-09-13). **Udostępnianie = brandowe ŻÓŁTE kółko z brązową ikoną bezpośrednio NA PRAWO od dolnego CTA** (na kolekcji obok „Zapisz tę listę" / „Dodaj nowe miejsce"). **ZAPIS WYJAZDU JEST NATYCHMIASTOWY** (2026-09-16): tapnięcie zakładki od razu wrzuca wyjazd do „Zapisane". ⛔ NIE przywracaj arkusza „Kiedy planujesz ten wyjazd?" - kalendarz przed zapisem był krokiem DODATKOWYM przed czynnością jednego tapnięcia (jak polubienie), a kto arkusz zamknął, ten nie zapisywał nic. `saved_routes.planned_date` zostaje w bazie i kafelek w „Zapisane" dalej pokazuje daty ustawione wcześniej, ale nowe zapisy jej nie mają - dlatego `upsert` świadomie **nie wysyła** tej kolumny (inaczej wyczyściłby historyczną wartość). ⚠️ **W WYJEŹDZIE u gościa jest inaczej od 2026-09-16:** zapis zszedł z dolnego paska i stoi jako **brandowa zakładka OBOK SERCA, na wysokości tytułu** (prośba Nat) - zapis i polubienie to ta sama klasa gestu („zostawiam ślad na cudzym wyjeździe") i mają stać razem, a nie jedno przy tytule, drugie na dole ekranu. Zwolniony dolny pasek to teraz **jeden guzik pełnej szerokości „Udostępnij ten wyjazd"**, żółty `#FDF184` z brązowym napisem - udostępnianie w całej apce ma kolor żółty, a pomarańcz zostaje dla akcji, która coś tworzy albo zapisuje. **Chipy dni** w wyjeździe: zaznaczony = peachy `#FCEDE3` + brązowy tekst (NIE pomarańcz, NIE żółty). Pod tytułem **chipy z SAMYM OBRYSEM** (`HighlightChips`, bez wypełnienia, **jeden szary obrys `#D9D9D9` 1,5 px dla wszystkich** - kolorowe obrysy i `border-2` odrzucone 2026-09-13, brązowy tekst): miasto (`route.city` przed krajami), liczba miejsc (copy z polską odmianą i wielką literą „15 Miejsc", a w kolekcji **„7 / 15 Miejsc"** = odwiedzone / wszystkie: moje odwiedziny na własnej kolekcji, autora na cudzej), wyróżnione gwiazdką (tylko gdy > 0) - ikony chipów to BRANDOWE SVG (`Ikona_Miejsca` w brązie `#5B2C06`, `Ikona_Gwiazdka` pomarańczowa) **w środku pigułki**, 18 px w linii z tekstem (wariant z ikoną wychodzącą poza obrys odrzucony 2026-09-14). Opis wyjazdu/listy `text-[15px] text-foreground/80`. Wiersz miejsca ([RoutePlaceRow](src/components/route/RoutePlaceRow.tsx)): miniatura 2:3 `w-16 h-24 self-start` (wariant rozciągany odrzucony), chip kategorii peach + brąz, akcje w kółkach 40 px: **gwiazdka „topki"** (pierwsze kółko od lewej; tylko gdy `onToggleTop`) · Google · **dodaj zdjęcie** (pomarańczowe kółko, brandowy aparat `CAMERA_ICON` = `public/aparat.svg` w żółtym `#FDF184` + mały plus) · „…" z resztą (w tym „dodaj notkę" - osobne kółko notki odrzucone jako zagęszczające) - zdjęcie wychodzi z `menuExtras` po kluczu `photo`, callery bez zmian. **Gwiazdka (wieczór 2026-09-13): tap = przełącz z lotem gwiazdki do nazwy (jak dotąd); PRZYTRZYMANIE = „ładowanie"** - po 180 ms wokół kółka rośnie pierścień (żółty tor, pomarańczowy postęp, ~1 s), haptyka NARASTA w progach (light ×2, medium ×2, na końcu heavy + success), a pełne naładowanie **przypieczętowuje** gwiazdkę (ta sama akcja `onToggleTop`, ale z pieczątką: odbicie kółka + fala + rozprysk, bez lotu). Puszczenie przed końcem = nic. Gwiazdka już przypięta nie ładuje się (tap ją zdejmuje). Postęp idzie w refach prosto w style (bez setState co klatkę). ⛔ **Werdykty przy miejscach („Musisz odwiedzić!" / „Warto wpaść" / „Przy okazji") USUNIĘTE z apki 2026-09-13** - jedynym wyróżnieniem miejsca jest gwiazdka; `pin_ratings.verdict` i stare wartości w `pins.tags` zostają w bazie, ale `verdictOf` służy już tylko do ich POMIJANIA przy renderze (wiersz, `PlaceNotes`, pasek udostępniania), a sortowanie „Wszystkie" ma tylko gwiazdkę. **Pływające akcje wyjazdu (`TripFabStack`): rozwinięcie przyciemnia ekran (`bg-black/35`) i każde kółko ma podpis w białej pigułce** („Zmień kolejność", „Dodaj miejsce", „Czat" = `sharing:fabs.chat`, **„Dostosuj ilość dni"** od 2026-09-16, tylko właściciel). ⚠️ Stos to rzeczy, które robi się Z wyjazdem w trakcie pracy nad nim; menu „…" to jego USTAWIENIA. „Dostosuj ilość dni" stoi w obu, bo pasuje do obu odczytań - i dlatego, że Nat trzy razy szukała tej opcji „pod chevronem", czyli właśnie w stosie. **„Odwiedzone" = ŻÓŁTA pigułka (`#FDF184`) POD miniaturką: awatar + podwójny ptaszek (`CheckCheck`), bez napisu** - na cudzej liście awatar autora, na własnej mój; przełącznik „byłem tu" na WŁASNEJ liście siedzi w menu „…" (2026-09-13; wcześniej osobne kółko z ptaszkiem i pigułka z napisem). **Notka albo zdjęcie dodane do miejsca we WŁASNEJ kolekcji odhacza je jako odwiedzone automatycznie** (`markVisitedAuto` w [SharedList](src/pages/SharedList.tsx), 2026-09-14; toast „Odhaczono jako odwiedzone" z „Cofnij"; tylko gdy jeszcze nie odhaczone). Kolumna treści wiersza ma `min-h-24` i składa akcje na dole (`mt-auto`) - miejsce bez notki i zdjęć (stan zero) jest zwartym wierszem z kółkami na wysokości dolnej krawędzi miniatury. **Zakładka „zapisz" = [BrandBookmark](src/components/BrandBookmark.tsx)** w wierszu, menu, kolumnie akcji karty i CTA „Zapisz ten wyjazd / tę listę" - NIE lucide `Bookmark` i NIE `BrandIcon src={SAVE_ICON}`. Powod (prosba Nat 2026-09-15): `BrandIcon` rysuje CSS maske, wiec zakladka byla ZAWSZE pelna i nie niosla zadnego stanu. `BrandBookmark` to inline svg z propem `filled`: **pusty kontur = miejsce/kolekcja/wyjazd nigdzie nie zapisane, pelny = zapisane**. `SAVE_ICON` zostaje w `BrandIcon.tsx` dla miejsc, gdzie stanu nie ma.

**Kolekcje na profilu wyglądają TAK SAMO jak w Eksploracji (2026-09-14):** **Kolekcje WSPÓŁTWORZONE pokazują się na profilu ich współtwórcy - własnym I PUBLICZNYM** (2026-09-15). Zapytania `profile-list-feed` i `public-list-feed` robią po DWA zapytania (własne + te z `discovery_collection_members`) i scalają je po `id`; kafelek pokazuje pigułkę WŁAŚCICIELA kolekcji, nie właściciela profilu. ⚠️ Na profilu publicznym czyta to OSOBA TRZECIA, więc działa dopiero od migracji `20260915k` - wcześniej polityka `dcm_read` wpuszczała tylko właściciela i członków, lista wracała pusta i współtworzone kolekcje po cichu znikały z cudzego profilu (zgłoszenie Nat: „dodałam usera, a kolekcji u niego nie widać"). **Od 2026-09-15 dotyczy to TAKŻE profilu publicznego** ([PublicProfile](src/pages/PublicProfile.tsx)) - do tego dnia renderował kolekcje `ProfileFeedCard` (rząd miniatur), więc ta sama kolekcja miała TRZECI wygląd; jego zapytanie dociąga teraz `theme`, `countries` i `fetchListVisitCounts`, a `savesCount` NIE jest podawany (licznik zapisów to informacja zwrotna dla autora, nie element kafelka u oglądającego). **UKŁAD kafelka kolekcji (prośba Nat 2026-09-15): autor i WSZYSTKIE chipy w JEDNYM rzędzie → tytuł → okładki miejsc.** Najpierw siatka stała na górze i podpis pod nią, potem podpis poszedł na górę, a na końcu chipy dojechały do rzędu autora (`flex-wrap items-center` - pigułka autora jest wyższa od chipów, stąd `items-center`), żeby podpis nie zajmował trzech osobnych linii nad zdjęciami. **Pigułka autora pokazuje NAZWĘ I @nick obok siebie** (`GridItem.authorHandle`, nick przygaszony `opacity-70`) - tylko na kolekcjach, kafelki wyjazdów zostają przy samym nicku. ⚠️ `AuthorPill` porównuje nazwę z nickiem i **chowa nick, gdy się dublują**: część zapytań podstawia username jako nazwę (`author_name: first_name || username`), więc bez tego wychodziło „nyszje @nyszje". zakładka Kolekcje → „Moje kolekcje" renderuje ten sam `GridTile` co siatka odkrywania (kolorowe tło z palety, mini-siatka miejsc 3 w rzędzie, pigułka autora, chipy „odwiedzone/wszystkie" i miasto) - wcześniej był tu `ProfileFeedCard` (rząd miniatur), więc ta sama kolekcja wyglądała inaczej w dwóch miejscach. **Podzakładka „Zapisane" (kolekcje od innych) używa tego samego kafelka** - z pigułką AUTORA kolekcji (nie właściciela profilu), więc zapytanie dociąga profile właścicieli (`username`, `avatar_frame`). Plakietka „ktoś dodał miejsce" została i stoi NAD kafelkiem (kafelek nie ma na nią slotu) - to jedyny powód, żeby do zapisanej kolekcji wrócić. Zapytanie `profile-list-feed` dociąga na to `theme`, `countries` i liczniki odwiedzin (`fetchListVisitCounts`). **Kolekcje nie pokazują już TAGÓW nigdzie (2026-09-15).** Historia: `af2f7ead` zdjął pigułki z tagami z kart na WŁASNYM profilu, ale świadomie zostawił je na publicznym („pomagają rozpoznać czym jest cudza lista"). Przejście profilu publicznego na wspólny `GridTile` (2026-09-15) zabrało je stamtąd przy okazji, bo kafelek ma chipy, nie tagi. Został ostatni ślad: **wiersz miejsca w widoku kolekcji** renderował `discovery_items.tags`, więc STARE kolekcje (tagi z przebudowy list 2026-08-17) wyglądały inaczej niż nowe, które ich nie zapisują - `SharedList` podaje teraz `tags: []` do `RoutePlaceRow`. Kolumna ZOSTAJE w bazie i edytor `/zestawienie/:id/edytuj` nadal je zapisuje, więc powrót to skasowanie jednej linii. ⚠️ Tagi miejsca w WYJEŹDZIE (`pins.tags`) to osobna, żywa funkcja i zostają. ⛔ Zmieniasz kafelek listy w [FeedTiles](src/components/home/FeedTiles.tsx) - zmieniasz go we WSZYSTKICH TRZECH miejscach naraz (eksploracja, własny profil, profil publiczny), i o to chodzi. **Przewijanie kolekcji na profilu ma SNAP** (prosba Nat 2026-09-14): scroller profilu dostaje `snap-y snap-mandatory scroll-pt-[44px]` (= wysokosc przyklejonej belki zakladek) TYLKO na podzakladkach z kafelkami kolekcji (`listSnap` w [TravelerProfile](src/pages/TravelerProfile.tsx)); „Ogolne" (siatka miejsc) i Wyjazdy (karty roznej wysokosci) zostaja przy swobodnym przewijaniu. Naglowek profilu ma wlasny `snap-start`, bo inaczej `mandatory` nie pozwolilby sie przy nim zatrzymac i krotkie pociagniecie od gory od razu skakaloby na pierwszy kafelek. **Licznik ZAPISOW kolekcji = brandowa zakladka + liczba W RZEDZIE CHIPOW, dociagnieta do PRAWEJ** (`GridItem.savesCount`; prosba Nat 2026-09-17 - ma stac na wysokosci pigulek o liczbie miejsc i miescie). ⚠️ Stal tam juz 2026-09-14, 15.09 przeniesiony do tytulu, 17.09 z powrotem - i to NIE jest krazenie w kolko: powodem przeprowadzki do tytulu bylo to, ze w rzedzie chipow wchodzil pod PIGULKE AUTORA i zabieral gore kafelka, a na profilu pigulki juz tam nie ma (od 2026-09-17 stoi klaster awatarow). `savesCount` podajemy wylacznie na wlasnym profilu, wiec w eksploracji, gdzie pigulka autora zostaje, ten rzad sie nie zageszcza. ⛔ „Nowe miejsce!" i licznik zapisow dziela JEDNA grupe z `ml-auto` - dwa osobne `ml-auto` w tym samym rzedzie rozjechalyby je na dwie krawedzie. Podawany WYLACZNIE na „Moje kolekcje", bo to informacja zwrotna dla autora, a nie element kafelka w eksploracji. Zero sie nie renderuje (wiekszosci kolekcji nikt jeszcze nie zapisal, a rzad zer wygladalby jak zepsuty widok). Ikona to INLINE svg ([BrandBookmark](src/components/BrandBookmark.tsx)), NIE `BrandIcon` - na kafelkach WebKit gubi maske CSS. **Obok zakladki stoi CHIP PRYWATNOSCI** (`GridItem.isPublic`, prosba Nat 2026-09-17): klodka = kolekcja prywatna, globus = publiczna, bez liczby, bo to stan, a nie statystyka. Stoi PRZED zakladka, wiec para czyta sie „prywatna, 3 zapisy" i od razu tlumaczy, czemu licznik prywatnej kolekcji nie rosnie. ⛔ Pole podaje WYLACZNIE wlasny profil („Moje kolekcje" i „Wspolne" - wspoltworca tez musi wiedziec, czy to, co dopisuje, zobaczy swiat). W eksploracji, na profilu publicznym i w „Zapisanych" `isPublic` zostaje `undefined` i chip sie NIE renderuje: tam wszystko, co widac, jest z definicji publiczne, wiec globus na kazdym kafelku bylby czystym szumem. ⛔ **Nie dokladaj tu serca z `likes_count`** - polubienia kolekcji zdjelismy 2026-09-01 (`likes.ts`), kolumna trzyma 7 lajkow sprzed tej daty i nigdy nie urosnie; Nat swiadomie zostawila sam `saves_count` (zywy, trigger `saved_collections_count_sync`).


**WSPÓŁTWORZONE kolekcje trafiają do „Moje kolekcje" (prośba Nat 2026-09-15).** Kolekcja, do której ktoś mnie zaprosił, jest moja do współtworzenia (dodaję miejsca, notki, zdjęcia), więc stoi w „Moje kolekcje", nie tylko w „Zapisane". `profile-list-feed` robi **DWA zapytania** - własne (`user_id = ja`) i te z `discovery_collection_members` - i scala je po `id`, sortując po `updated_at`. ⛔ NIE łącz tego w jedno `.or(...)`: lista id w `id.in.(…)` ma przecinki w środku nawiasu, a PostgREST rozbija `or` po przecinkach i po cichu oddaje pustkę (memory `feedback_postgrest_or_filter_quoting`). Widzialność wierszy daje polityka `discovery_collections_member_read` (+ `discovery_items_member_read` na pozycje) z migracji `20260915c`. Kafelek współdzielonej kolekcji pokazuje **pigułkę jej WŁAŚCICIELA**, nie moją (zapytanie dociąga profile właścicieli do `_owner`), i **nie pokazuje licznika zapisów** - to informacja zwrotna dla autora.

**Współtwórcy w BELCE widoku kolekcji (prośba Nat 2026-09-15):** obok pigułki autora stoją pigułki pozostałych uczestników (awatar z ramką + `@nick`, tap = ich profil), `CO_AUTHORS_SHOWN` = 2, reszta jako `+N`. Rząd się **zawija** (`flex-wrap`): między guzikiem wstecz a akcją zostaje ~280 px, więc trzy pigułki obok siebie by się nie zmieściły, a poziomy scroll w belce gryzłby się z gestami. Przy zero współtwórcach belka wygląda jak dotąd. ⚠️ Zapytanie o członków (`collectionMembersKey`) trzyma teraz PEŁNE wiersze, a `memberIds` liczy się obok przez `useMemo` - wcześniej `select` spłaszczał je do samych id, a belka potrzebuje awatarów i nicków. ⚠️ W JSX nie pisz `coAuthors.length > CO_AUTHORS_SHOWN` - bramka `i18n:check` czyta `>` w znaczniku jako początek tekstu i zgłasza „polski napis na sztywno"; porównanie ma siedzieć w zmiennej (`extraCoAuthors`).

**Profil publiczny (cudzy) — `/profil/:username`, [PublicProfile.tsx](src/pages/PublicProfile.tsx):** ten sam layout kart, ale **2 zakładki** (Listy · Wyjazdy), bez „Zapisane", tylko listy `visited`, **bez edycji/usuwania** (owner-only).

**⛔ USUNIĘTE ekrany (2026-08-20):** `/dziennik` (Journal) i `/polubione` (LikedPlaces) — cała treść przeniesiona do zakładek profilu. `Journal.tsx` + `LikedPlaces.tsx` usunięte. **Routy zostają jako `<Navigate>` redirecty** (stare deep-linki / pushe nie ubijają apki): `/dziennik` → `/moj-profil?tab=wyjazdy`, `/polubione` → `/moj-profil` (dawniej `?tab=zapisane`, zakładka usunięta 2026-08-24). Nowy kod nawiguj **wprost** na `/moj-profil?tab=…`, nie na `/dziennik`/`/polubione`. **`JournalTab` ZOSTAJE** (reused w [CreateDrafts](src/pages/CreateDrafts.tsx) „Robocze", route `/utworz/robocze`).

**Model prywatności list (patrz też memory `project_place_lists_model`):** zapis miejsca = **prywatna** lista „Do zobaczenia" (`list_status='to_visit'`, `is_public=false`). Świadoma polecajka = `list_status='visited'`, domyślnie `is_public=true`. Bookmark ≠ polecenie.

**PRYWATNOŚĆ KOLEKCJI WRACA POD „…" (prośba Nat 2026-09-17).** [ListPrivacySheet](src/components/lists/ListPrivacySheet.tsx), pozycja w menu obok „Osoby w kolekcji" (obie odpowiadają na to samo pytanie: kto ma dostęp), ikona w wierszu niesie stan AKTUALNY. ⚠️ To POWRÓT opcji zdjętej 2026-08-24 (`ad62204b`, „wszystkie kuratorskie listy publiczne - rozwój bazy discovery") - baza nigdy nie przestała jej obsługiwać, więc **nie było potrzebnej migracji**. Sprawdzone na prodzie w `BEGIN … ROLLBACK` na koncie NIE-ADMINA (Nat i `99148a59` są adminami i `discovery_collections_admin_all` przepuszcza je wszędzie, więc test na ich koncie nic by nie powiedział): właściciel przełącza `is_public` w obie strony (kolumny nie chroni `guard_collection_protected_columns`), po sprywatyzowaniu obcy nie czyta ani kolekcji, ani pozycji, ani składu (0/0/0), a właściciel i współtwórcy czytają dalej (`can_read_collection` ich przepuszcza niezależnie od `is_public`).
- ⚠️ **Kto kolekcję ZAPISAŁ, dostęp TRACI** (zmierzone na prodzie). To jedyny skutek, którego autor nie zobaczy u siebie na ekranie, więc opcja „Tylko dla mnie" pokazuje liczbę takich osób - ale tylko wtedy, gdy zmiana faktycznie komuś coś zabierze (kolekcja jeszcze publiczna i `saves_count > 0`). Dlatego zapytanie `shared-list` dociąga `saves_count`.
- ⚠️ **Moderacja NIE wraca do kolejki.** Żywy `guard_discovery_moderation` zostawia `approved`, a `rejected` zostawia `rejected` - przełączaniem prywatności nie da się wyprać decyzji admina. ⛔ Wersja z migracji `20260822` (publiczna → `pending`) jest NIEAKTUALNA, nie sugeruj się plikiem - czytaj `pg_get_functiondef`.
- **Po zapisie wołamy `invalidateContentLists()`**, nie pojedyncze klucze: prywatyzacja zabiera kolekcję z eksploracji, obu profili, „Zapisanych" u innych i z wyszukiwarki naraz. Czyszczenie kluczy po jednym było źródłem błędu „usuwam, wracam na profil i nadal to widzę" (kosz).
- **Toast ma „Cofnij"** - cofnięcie to jeden zapis w drugą stronę, a o skutku user często dowiaduje się dopiero z arkusza.
- **Chip „Prywatna" stoi w rzędzie chipów nagłówka** (`HighlightChips privateLabel`). Przełącznik, którego stanu nie widać na ekranie, zmusza do otwierania menu za każdym razem. ⛔ Prywatnej „Ogólne" (`to_visit`) NIE oznaczamy i nie dostaje ona tej pozycji w menu - tam prywatność to definicja, nie stan do sprawdzenia.

**Polubienia + powiadomienia (2026-08-20):** tabele `likes` (trasy) i `collection_likes` (listy) + kolumny `likes_count`. Powiadomienia `route_liked` / `list_liked` (serce) i `list_saved` (bookmark) wstawiane triggerami **SECURITY DEFINER** (klient nie ma INSERT na `notifications`). Helpery: [src/lib/likes.ts](src/lib/likes.ts). Zapis cudzej trasy woła RPC `notify_route_used`, zapis listy `notify_collection_saved`.

**Opis wyjazdu = `routes.review_narrative` (właściciel edytuje go w miejscu „Dodaj / Edytuj opis"); notka WŁAŚCICIELA w `route_member_covers.note` to ten sam głos** - gdy opisu nie ma, jest opisem (czysty tekst pod tytułem) i NIGDY nie renderuje się jako dymek pod spodem (dymki = notki pozostałych uczestników; zgłoszenie Nat 2026-09-14 „Majówka 2025", dane przeniesione migracją `20260914_owner_trip_note_to_description`). **TAPNIĘCIE W NAZWĘ otwiera jej zmianę** (zgłoszenie testerki 2026-09-16: „zamiast kropek od razu intuicyjnie klikam na samą nazwę"). Menu „…" zostaje - tapnięcie to skrót, nie zamiennik. W wyjeździe pod `canEdit`, w kolekcji pod `isOwner` (nazwę kolekcji zmienia właściciel); kto nie ma prawa, dostaje zwykły nagłówek, bez guzika - przycisk bez akcji uczy, że nic się nie dzieje. **Zmiana nazwy wyjazdu/kolekcji z menu „…"**: menu ma `onCloseAutoFocus={preventDefault}` (Radix oddawał fokus guzikowi i klawiatura nie wchodziła), pole dostaje fokus z efektu, a dolny pasek CTA i stos akcji są schowane na czas edycji (`editingName`).

**ZDJECIE W WYJEZDZIE: PUBLICZNE albo TYLKO DLA ZNAJOMYCH (prosba Nat 2026-09-17, migracja `20260917b`).** Przelacznik stoi w podgladzie pelnoekranowym galerii (pigulka w prawym dolnym rogu, naprzeciw polubienia - pokazuje stan AKTUALNY, tapniecie go zmienia), a na kafelku siatki zdjecie prywatne ma plakietke „Znajomi". Plakietke widzi KAZDY, kto to zdjecie dostal, nie tylko wlasciciel: znajomy ma wiedziec, ze oglada tresc prywatna. Warstwa danych: [tripPhotoAudience.ts](src/lib/tripPhotoAudience.ts).
- ⛔ **Bramka MUSI byc w bazie, nie w kliencie.** `routes.review_photos` to kolumna TABLICOWA - RLS filtruje wiersze, nie elementy tablicy, wiec zdjecie zostawione w niej jest czytelne dla kazdego, kto moze przeczytac trase. Dlatego zdjecia dla znajomych maja wlasna TABELE `route_friend_photos`, a `review_photos` staje sie z definicji tablica zdjec PUBLICZNYCH. Skutek uboczny na plus: ~40 miejsc w kodzie, ktore ja czytaja (okladka hero, strona linku, podsumowanie, przypomnienia, migawka przy usunieciu hosta), dziala bez zmian i Z GRUNTU nie moze pokazac tresci prywatnej. Zdjecia przy MIEJSCU sa juz wierszami, wiec `pin_photos` dostaly kolumne `visibility`.
- Przelaczanie obu zrodel idzie przez JEDNO wejscie `set_trip_photo_audience(p_route, p_url, p_friends)`, zeby galeria nie musiala wiedziec, skad pochodzi zdjecie. `sort_order` trzyma pozycje, ktora zdjecie mialo w `review_photos` - powrot na publiczne wstawia je TAM, GDZIE BYLO, a nie na koniec galerii.
- ⚠️ **„Tylko dla znajomych" bramkuje SWIAT ZEWNETRZNY, nie ludzi wewnatrz wyjazdu.** Wlasciciel i uczestnicy widza w swoim wspolnym wyjezdzie wszystko - inaczej wlasciciel mialby u siebie tresc, ktorej nie widzi, a ktora wolno mu skasowac (polityka DELETE), i to jest niespojnosc, nie prywatnosc.
- ⚠️ Dopasowanie po KLUCZU PLIKU (`trip_photo_key` w SQL = `photoStorageKey` w TS), nie po calym adresie: ten sam plik bywa zapisany raz przez api.spontaway.com, a raz przez `<ref>.supabase.co`. Zmieniasz jedno, zmien drugie.
- ⛔ **W plpgsql NIE opieraj sie na `found`** - nadpisuje je KAZDY kolejny `SELECT INTO`. W pierwszej wersji `set_trip_photo_audience` odczyt `review_photos` ustawial `found` na true i funkcja wychodzila przed zapisem, zwracajac „friends" bez zadnego skutku (zlapane testem na prodzie). Sprawdzaj wprost, czy rekord sie wypelnil.
- ⚠️ **Przy okazji zalatany PRZECIEK zdjec.** Polityka `pin_photos` brzmiala `r.user_id = auth.uid() OR r.is_shared = true`, a `is_shared` NIE znaczy „opublikowany" (`inviteUsersToRoute` ustawia je kazdemu wyjazdowi grupowemu, takze roboczemu) - ta sama klasa bledu, ktora naprawila migracja `20260916c` na `routes`, tylko na tabeli zdjec. Zmierzone przed naprawa: 22 zdjecia z 1 nieopublikowanego wyjazdu czytelne dla KAZDEGO zalogowanego. Ta sama luka zalatana w `group_trip_photos`. Dokladajac polityke na tresc pochodna od trasy: warunek publicznosci to `is_shared AND status = 'published'`, a uczestnicy potrzebuja JAWNEGO warunku czlonkostwa w `group_session_members`.


**ZDJĘCIE MIEJSCA TEŻ IDZIE ZA MIEJSCEM (2026-09-16, migracja `20260916h`).** Notki działały tak od 2026-09-13, zdjęcia zostały per-kolekcja - ta sama kawiarnia miała moje zdjęcie w jednej kolekcji i pustkę w drugiej (zgłoszenie testerki „notatki i zdjęcia dla miejsc nie przenoszą się między kolekcjami"). Teraz lustro notek: trigger `trg_dip_spread` rozsyła nowe zdjęcie na WSZYSTKIE kolekcje tego usera (własne i współtworzone) z tym miejscem, `trg_dip_unspread` zabiera je stamtąd przy usunięciu, a `trg_dip_prefill` daje istniejące zdjęcia nowo dodanej pozycji. ⚠️ Rozchodzi się WYŁĄCZNIE własne zdjęcie (`user_id`) - cudzych nie kopiujemy nigdzie. Tożsamość miejsca = `place_note_key`, rekurencję wycisza `trasa.photo_sync` (jak `trasa.note_sync`). Unikalny indeks `dip_unique_place_url` jest po to, żeby `on conflict do nothing` miało na czym pracować. ⛔ **Po zapisie notki albo zdjęcia unieważniaj cache PREFIKSEM bez id** (`["collection-notes"]`, `["collection-photos"]`, `["shared-list-items"]`): baza rozsyła zmianę po pozostałych kolekcjach, ale `collectionNotesKey(id)` czyści tylko bieżący widok, a sąsiednia kolekcja trzyma swoją odpowiedź przez `staleTime` 30 s i pokazuje pusto. Notki propagowały się poprawnie już wcześniej - zgłoszenie dotyczyło WYŁĄCZNIE nieodświeżonego ekranu (sprawdzone na prodzie: wpis w jednej kolekcji = wiersz w obu).

**Notka o miejscu = JEDNA na (user, miejsce) - wszędzie (decyzja Nat 2026-09-13, migracja `20260913f_place_note_sync.sql`):** dawniej notka żyła per wiersz (`discovery_items.short_desc` w każdej kolekcji osobno, `pin_ratings.note` w każdym wyjeździe osobno). Teraz tabele mają ten sam kształt, ale **triggery je synchronizują**: zapis notki w dowolnym miejscu → `propagate_place_note(user, nazwa, notka)` rozchodzi ją na wszystkie kolekcje usera (także prywatną „Ogólne"), wszystkie jego wiersze `pin_ratings` i **wyjazdy usera** (`routes.user_id`) z tym miejscem (wiersz `pin_ratings` powstaje, gdy go nie było); nowa pozycja kolekcji / nowy pin dostaje istniejącą notkę od razu (`list_item_note_prefill`, `pin_note_prefill` dla `added_by`/właściciela). Tożsamość miejsca = **znormalizowana nazwa** (`place_note_key` = lower/trim), ta sama reguła co `place_photos` `nm:<nazwa>` - sieciówka o tej samej nazwie w dwóch miastach dzieli notkę (świadome uproszczenie). Rekurencję wycisza flaga `trasa.note_sync` (transakcyjna). Wyczyszczenie notki w jednym miejscu czyści ją wszędzie. **Konsekwencja prywatności:** notka napisana w prywatnym wyjeździe jest tą samą notką, co w publicznej kolekcji z tym miejscem - i tam jest widoczna. Wizytówka „Od użytkowników" (`fetchPlaceNotes`) dedupuje **po `user_id`** (jedna notka na osobę) i bierze nazwę/awatar z `profiles` dla obu źródeł. Backfill: przy konflikcie wygrała notka z kolekcji, potem dłuższa (5 konfliktów, 4 u Nat).

**Sygnał „nowe miejsce" na kafelku kolekcji w Eksploracji (2026-09-14):** okładka nowo dodanego miejsca dostaje **brandową gwiazdkę NACHODZĄCĄ na róg** (inline `BrandStar`, nie maska CSS - patrz reguła przy nakładkach awatara), a w rzędzie chipów, **dociągnięta do prawej**, staje biała pigułka „★ Nowe miejsce!". Nowe miejsca idą na POCZĄTEK mini-siatki, bo kafelek pokazuje tylko kilka pierwszych - inaczej nowość chowałaby się pod „+N". ⚠️ Punkt odniesienia to `saved_collections.seen_item_count`, więc gwiazdka pojawia się na kolekcjach, które user ma **ZAPISANE** (ten sam mechanizm co plakietka w „Zapisane" na profilu). Dla pozostałych nie ma od czego liczyć nowości: `discovery_items` **nie ma `created_at`**, a dorobienie go nic nie da, dopóki `CreateRanking` przy edycji kasuje i wstawia wszystkie pozycje od nowa (każda edycja wyglądałaby jak komplet nowych miejsc). Obserwujący bez zapisu dostają zamiast tego powiadomienie (niżej).

**Kolory kolekcji: 18 presetow + kolor wlasny (2026-09-15, migracja `20260915_list_theme_custom_color`).** `discovery_collections.theme` przyjmuje trzy ksztalty: NULL (kolor liczony z id), id z palety (9 cieplych z rdzenia marki + 9 chlodniejszych: `cream sand sage moss teal sky denim plum wine`) albo `#RRGGBB` z pipety (`<input type="color">`, ten sam uklad co przy kolorze ramki awatara: `input` = podglad + zapis odroczony 600 ms, `change` = zapis od razu). ⛔ **Kolor tekstu NIE jest zapisywany** - liczy go `inkFor()` z luminancji tla (WCAG, prog 0.5), wiec zaden kolor z pipety nie moze dac nieczytelnego kafelka. Dokladajac kolor do palety: wpis w `LIST_THEMES` + id w CHECK migracji + etykieta w `routelist.theme.*` (PL i EN).

**KOSZ: usuniety wyjazd i kolekcja wracaja przez 7 dni (2026-09-15, migracja `20260915b_trash_soft_delete`).** Do tej pory „Usun" bylo nieodwracalne - klient robil DELETE, a kaskada FK zabierala piny, zdjecia, notki i zapisy innych userow; jedynym ratunkiem byl toast „Cofnij" na kilka sekund.
- **„Usun" = `deleted_at`**, nie DELETE. Wszystkie usuniecia z UI ida przez [trash.ts](src/lib/trash.ts) (`moveToTrash` / `moveManyToTrash`). ⛔ **NIE kasuj przy tym dzieci** (`pins`, `discovery_items`, `chat_sessions`) - odzyskany wyjazd ma wrocic kompletny. Wyjatkiem sa dwie sciezki ROLLBACKU po nieudanym tworzeniu (`createWyjazd.ts`, `placeLists.ts`), gdzie zostaje twardy DELETE osieroconego wiersza.
- **Widocznosc egzekwuje RLS, nie klient:** kazda polityka SELECT na `routes` i `discovery_collections` ma `deleted_at IS NULL`, wiec wiersz znika ze WSZYSTKICH widokow (eksploracja, profil, zapisane u innych, wyszukiwarka, link) bez dotykania jednego zapytania. Dzieci znikaja same, bo czyta sie je przez zlaczenie z rodzicem - ale ZOSTAJA w bazie (sprawdzone na prodzie w `BEGIN … ROLLBACK`: 10 pozycji przed, 0 widocznych po usunieciu, 10 z powrotem po przywroceniu). Polityka admina (`discovery_collections_admin_all`) celowo BEZ tego warunku - panel ops musi widziec tresc zgloszona tuz przed „usunieciem".
- **Kosz obsluguje sie WYLACZNIE przez SECDEF RPC**, bo wiersz jest niewidoczny nawet dla wlasciciela: `my_trash()`, `restore_from_trash(kind,id)`, `purge_from_trash(kind,id)`, `soft_delete_content(kind,id)`. Kazda ma `REVOKE ... FROM PUBLIC, anon` + `GRANT ... TO authenticated` (regula z audytu). `purge_expired_trash()` nie ma grantu dla nikogo - wola ja tylko cron `purge-expired-trash` (3:17 UTC).
- **Usuwanie jest NATYCHMIASTOWE, a „Cofnij" przywraca z kosza** (`deleteWithUndo` w [trash.ts](src/lib/trash.ts), poprawka 2026-09-15). ⛔ NIE owijaj usuwania w `deferDelete`: odroczenie commitu o 5 s bylo jedna z dwoch przyczyn zgloszenia „usuwam wyjazd, wracam na profil i nadal go widze" - commit szedl z `setTimeout` juz po odmontowaniu ekranu i gdy user w tym czasie wyszedl z apki (albo iOS uspil WebView), zapis nie szedl wcale. Druga przyczyna: kazde miejsce usuwania uniewazniało tylko SWOJE klucze cache (Eksploracja `my-collections`, widok kolekcji `profile-list-feed`, karta w feedzie `explore-rankings`), wiec wystarczylo usunac z innego ekranu niz ten, na ktory sie wracalo. Teraz lista kluczy zyje w JEDNYM miejscu (`invalidateContentLists`, ~25 kluczy) i wola ja warstwa danych, nie ekran - `queryClient` zostal w tym celu wyciagniety z `App.tsx` do [queryClient.ts](src/lib/queryClient.ts).
- **UI:** wejscie w Ustawieniach → [TrashSheet](src/components/profile/TrashSheet.tsx) (okladka, liczba miejsc, „Zostaje N dni", przywroc / usun na zawsze). `TRASH_DAYS = 7` w jednym miejscu; zmiana okna to ta stala + `interval '7 days'` w trzech miejscach migracji.

**WSPOLTWORZENIE KOLEKCJI (2026-09-15, migracja `20260915c_collection_members`).** Kolekcja przestala byc owner-only: wlasciciel moze zaprosic innych do dodawania miejsc. Model byl na to przygotowany od 2026-08-26 (`discovery_items.added_by` = KTO dodal, populowane na wszystkich sciezkach insertu), teraz doszla reszta.
- **Czlonkostwo:** `discovery_collection_members (collection_id, user_id, role='editor', added_by, created_at)`. Dopisywanie CUDZYCH tylko przez SECDEF `add_member_to_collection` (wolajacy musi byc wlascicielem); z klienta zostaje samo odejscie z kolekcji i usuniecie czlonka przez wlasciciela. ⛔ Prywatna „Ogolne" (`list_status='to_visit'`) nie podlega wspoltworzeniu - RPC ja odrzuca.
- **Kto co moze (RLS, sprawdzone na prodzie w `BEGIN … ROLLBACK`):** czlonek CZYTA kolekcje i sklad, DODAJE miejsca, a edytuje i kasuje **wylacznie swoj wklad** (`added_by = auth.uid()`). Nie skasuje cudzego miejsca, nie podmieni cudzej notki, nie zaprosi nikogo. Nazwa, tlo, zasieg i usuniecie CALEJ kolekcji zostaja przy wlascicielu. ⚠️ Testujac RLS NIE bierz konta Nat jako „zwyklego usera" - ma role admin i polityka `discovery_items_admin_all` przepuszcza ja wszedzie (pierwszy przebieg testu wyszedl przez to falszywie alarmujaco).
- **Zaproszenie + PUSH:** `notify_collection_invite` (SECDEF, **dedup DOBOWY** po (osoba, kolekcja) - wieczny blokowal ponowne zaproszenie po wcześniejszym usunieciu wspoltworcy, migracja `20260915f`) wstawia powiadomienie typu **`list_invite`** - nowa wartosc enuma `notification_type`, dodana OSOBNA migracja przed uzyciem. Galaz w `notify_push` (push) i wpis w `NotificationsDrawer` (ikona `Users`, przejscie na `/lista/<id>`). `add_member_to_collection` przy okazji wrzuca kolekcje do `saved_collections` zaproszonego, zeby pojawila sie u niego w „Zapisane" bez szukania. Push dziala od pierwszego dnia i jest potwierdzony na prodzie: wpis w `notifications` o 14:25:51.788 -> `net._http_response` 200 `{"sent":1,"failed":0}` o 14:25:51.867 (regula z memory `feedback_cron_success_is_not_http_success`: prawda o wysylce jest w `net._http_response`, nie w tym, ze RPC nie rzucil bledem).
- ⛔ **Jeden klucz zapytania = jeden ksztalt danych.** `collectionMembersKey` czytaja DWA miejsca (widok kolekcji i arkusz osob). Gdy `SharedList` mial przy tym kluczu wlasne `queryFn` mapujace od razu na tablice id, to kto pierwszy wypelnil cache, ten narzucal ksztalt drugiemu - arkusz dostawal tablice stringow i kazdy wspoltworca wyswietlal sie jako „Użytkownik" bez awatara (zgloszenie Nat 2026-09-15). Przeksztalcenie rob przez `select` w `useQuery`, ktore NIE dotyka cache.
- ⚠️ **DODAWANIE OSÓB MA DWA ETAPY: zaznacz, potem zatwierdź** (prośba Nat 2026-09-17). Do tego dnia każde tapnięcie w wiersz w `CollectionPeopleSheet` od razu wysyłało zaproszenie i powiadomienie - nieodwracalna akcja bez chwili na rozmyślenie, a dodanie trzech osób to były trzy zapytania i trzy toasty jeden po drugim. Teraz wybór siedzi w stanie, a guzik `Dodaj (N)` wysyła JEDNO zapytanie na całą paczkę. Zapraszanie do WYJAZDU (`InviteFriendsSheet`) i kreator działały tak od początku - ten arkusz był jedynym miejscem, które robiło to inaczej. ⚠️ Osoby, które już współtworzą, są w pickerze PRZYGASZONE i nieklikalne (`locked` w `AddPeoplePicker`): odbieranie dostępu ma jedno miejsce - krzyżyk przy liście współtwórców - żeby ten sam wiersz nie znaczył raz „dodaj", a raz „usuń". Zamknięcie arkusza porzuca niezatwierdzony wybór.
- **UI:** w kreatorze wiersz „Dodaj osoby" stoi NA DOLE ostatniego kroku (dokladnie jak przy wyjezdzie) i prowadzi na blizniaczy ekran z `AddPeoplePicker`; zaproszenia wychodza PO utworzeniu kolekcji, bo wczesniej nie ma do czego zapraszac. Po utworzeniu: menu „…" → „Osoby w kolekcji" → [CollectionPeopleSheet](src/components/lists/CollectionPeopleSheet.tsx) (sklad + dodawanie + odbieranie dostepu). Warstwa danych: [collectionInvite.ts](src/lib/collectionInvite.ts).
- **NOTKI PER-UCZESTNIK (2026-09-15, migracja `20260915d_collection_item_notes`):** kazdy uczestnik pisze SWOJA notke przy miejscu i widzi notki wszystkich - lustro `pin_ratings` z wyjazdow. Tabela `discovery_item_notes (collection_id, user_id, place_name, note)`, jeden wiersz na (kolekcja, user, miejsce), indeks unikalny na ZNORMALIZOWANEJ nazwie (`place_note_key`). Czyta kazdy, kto widzi kolekcje (`can_read_collection`: publiczna+zaakceptowana OR wlasciciel OR czlonek); pisze i kasuje **wylacznie swoj wiersz**. UI: moja notka w `PlaceNoteEditor`, cudze pod spodem w `PlaceNotes` (ten sam komponent, co przy miejscu w wyjezdzie). ⛔ **Z UI piszemy TYLKO do `discovery_item_notes`** (`saveCollectionNote`), NIGDY do `discovery_items.short_desc` - to pole trzyma notke WLASCICIELA i aktualizuje je `propagate_place_note` z triggera; dwa punkty zapisu rozjechalyby oba zrodla. `short_desc` zostaje, bo czyta go „Od użytkowników" na wizytowce (`fetchPlaceNotes`) i caly mechanizm synchronizacji notek z 2026-09-13. Backfill: 73 istniejace `short_desc` = notki wlascicieli. Sprawdzone na prodzie w `BEGIN … ROLLBACK`: obaj widza obie notki, wspoltworca nie podmieni i nie skasuje cudzej, jego notka nie rusza `short_desc` wlasciciela.
- **ZDJECIA PER-UCZESTNIK (2026-09-15, migracja `20260915e_collection_item_photos`):** kazde zdjecie przy miejscu ma AUTORA i jego awatar w lewym-dolnym rogu miniatury - widac, kto co wrzucil. Tabela `discovery_item_photos (collection_id, place_name, user_id, url)`, lustro `pin_photos`. Czyta kazdy, kto widzi kolekcje; dodaje wlasciciel i wspoltworca (zawsze jako SIEBIE - RLS odrzuca wstawienie z cudzym `user_id`); **kasuje autor zdjecia ALBO wlasciciel kolekcji** (odpowiada za jej wizerunek). ⚠️ `discovery_items.images[]` ZOSTAJE - czytaja je okladki kafelkow i most do `place_photos` - ale zrodlem prawdy dla WIDOKU kolekcji sa teraz wiersze z autorem; nieudany zapis `images[]` u wspoltworcy jest tylko ostrzezeniem w konsoli, nie bledem. Backfill: 45 zdjec z `images[]` = zdjecia wlascicieli. Notke i zdjecie moze dodac KAZDY uczestnik, takze przy cudzym miejscu - `added_by` rzadzi tylko usuwaniem samej POZYCJI z kolekcji.
- ⛔ **Warunek renderowania arkusza/ukrytego inputu MUSI byc ten sam, co warunek pokazania guzika, ktory go otwiera.** W widoku kolekcji guziki chodzily pod `isOwner || isMember`, a trzy rzeczy, ktore otwieraja, wisialy pod samym `isOwner`: ukryty `<input type="file">` dla zdjecia przy miejscu, `AddPlaceSheet` pod dolnym CTA "Dodaj nowe miejsce" i `PeopleSheet` pod "+N" w belce. Skutek: wspoltworca widzial pomaranczowe kolko aparatu, tapal je i **nie dzialo sie NIC** - `ref.current` byl `null`, wiec `?.click()` byl cichym no-opem (zgloszenie Nat 2026-09-16 "jako uczestnik czyjejs kolekcji nie moge dodawac zdjec do miejsc"). Baza byla caly czas w porzadku: sprawdzone na prodzie w `BEGIN … ROLLBACK`, ze czlonek (NIE admin) wstawia i do `discovery_item_photos`, i do `discovery_items`, a storage `route-images` wpuszcza go po wlasnym folderze `<uid>/`. Ten rozjazd nie wywala ani `tsc`, ani buildu, ani `hooks:check` - objawia sie wylacznie martwym guzikiem u czesci userow. Dlatego warunek zyje w JEDNEJ zmiennej (`canContribute` w [SharedList](src/pages/SharedList.tsx)), a nie jest przepisywany osobno przy guziku i przy arkuszu.
- **ODWIEDZINY SĄ WSPÓLNE (2026-09-16, migracja `20260916f`):** „byłem tu" odhacza KAŻDY uczestnik kolekcji, nie tylko właściciel (zgłoszenie testerki). Żółta pigułka pokazuje do DWÓCH awatarów + „+N" (mój awatar zawsze pierwszy), a etykieta w menu („byłem" / „nie byłem") chodzi po MOIM śladzie - stąd osobny prop `visitedByMe` w [RoutePlaceRow](src/components/route/RoutePlaceRow.tsx): pigułka i przełącznik przestały znaczyć to samo. ⚠️ `place_visits` jest PRYWATNE (RLS: tylko swoje wiersze), więc ślad grupowy idzie wąskim SECDEF RPC `list_collection_visits`: wyłącznie miejsca z TEJ kolekcji i wyłącznie jej uczestnicy. Nie da się tym zapytać „gdzie był user X". ⛔ Po każdym przełączeniu unieważnij `["collection-visits", id]` - bez tego własny awatar nie pojawia się w pigułce do następnego wejścia. `markVisitedAuto` (notka/zdjęcie = byłem tu) ZOSTAJE owner-only - to osobna decyzja, nie przeoczenie.
- **GWIAZDKI W PLANIE TEZ PER UCZESTNIK (2026-09-21, migracja `20260921_pin_stars`):** lustro kolekcji - tabela `pin_stars (route_id, user_id, place_name)`, `pins.is_top` = gwiazdka WLASCICIELA utrzymywana triggerem (`resync_pin_top`), klient pisze WYLACZNIE do tabeli ([tripStars.ts](src/lib/tripStars.ts)). Do 21.09 uczestnik przelaczal `pins.is_top` wprost (RLS `Group members can update pins` to przepuszczala), wiec jego gwiazdka wpadala do licznika „Wyroznione" na profilu AUTORA planu, a na jego wlasnym nie bylo jej wcale (zgloszenie Nat). Licznik profilu ([starredPlaces.ts](src/lib/starredPlaces.ts)) liczy z OBU tabel gwiazdek, takze w cudzych planach/kolekcjach, gdzie user uczestniczy; `place_star_count` na wizytowce przepisany na te tabele (liczy OSOBY, nadal tylko tresc publiczna). Odczyt `pin_stars`: uczestnicy zawsze, reszta tylko plan OPUBLIKOWANY (`is_shared AND status='published'`); pomocnik `is_route_participant(route, user)` ma EXECUTE dla anon (uzyty w polityce). Backfill przypisal dawne `is_top` wlascicielom - pin nie trzymal, kto go postawil.
- **GWIAZDKI PER UCZESTNIK (2026-09-20, migracja `20260920b_collection_item_stars`):** kazdy z kolekcji (wlasciciel i wspoltworcy) wyroznia miejsca SAM - tabela `discovery_item_stars (collection_id, user_id, place_name)`, lustro notek. ⛔ `discovery_items.is_top` znaczy odtad „gwiazdka WLASCICIELA" i utrzymuje je TRIGGER (`resync_item_top`) - klient (takze wlasciciel) pisze WYLACZNIE do tabeli gwiazdek ([collectionStars.ts](src/lib/collectionStars.ts)). Wiersz: gwiazdka przy nazwie = wyroznil KTOKOLWIEK, guzik = MOJ stan (`topByMe` w `RoutePlaceRow`); przy > 1 osobie obok gwiazdki stoi LICZBA, a gdy wyroznili WSZYSCY uczestnicy - sama gwiazdka (prosba Nat). Licznik „Wyroznione" na profilu ([starredPlaces.ts](src/lib/starredPlaces.ts)) liczy z tej tabeli, wiec obejmuje tez gwiazdki w cudzych kolekcjach. Indeks unikalny jest FUNKCYJNY (`place_note_key`), wiec `upsert(onConflict)` nie dziala - insert z ignorowaniem 23505. **Gwiazdka od wspoltworcy = powiadomienie + push dla wlasciciela** (`list_starred`, migracje `20260920c` enum + `20260920d` trigger `notify_list_starred` na tabeli gwiazdek: pomija gwiazdke wlasciciela, dedup 5 min na (osoba, kolekcja); galaz w `notify_push`, wiersz w `NotificationsDrawer`, toast na zywo). Potwierdzone na prodzie: `net._http_response` 200 `sent:4`.
- **ZOLTA PIGULKA „ODWIEDZONE" JEST KLIKALNA (2026-09-20):** [PlaceVisitorsSheet](src/components/lists/PlaceVisitorsSheet.tsx) = kto tu byl (`list_collection_visits`) + kto wyroznil; osoba z sama gwiazdka tez stoi na liscie.
- **OPUSZCZENIE KOLEKCJI (2026-09-20):** przy wspoltworcach KAZDE tapniecie w belke uczestnikow (pigulka autora, druga osoba, „+N") otwiera `PeopleSheet`, a wspoltworca ma tam przy SWOIM wierszu „Opusc" (prop `leave`; jedyny wyjatek od „tylko do odczytu") → potwierdzenie → `removeCollectionMember` (RLS: wlasny wiersz) → `invalidateContentLists` + powrot. Kolekcja jednoosobowa: pigulka autora dalej prowadzi na profil. ⚠️ `PeopleSheet` porownuje `Participant.id`, a wiersz czlonka ma `user_id` - mapuj przy przekazaniu.
- **MIEJSCE DODANE DO KOLEKCJI LADUJE TEZ W „OGOLNE" DODAJACEGO (2026-09-20):** `addPlaceToList` (bez `general`) wola `quickSavePlace` best-effort - arkusz „Zapisz miejsce" robil to od zawsze, teraz takze „Dodaj nowe miejsce" w widoku kolekcji i wspoltworca w cudzej kolekcji.
- **Czego NIE zrobiono:** nic z tej rodziny - notki, zdjecia i gwiazdki sa juz per-uczestnik. Patrz memory `project_list_cocreation_architecture`.

**„Ktoś dodał miejsce do kolekcji" (2026-09-14):** powiadomienie `list_updated` dostają **ZAPISUJĄCY ∪ OBSERWUJĄCY autora** (RPC `notify_collection_updated`, migracja `20260914e`). Wcześniej tylko zapisujący - obserwowanie kogoś nie dawało nic, choć wyjazdy miały to od dawna (`notify_followers_on_new_route`). ⛔ Obserwujący **tylko dla kolekcji PUBLICZNEJ** (i nieukrytej, nieodrzuconej) - bez tego prywatna „Ogólne" rozsyłałaby obserwującym każdy prywatny zapis. Zapisujący bez tego warunku (zapisać można tylko publiczną). `UNION` = kto i obserwuje, i ma zapisane, dostaje JEDNO. Dedup: max 1 na (odbiorca, kolekcja) na 5 minut, więc dosypanie dziesięciu miejsc = jedno powiadomienie. **RPC wołane z `addPlaceToList`** ([placeLists.ts](src/lib/placeLists.ts)), czyli ze wspólnej warstwy danych - wcześniej wisiało tylko w widoku kolekcji, przez co najczęstsza ścieżka (arkusz „Zapisz miejsce" → zaznacz swoją kolekcję) nie powiadamiała NIKOGO. Nie dubluj wywołania w widokach. ⛔ Świadomie NIE ma triggera na `discovery_items` - `CreateRanking` przy edycji kasuje i wstawia wszystkie pozycje od nowa, więc trigger krzyczałby „dodano miejsce" przy samej zmianie kolejności.

**CALY WIERSZ POWIADOMIENIA = JEDEN CEL (2026-09-18).** Do tego dnia awatar w wierszu prowadzil na profil AUTORA, a zdanie obok - do tresci. PostHog pokazal, ze userzy tapaja w awatar z plakietka typu (najbardziej „klikalny" element) i laduja na profilu zamiast w wyjezdzie/kolekcji, a przy `trip_reminder` (bez autora) tapniecie w awatar nie robilo NIC - stad zgloszenie Nat „nie dziala przekierowanie z powiadomien do wyjazdu" (w harnessie WebKit nawigacja z wiersza i z pusha dzialala od poczatku). Teraz awatar, zdanie i miniatura maja ten sam `open`; `follower` / `friend_*` prowadza na profil TEJ osoby. **Push przy ZIMNYM starcie:** cel z `pushNotificationActionPerformed` siedzi w `pendingPushUrl` ([useNativePush](src/hooks/useNativePush.ts)), `RootPage` bierze go zamiast `/eksploruj` (`consumePendingPushUrl`), a `openPushUrl` po 0,6 i 2 s sprawdza, czy adres sie utrzymal - nawigacja z pusha scigala sie ze startowym przekierowaniem. ⛔ Legacy cron `push-scheduler-morning/-evening` WYLACZONY 2026-09-18 (decyzja Nat, migracja `20260918_unschedule_legacy_push_scheduler`): slal pushe z martwymi adresami (`/day/<id>` = NotFound) rownolegle z `enqueue_trip_reminders`, bo migracja 20260830 zakladala podmiane wpisu po nazwie, a nazwy sie roznily. Funkcja `push-scheduler` zostaje jako martwy kod - nie wlaczaj jej z powrotem. Raport dzienny ma tryb `{"test_to": "<adres z ALERT_EMAILS>"}` = wysylka TYLKO na ten adres z prefiksem `[TEST]` w temacie. **Cron raportu: 5:00 UTC = 7:00 czasu polskiego latem** (migracja `20260918b`; zima ten sam wpis da 6:00). W mailu pod „Aktywacja i retencja" stoi jednozdaniowy slowniczek (aktywacja, DAU/WAU/MAU) - founderzy nie znali tych skrotow.

**GUZIK „SCHOWAJ KLAWIATURE" NAD KLAWIATURA (2026-09-18):** [KeyboardDismissButton](src/components/layout/KeyboardDismissButton.tsx), montowany raz w `App.tsx` (tylko natywka). Zolte kolko z brazowa ikona i biala obwodka, `fixed right-3 bottom-3` - przy `Keyboard.resize = 'native'` dol WebView = gorna krawedz klawiatury, wiec NIE dokladaj `keyboardHeight` z eventu (podwoiloby przesuniecie). Pokazuje sie na `keyboardWillShow`, znika na `keyboardWillHide`; tapniecie = `blur()` + `Keyboard.hide()` przez `onPointerDown` z `preventDefault`. Ikona = brandowa `BrandKeyboardHide` (od 2026-09-18 wieczor; wczesniej wlasny inline svg w kresce lucide).

**GALERIA WYJAZDU PO DNIACH - dzien zdjecia liczy sie z PINU (2026-09-18).** Wiersz `pin_photos` NIE MA `day_index`, a filtr dnia wolal `pinDay(wierszZdjecia)` → `Number(undefined) || 1` → kazde zdjecie w dniu 1 (zgloszenie Nat „wszystkie zdjecia dodaja sie do jednego dnia"). Teraz `photoDay()` w [SharedRoute](src/pages/SharedRoute.tsx) szuka pinu po `pinPhotoKey(place_name)`. Sprawdzone na „Trip do Wro": 68 / 13 zdjec = dokladnie jak w bazie. Przy okazji **„Dodaj notkę" zdjete z menu miejsca w WYJEZDZIE** - zostaje tylko „Edytuj notkę" dla miejsca, ktore notke juz ma (edytor pod wierszem ma `hideActions`, wiec bez tego istniejaca tresc nie mialaby wejscia).

**KONTA ZESPOLU POZA ANALITYKA (2026-09-18):** [internalAccounts.ts](src/lib/internalAccounts.ts) (`nat.maz98`, `tomalab97`) - PostHog `opt_out_capturing` zamiast `identify` (useAuth, Auth.tsx, consent), przy starcie sprawdzane SYNCHRONICZNIE z sesji w localStorage (`main.tsx`), bo pierwszy `$pageview` leci przed `getSession`. Raport dzienny (`daily-analytics-digest`) filtruje je dodatkowo: Supabase po id (`INTERNAL_USER_IDS` na sztywno - `auth.admin.listUsers` stronicuje po 1000, a auth.users ma tysiace osieroconych anonimow), PostHog po **`distinct_id NOT IN (...)`** - ⛔ NIE po `person.properties.email` ani `person_id`: zlaczenie z osobami przy 30 dniach przekracza limit czasu HogQL API (15-110 s). ⚠️ Zapytania HogQL w funkcji ida SEKWENCYJNIE z retry - przy siedmiu naraz PostHog odpowiadal „Queries are a little too busy". Raport mowi „wyjazdy" (nie „trasy"), ma sekcje „Kolekcje miejsc" (zdarzenia `collection_created` + `list_place_added` bez `general: true` = zapis do „Ogólne") i proste nazwy zdarzen (`EVENT_PL`, zdarzenia techniczne SDK w `HIDDEN_EVENTS`). Dry-run: `{"dry_run": true}` przez `net.http_post` z sekretem z Vault (klucz service_role w `.env` NIE zgadza sie z env funkcji → 401).

**Przejście z powiadomienia: NAJPIERW nawigacja, zamknięcie arkusza w NASTĘPNYM tiku (2026-09-15).** Zgłoszenie Nat: „klikam «Zobacz» przy wiadomości z czatu i ląduję na zupełnie innym wyjeździe, a cofnięcie wraca na właściwy". Dwa wpisy w historii = dwie nawigacje. Winne `onClose(); navigate(...)` w [NotificationsDrawer](src/components/layout/NotificationsDrawer.tsx): `onClose()` zdejmuje nakładkę arkusza jeszcze w trakcie obsługi tapnięcia, więc dogenerowany `click` trafia w to, co jest POD spodem, a pod spodem stoi feed Eksploracji, czyli kafelki innych wyjazdów. Wszystkie akcje w szufladzie idą teraz przez jeden helper `go(to, opts)`: `navigate` od razu, `onClose` w `setTimeout(…, 0)`. ⛔ Nie wracaj do `onClose(); navigate(...)` w arkuszach nad listą klikalnych kafelków. **Powiadomienie o wiadomości OTWIERA CZAT**, a nie sam wyjazd: guzik nazywa się „Otwórz czat", więc `go()` i toast z [useNotificationsLive](src/hooks/useNotificationsLive.ts) podają `state: { openChat: true }`, a `SharedRoute` seeduje tym `chatOpen`. Flagę zużywamy RAZ i podmieniamy wpis historii (`replace`, `state: null`) - inaczej powrót „wstecz" na ten wyjazd otwierałby czat drugi raz. ⚠️ Arkusz czatu renderuje się dopiero przy `canEdit` (właściciel lub uczestnik), więc otwiera się w chwili, gdy dojedzie zapytanie o członkostwo - to samo `canEdit` gatuje ikonę czatu w stosie akcji.

**UCZESTNICY W GÓRNEJ BELCE - jedna reguła dla wyjazdu i kolekcji (Nat 2026-09-15).** Autor ZAWSZE w całości (awatar + handle) · drugi uczestnik też w całości, ale **tylko jeśli się mieści** · reszta jako **„+N", tapnięcie otwiera arkusz z pełną listą** ([PeopleSheet](src/components/route/PeopleSheet.tsx), tylko do odczytu, tap w osobę = jej profil). Wspólny komponent [ParticipantsRow](src/components/route/ParticipantsRow.tsx); wcześniej każdy widok miał swoją regułę (wyjazd: dwie osoby z nazwą + nachodzący stos awatarów + nieklikalne „+N"; kolekcja: dwie pigułki + nieklikalne „+N") i przy dłuższych nickach belka się rozpychała albo zawijała.
- **„Mieści się" jest MIERZONE**, nie liczone z długości tekstu: rząd nie zawija (`flex-nowrap` + `overflow-hidden`), więc wystarczy `scrollWidth > clientWidth`. Budżet znaków rozjeżdża się przy innym kroju, większej czcionce systemowej i szerszym ekranie.
- ⚠️ **Autor musi być `shrink-0`, dopóki widać drugiego.** Bez tego flex zgniata pigułkę autora, rząd nigdy nie przekracza kontenera, pomiar nie wykrywa przepełnienia i znika nazwa AUTORA zamiast drugiej osoby - dokładnie odwrotnie niż mówi reguła. Dopiero po schowaniu drugiego autor może się skrócić (gdy sam nick nie mieści się w belce).
- Zarządzanie składem kolekcji zostaje tam, gdzie było: menu „…" → „Osoby w kolekcji" (`CollectionPeopleSheet`). „+N" tapie każdy oglądający, więc prowadzi do widoku tylko do odczytu.
- **Współtwórcy widoczni też na KAFELKU kolekcji**: nachodzące awatary tuż przy pigułce autora (bez handle - nie ma miejsca), przy >3 dochodzi „+N". Dane hurtowo przez `fetchCollectionMembersBulk` (jedno zapytanie na feed, nie N).
- ⚠️ **To wymagało MIGRACJI `20260915k`.** Polityka `dcm_read` wpuszczała do `discovery_collection_members` wyłącznie właściciela i członków, więc oglądający dostawał pustą listę bez błędu - i ani belka, ani kafelek nie mogły pokazać współtwórców, niezależnie od UI. Teraz czyta ją każdy, kto może czytać kolekcję (`can_read_collection`). Nic nowego to nie ujawnia: publiczna kolekcja i tak pokazuje notki i zdjęcia współtwórców z imieniem i awatarem. Skład kolekcji PRYWATNEJ (w tym „Ogólne") zostaje niewidoczny.

**Arkusz powiadomień = SKRZYNKA Z SEKCJAMI (kierunek A, wybór Nat 2026-09-16).** [NotificationsDrawer.tsx](src/components/layout/NotificationsDrawer.tsx); makiety trzech kierunków w Figmie: `[NEW] Ekrany` → „Powiadomienia - eksploracja kierunków (2026-09-15)". Trzy reguły, które weszły razem:
- **Trzy sekcje zamiast płaskiego strumienia:** `Wymaga odpowiedzi` (typy z `ACTION_TYPES`: `route_invite`, `group_invite`, `list_invite`, `friend_request`, **tylko nieprzeczytane**) · `Nowe` (reszta nieprzeczytanych) · `Wcześniej`. **Nagłówek sekcji renderuje się WYŁĄCZNIE dla niepustej sekcji** - konto bez zaproszeń ma wyglądać jak zwykła lista, nie jak trzy puste szufladki. Zaproszenie dostaje **szerszą kartę na peachy `#FCEDE3`** (jedyne wypełnione tło w arkuszu) z guzikami, reszta to zwykłe wiersze.
- ⛔ **Otwarcie arkusza NIE oznacza niczego jako przeczytane** (efekt `markReadMutation` na `open` USUNIĘTY 2026-09-16). Sygnał gasi wyłącznie AKCJA: tapnięcie wiersza (`openNotif` → `markRead` + nawigacja), „Nie teraz" na karcie zaproszenia albo **„Oznacz wszystkie"** w nagłówku. Guzik w nagłówku zmienia rolę: są nieprzeczytane → „Oznacz wszystkie", nie ma → „Wyczyść". **Konsekwencja: plakietka na dzwonku zostaje, dopóki user czegoś nie zrobi** - i o to chodzi.
- **Wiersz = awatar z PLAKIETKĄ TYPU w rogu + zdanie + miniatura treści po prawej + kropka nieprzeczytanego po lewej.** ⛔ Kolory plakietek **tylko z marki**: `TONE_CLASS` ma trzy tony - pomarańcz (zaproszenia, reakcje), złoto `#FDCD84` (zmiany w treści), brąz `#5B2C06` (zapisy, czat, moderacja). Do 16.09 chodziło tu **dziewięć** rodzin kolorów (emerald, violet, sky, red, teal, amber...), z których połowa nie występuje nigdzie indziej w aplikacji - nie przywracaj ich, dodając nowy typ. Miniatura: okładka wyjazdu (`routes.list_cover_url ?? cover_url`), **kolor przewodni kolekcji** z `listTheme` (kolekcja nie ma okładki, a kolor to ta sama tożsamość co kafelek w Eksploracji) albo logo lokalu z `metadata.logo_url`; oba źródła dociągane JEDNYM zapytaniem na typ, nie per wiersz. Plakietka `trip_reminder` = **wypełniony brandowy aparat `BrandCamera`** (`public/aparat.svg`, ten sam plik co kółko „dodaj zdjęcie" przy miejscu; prośba Nat 2026-09-18, wcześniej lucide `Camera`). Per-typowe guziki („Zobacz trasę →" itd.) ZDJĘTE - cały wiersz jest klikalny, cel liczy `targetOf()`.
- **ZAPROSZENIE WYMAGA ZGODY - plan I kolekcja (decyzja Nat 2026-09-21, migracja `20260921e`).** Karta `route_invite` / `list_invite` ma guziki **„Dołączam"** (RPC `respond_to_route_invite` / `respond_to_collection_invite`) i **„Nie teraz"** (= odmowa, wiersz zaproszenia znika), a tapnięcie w treść karty otwiera plan/kolekcję do podglądu bez decyzji. Kolekcje: `discovery_collection_members.status` (`pending` | `accepted`); `add_member_to_collection` wstawia PENDING, a do „Zapisane" zaproszonego kolekcja trafia dopiero po akceptacji. `is_collection_member` (bramkuje ZAPIS: miejsca, notki, zdjęcia, gwiazdki) liczy tylko `accepted`; `is_collection_invitee` (accepted lub pending, EXECUTE dla anon) bramkuje ODCZYT - zaproszony ma widzieć, na co się zgadza, także w kolekcji prywatnej. Widok kolekcji pokazuje pendingowi ten sam peachy pasek „Dołączam / Nie teraz" co plan; belka, „wyróżnili wszyscy", kafelki (`fetchCollectionMembersBulk`) i zakładka „Wspólne" liczą wyłącznie `accepted`; w `CollectionPeopleSheet` pending ma podpis „Czeka na potwierdzenie". Plany miały to od 2026-09-08 (`group_session_members.status`); ⚠️ polityki RLS na `pins`/`group_trip_photos` po członkostwie nadal NIE sprawdzają statusu - pending nie ma tego w UI (`isGroupMember` wymaga accepted), ale baza by go przepuściła. Sprawdzone na prodzie w `BEGIN … ROLLBACK` na kontach nie-adminów: pending czyta prywatną kolekcję i pozycje, INSERT pozycji = 42501, po akceptacji przechodzi, `saved_collections` dopiero po akceptacji.

**Zaproszenie do wyjazdu + push (2026-08-21):** zaproszenie do wspólnej trasy tworzy powiadomienie IN-APP typu `route_invite` (RPC `notify_route_invite`, host-only, dedup) - `inviteUsersToRoute` woła RPC zamiast klienckiego push (`sendGroupInvitePush` USUNIĘTY). **Jeden kanał push** = trigger `notify_push` na `notifications` → `net.http_post` (pg_net, schemat `net`, NIE `extensions.net`; `body` = jsonb) → `send-push`. **Uwierzytelnianie: nagłówek `x-trigger-secret` = sekret z Vault (`push_trigger_secret`), który `send-push` akceptuje jako wywołanie wewnętrzne (`isTrigger`).** NIE anon Bearer - `send-push` odrzuca anon (401, hardening [H2]); klucz service_role bywa rotowany. Sekret żyje w Vault (DB) + edge env `PUSH_TRIGGER_SECRET` (oba poza repo). Cały `http_post` + odczyt Vault w `EXCEPTION...NULL` (nigdy nie blokuje insertu notyfikacji). `notify_push` wysyła push dla: `group_invite`, `route_invite`, `friend_request`, `friend_accept`, `route_used`, `route_liked`, `list_liked`, `list_saved` (+ pozniejsze: `list_updated`, `list_invite`, `list_starred`, `trip_reminder`, `trip_message`, `business_thanks`...). Dodając nowy typ powiadomienia z pushem: dopisz gałąź w `notify_push` (migracje `20260821_notif_push_route_invite.sql`, `20260827_notify_push_trigger_secret.sql`). Enum `notification_type` ADD VALUE aplikuj **osobno** przed użyciem (nie w tej samej transakcji).
⛔ **`group_sessions.city` jest TEXT NOT NULL, a nowe wyjazdy NIE MAJĄ miasta (bug 10.09-15.09.2026).** Od 2026-09-10 zasięgiem wyjazdu jest KRAJ i `createWyjazd` zapisuje `routes.city = NULL` (kreator wybiera wyłącznie kraje). `inviteUsersToRoute` wstawiało to NULL wprost do `group_sessions.city`, które od pierwszej migracji (`20260403000001`) jest NOT NULL i nikt tego nie poluzował. Efekt: błąd 23502, `{ ok:false }` i toast „Nie udało się zaprosić. Spróbuj ponownie." - czyli **przez pięć dni nie dało się zaprosić nikogo do żadnego nowo utworzonego wyjazdu** (zgłoszenie testerki 2026-09-15). Dotyczyło TYLKO pierwszego zaproszenia, bo sesja grupowa powstaje raz; wyjazdy z sesją sprzed 10.09 działały, co maskowało błąd. Naprawione po stronie klienta (`city: route.city ?? ""`) - bez migracji, bo kolumna jest szczątkowa: czytało ją stare parowanie grupowe (zdjęte 2026-08-06), a jedyne miejsce, które ją jeszcze pokazuje (`ActiveTripsDashboard`), ma `name` z tytułem i pomija puste `city`. **Kolumna straciła NOT NULL migracją `20260916`**: poprawka po stronie klienta działa dopiero po wgraniu builda, a testerzy na TestFlight zostają na starszej wersji jeszcze długo - logi z 16.09 (11:17-11:19) pokazywały ten sam błąd 23502 dzień po naprawie w repo. Kolumna jest szczątkowa, więc zdjęcie NOT NULL naprawia to dla wszystkich istniejących instalacji od razu. ⛔ **`inviteUsersToRoute` NIE RZUCA przy porażce - oddaje `{ ok: false }`.** Ścieżki tworzenia wyjazdu (`CreateFlowSheet`, `ComposeWyjazd`) miały tam sam `try/catch`, więc nieudane zaproszenia przechodziły BEZ ŚLADU: wyjazd powstawał, zaproszenia nie szły, a user dostawał haptykę sukcesu i zamknięty arkusz. Sprawdzaj `res.ok`, nie licz na wyjątek. ⚠️ Nauka na przyszłość: **błędy z `group_sessions` / `routes` w `inviteUsersToRoute` idą teraz do konsoli** - wcześniej user widział sam toast, a prawdziwa przyczyna nie wychodziła nigdzie. Dokładając pole do tej wstawki sprawdź NOT NULL w migracji, nie w typach TS - `types.ts` jest nieaktualny i `supabase as any` nie ostrzeże.

---

## Architektura — czego NIE ruszać

### Bezpieczeństwo bazy - reguły z audytów (2026-08-21, 2026-09-08, 2026-09-14)

Trzy audyty i te same klasy błędów wracają, więc zasady na stałe:

- ⚠️ **Funkcja użyta W POLITYCE RLS musi być wykonywalna przez KAŻDĄ rolę, która czyta tę tabelę** - inaczej REVOKE z reguły niżej wywala całe zapytanie. Migracja `20260915c` dołożyła polityki SELECT wołające `is_collection_member(...)` i w tym samym pliku odebrała tej funkcji EXECUTE dla `anon`: od tej chwili **anon nie mógł czytać ŻADNEJ kolekcji** (`42501 permission denied for function`), co zabiło stronę linku `/l/<id>` (każdy udostępniony link pokazywał „Treść niedostępna") i talię okładek na ekranie powitalnym, która czyta publiczne kolekcje PRZED zalogowaniem. ⛔ Nie ratuje tego `auth.uid() IS NOT NULL AND ...` przed wywołaniem - Postgres sprawdza prawo do funkcji niezależnie od tego, czy gałąź się wykona, więc wychodzi BŁĄD, nie pusta lista. Naprawione migracją `20260916b`. Dokładając funkcję do polityki sprawdź, kto czyta tabelę: publiczny odczyt = grant też dla `anon`.
- **Każda funkcja SECURITY DEFINER dostaje `EXECUTE` dla `anon`/`authenticated` Z AUTOMATU** (PUBLIC). Pomocnik wyzwalacza, funkcja cronowa, licznik kwoty, wszystko z parametrem `p_user` - to jest wywoływalne z anon key przez `rpc/`, dopóki nie napiszesz `REVOKE EXECUTE ON FUNCTION ... FROM PUBLIC, anon, authenticated`. Tak wyciekły `propagate_place_note(p_user, …)` (notka jako dowolny user) i `enqueue_trip_reminders()` (push do wszystkich) - migracja `20260914c`. Wyzwalacze i pg_cron działają jako `postgres`, więc REVOKE ich nie rusza. Funkcja dla klienta = jawny `GRANT EXECUTE ... TO authenticated` (i `anon` tylko, gdy naprawdę ma działać przed logowaniem) + sprawdzenie `auth.uid()` w środku.
- **Polityka RLS to filtr WIERSZY, nie kolumn.** Kolumny wrażliwe w tabeli z publicznym SELECT (`profiles`, `business_profiles`) chronią **kolumnowe granty**: tabelowy `REVOKE SELECT/UPDATE/INSERT`, potem `GRANT ... (lista kolumn)`. ⛔ Column-level `REVOKE` przy grancie tabelowym NIC nie robi. Konsekwencja: klient NIE może robić `select("*")` na tych tabelach (PostgREST rozwija `*` na wszystkie kolumny → `permission denied`) - własny pełny wiersz czyta przez SECDEF RPC (`get_my_profile`, `business_profile_for_dashboard`). Nowa kolumna w `profiles`/`business_profiles` = dopisz ją do grantów, inaczej klient jej nie zobaczy mimo poprawnego RLS (patrz memory `feedback_profiles_column_grants`).
- **Kolumny, których właściciel nie ma prawa zmienić**, pilnują wyzwalacze BEFORE UPDATE `guard_route_protected_columns` / `guard_collection_protected_columns` (`hidden_by_admin`, `published_at`, liczniki `likes/saves/views`, `user_id`, `created_at`): dla ról `anon`/`authenticated` bez roli admina wartości wracają do OLD. SECDEF (`current_user = postgres`), service_role i admin przechodzą. Nowa kolumna "systemowa" w `routes`/`discovery_collections` = dopisz ją do strażnika.
- **Polityki „obserwujący widzą…" są z czasów, gdy wszystko było publiczne** - dziś wyjazd roboczy jest prywatny do „Zapisz trasę", więc `routes` czyta wyłącznie właściciel, członkowie grupy i **opublikowani** (`is_shared AND status='published'`). Nie dopisuj polityk po `followers` do treści.
- ⛔ **`is_shared` NIE ZNACZY „opublikowany" - nie buduj na nim widoczności publicznej.** Od 2026-08-23 publikacja to `status = 'published'`, ale `inviteUsersToRoute` ustawia `is_shared = true` KAŻDEMU wyjazdowi grupowemu, także roboczemu (kiedyś było to jedyne, co pozwalało zaproszonym go odczytać). Przez to polityka `Public can read shared routes` wystawiała **robocze wyjazdy grupowe całemu światu**: sprawdzone kluczem ANONIMOWYM na prodzie - trzy szkice wracały z `GET /rest/v1/routes` razem z pinami, czyli pełny plan podróży, którego nikt nie opublikował (zgłoszenie Nat 2026-09-16 „na profilach publicznych widzę robocze wyjazdy"). Naprawione migracją `20260916c` (polityka wymaga `status = 'published'`) - zaproszeni nic nie tracą, bo czytają przez polityki po CZŁONKOSTWIE (`Group members can ... group routes`, warunek na `group_session_id`, bez `is_shared`). ⚠️ **Sama RLS to za mało w widokach**: uczestnik wyjazdu ma prawo widzieć szkic, więc wchodząc na profil hosta nadal by go zobaczył - `PublicProfile` musi o szkice NIE PYTAĆ (`.eq("status","published")` w zapytaniu `public-trip-feed`). Reguła ogólna: filtr publiczności stawiaj w OBU miejscach, bo RLS odpowiada na pytanie „czy wolno mi to przeczytać", a nie „czy to należy pokazać w tym widoku".
- `place_photos.photo_url` ma CHECK na nasz storage (`api.spontaway.com` / `*.supabase.co`) - to publiczna galeria i talia na ekranie powitalnym, więc obcy URL (bez SafeSearch) nie może tam wejść.
- **`business_profiles`:** anon widzi tylko kolumny widoku `business_profiles_public` (+`is_draft`, `created_at`); `authenticated` dodatkowo pola robocze (`email`, daty, `moderation_status`) - panel ops moderacji je czyta; `preview_token` i `promo_code` NIKT poza RPC. Panel lokalu (`BusinessDashboard`) ładuje wiersz przez `business_profile_for_dashboard(p_key, p_token)` (właściciel / admin / `?t=<preview_token>`). Hasło demo `trasa2026` w kliencie **już nie otwiera cudzego panelu** (bez wiersza z RPC ekran mówi „nie znaleziono") - podgląd dla lokalu = link z tokenem skopiowany przez admina.
- Test RLS z konta zwykłego usera (nie Nat - Nat ma rolę admin, strażniki ją przepuszczają!): `set_config('request.jwt.claims', '{"sub":"<uuid>","role":"authenticated"}', false); set role authenticated; …; reset role;` przez Management API, albo `scratchpad/asuser.mjs` (GET jako Nat / anon).

### Google Places Proxy (KRYTYCZNE)

Cały pipeline zdjęć i danych miejsc musi przechodzić przez proxy. NIE fetchuj Google API bezpośrednio z klienta.

```
Klient → getPhotoUrl(ref) → /api/place-photo?ref=...&w=... → Google Places Photos API
                                       ↑ 1-rok CDN cache (Vercel Edge)

Klient → supabase.functions.invoke("google-places-proxy", ...) → Google Places API
                                       ↑ server-side, klucz bezpieczny
```

**Pliki proxy (NIE EDYTOWAĆ bez potrzeby):**
- `api/place-photo.ts` — Vercel Edge Function, proxy zdjęć z 1-rok cache
- `api/demo-places.ts` — Google Text Search dla demo, 24h cache
- `supabase/functions/google-places-proxy/` — główny server-side proxy
- `src/lib/placePhotos.ts` — `getPhotoUrl()` helper

**Zasady:**
1. `getPhotoUrl(photoReference)` → zawsze przez `/api/place-photo`
2. Filtr URL w komponentach musi akceptować zarówno `http://` jak i `/api/` prefiksy
3. `GOOGLE_MAPS_API_KEY` = tylko server-side (NIE VITE_ prefix)
4. Gdy `photo_url` w DB jest null → fallback do `/api/demo-places?city=...&category=...`
4a. **Wizytówka z dodawania miejsc (2026-09-13):** `PlaceSwiperDetail` otwarta z [route/AddPlaceSheet](src/components/route/AddPlaceSheet.tsx) i [CreateFlowSheet](src/components/create/CreateFlowSheet.tsx) dostaje `onAdd`/`added` = toggle z wiersza, z którego ją otwarto → rząd **„Zapisz" (szary, brandowa zakładka `SAVE_ICON`) · „Dodaj" (primary, brandowy plus `PLUS_ICON` = `public/Ikona_Plus.svg`, same kreski z `Ikona_Dodaj.svg`) · żółte kółko udostępniania** (krótkie napisy od 2026-09-14 - trzy guziki nie mieściły pełnych zdań); po dodaniu wizytówka się zamyka, „Dodano" = już zaznaczone / już w wyjeździe (guzik nieaktywny, odznaczenie na wierszu). Poza dodawaniem zostaje „Zapisz to miejsce" (z tą samą brandową zakładką) + udostępnianie.
5. `skipGoogleFetch` prop w PlaceSwiperDetail/SwipeCard: używaj `false` dla fullscreen drawer (żeby były recenzje i zdjęcia Google), `true` tylko gdy zależy Ci na szybkości i masz własne zdjęcia
6. **Mapa „Wybierz z mapy" ([PlaceMapPicker](src/components/route/PlaceMapPicker.tsx)) pyta Google WYŁĄCZNIE za jawną intencją (decyzja Nat 2026-09-11):** tapnięcie w etykietę lokalu na podkładzie (`placeId` z Maps JS za darmo → proxy `action: "placeid"`, pola podstawowe, cache 7 dni w `place_details_cache`) albo „+" z wpisaną **nazwą lub adresem** (jedno `textsearch` z `latitude/longitude` = bias 3 km, kandydaci od najbliższych pinezce, mapa jedzie na pierwszy wynik - 2026-09-13). Guzik „Moja lokalizacja" ZAWSZE robi świeży odczyt GPS (`requestLocation(true, { highAccuracy: true })`) - cache z localStorage bywał z innego miasta.
6a. **Wyszukiwarka miejsc w listach/wyjazdach ([route/AddPlaceSheet](src/components/route/AddPlaceSheet.tsx), [usePlaceSearch](src/hooks/usePlaceSearch.ts), 2026-09-13):** MIASTO najpierw (`"<fraza> <miasto>"` + bias na środek: centroida miejsc listy albo geokod miasta), KRAJE tylko gdy brak miasta / kilka krajów / miasto oddało < 2 wyniki (max 2 kraje = 2 płatne zapytania), wyniki „blisko środka" na górze - kolejność, nie odsiew. ⛔ Zasięg krajowy NIE może wypierać miasta („BADI cafe Polska" dawało cukiernie z Kudowy). ⛔ NIE dodawaj rozpoznawania po postoju pinezki / przy zoomie (Nearby przy każdym przybliżeniu) - odrzucone tego samego dnia: koszt przy przeglądaniu mapy i lokale niezwiązane z celem. Miejsce ręczne (bez dopasowania) ma `address: ""`, nigdy `null` (`pins.address` NOT NULL). ⛔ **Wybór wyniku NIE czyści frazy** (2026-09-16): wiersz wyników to ZAZNACZENIE, nie „dodaj i wyjdź". Do tego dnia `pickGoogle` / `pickResult` robiły `setQuery("")` z komentarzem „powrót do siatki", więc po wybraniu JEDNEGO wyniku cała lista znikała i user wracał na początek arkusza - żeby dodać drugie miejsce z tej samej frazy, musiał wpisać ją od nowa (zgłoszenie testerki: „chciałabym kilka od razu wybrać, a nie mogę, bo po wybraniu jednej od razu mnie resetuje"). Dotyczyło OBU ścieżek: dodawania do istniejącej kolekcji ([route/AddPlaceSheet](src/components/route/AddPlaceSheet.tsx)) i kreatora ([CreateFlowSheet](src/components/create/CreateFlowSheet.tsx)). Ponowne tapnięcie ODZNACZA, a na guziku („Dodaj (3)" / „Dalej (3)") stoi licznik, bo przy dłuższej liście zaznaczone wiersze wyjeżdżają poza ekran. Efekt uboczny na plus: user nie przepisuje frazy, więc znika jedno płatne zapytanie do Google na każde kolejne miejsce. Wybór Z MAPY zostaje „dodaj" (`addOnly`) - tapnięcie pinezki to intencja, nie przełącznik.
6b. **⛔ ZWYKLE MIEJSCE = WYLACZNIE ZDJECIA OD USEROW (decyzja Nat 2026-09-15).** `enrichWithBusinessProfile` dla miejsca BEZ profilu biznesu zwraca `photo_url: undefined` i `galleryPhotos: []` - bez zadnego wyjatku. Odrzucone sa wszystkie trzy dotychczasowe zrodla: proxy Google na zywo (`/api/place-photo`, 49 miejsc), pliki z cache (`gpid_`/`hash_`, 4) i okladki wgrane RECZNIE (`/place-photos-cache/manual/`, 54). Okladka moze pochodzic tylko z `place_photos` / `pins.user_photo_urls` (dociaga je `SwipeCard`) albo od samego lokalu; brak -> ikona kategorii na peachy tle. `places.gallery_urls` nie jest juz czytane NIGDZIE. ⚠️ To odsiew PO STRONIE KLIENTA - kolumny w bazie zostaja nietkniete, wiec przywrocenie dowolnego zrodla to jedna linijka w `enrichWithBusinessProfile`.
7. **Biznes z własnymi zdjęciami → blokuj Google Photos (KRYTYCZNE):** Gdy lokal ma choć jedno własne zdjęcie (cover image, cover video, lub galeria `gallery_urls`), NIE pobieraj zdjęć z Google Places. Pole `businessHasOwnPhoto: boolean` w `MockPlace` (ustawiane w `enrichWithBusinessProfile`) jest źródłem prawdy — `PlaceSwiperDetail` respektuje je automatycznie. Nie nadpisuj tego zachowania bez wyraźnego powodu.

### Proporcje zdjęć wizytówek (KRYTYCZNE)

Dwa konteksty, dwie proporcje:

- **Okładka swipe (SwipeCard / cover) = STRICT portret `9:16`** — kontener: `className="relative my-auto mx-auto w-full aspect-[9/16]"` + `style={{ maxWidth: "calc(100% - 2rem)", maxHeight: "min(680px, 100%)" }}`. Aspect-ratio CSS wymusza 9:16 niezależnie od device. `max-h: 100%` to parent-based (NIE `78dvh` viewport-based) - parent w `exploreMode` ma `pb-[calc(3rem+env(safe-area-inset-bottom,0px))]` chronace przed AppLayout fixed BottomNav (~94px). Bez tego card wystawala pod BottomNavem (ucinała się od dołu). Gdy max-height clampuje, browser shrinkuje width proporcjonalnie zachowując 9:16. `mx-auto` centruje horyzontalnie, `my-auto` wertikalnie w spare space. Sam obraz używa `object-cover` żeby kadrować do portretu niezależnie od formatu źródłowego.
- **Wewnątrz wizytówki (PlaceSwiperDetail - WSZYSTKIE zdjęcia) = `4:3` (pozioma).** Bottom-sheet otwierany po kliknięciu karty, drawer `h-[96dvh]` (prawie pełny ekran). Tailwind: `aspect-[4/3]`, `object-cover`. **Reguła sztywna:** Hero + Aktualności (posty) + galeria + każde inne zdjęcie w drawerze wizytówki MUSI mieć `aspect-[4/3]`. NIE używaj `aspect-square` ani innych proporcji dla zdjęć w wizytówce, nawet w grid 2-col. Wyjątek: fullscreen photo viewer (zoom) - tam `object-contain` bez aspect constraint.

**Auto-crop:** Jeżeli lokal wrzuci ze swojego profilu zdjęcie w innej proporcji, **NIE** wyświetlaj pełnego obrazka. Kontener z fixed aspect + `object-cover` na `<img>` automatycznie kadruje/centruje. Nie używaj `object-contain` na cover ani galerii — to psuje układ (czarne paski). `object-contain` dopuszczalny tylko w fullscreen photo viewer.

**Implementacja referencyjna:**
- [src/components/plan-wizard/PlaceSwiper.tsx](src/components/plan-wizard/PlaceSwiper.tsx) - kontener karty z `aspect-[9/16]` + max constraints
- [src/components/plan-wizard/PlaceSwiperDetail.tsx](src/components/plan-wizard/PlaceSwiperDetail.tsx) - SheetContent `h-[96dvh]`, hero `aspect-[4/3]`, Aktualności `aspect-[4/3]`, fullscreen viewer z `object-contain`

**Historia:**
- 2026-05-25: Wewnątrz wizytówki proporcja zmieniona z `16:9` na `4:3` żeby zdjęcia były większe (więcej powierzchni dla biz content), drawer wydłużony 92dvh→96dvh.
- 2026-05-26: Karta swipera przeszła z height-based (`flex: 1 1 0, maxHeight: min(680px, 78dvh)`) na strict `aspect-[9/16]` + max constraints. Wcześniejsze podejście dawało nieprawidłowy aspect na native iOS w standalone WebView (78dvh inne niż na web). Aspect-ratio + maxWidth/maxHeight rozwiązuje problem: browser shrinkuje width gdy height clampuje, utrzymując 9:16. Aktualności posts thumbnails: z `aspect-square` na `aspect-[4/3]` (reguła sztywna: WSZYSTKIE zdjęcia w wizytówce = 4:3).
- 2026-05-27: Karta w exploreMode (HomeSwipe) wystawała pod fixed BottomNavem (~94px) - ucinała się od dołu. Fix: PlaceSwiper root dostaje `pb-[calc(3rem+env(safe-area-inset-bottom,0px))]` w exploreMode (chronie przed BottomNav), card max-h zmieniona z `78dvh` (viewport-based, ignorowala BottomNav) na `min(680px, 100%)` (parent-based, wlicza pb). Plus `mb-4` zmienione na `my-auto` zeby card było wycentrowane wertikalnie w spare space. W PlanWizard mode (route /plan, BEZ BottomNav) `pb` nie jest stosowane.

### Okładki wyjazdów: tylko pionowe (2026-09-13)

Okładka w eksploracji (`routes.list_cover_url`) i okładka członka (`setMyRouteCover`) muszą być **pionowe: 3:4 albo 9:16** (`COVER_MAX_RATIO = 0.8` szer/wys, [coverFormat.ts](src/lib/coverFormat.ts) - `isPortraitCover(url)` mierzy obraz po załadowaniu; brak pomiaru = NIE blokuj). Setter odrzuca panoramę toastem `toast.cover_portrait_only` ([SharedRoute](src/pages/SharedRoute.tsx) `setCoverFromGallery`/`handleSetCover`, [ReviewSummary](src/pages/ReviewSummary.tsx) `setPlanListCover`). Auto-losowanie (`ensureListCover` / `pickPortraitCover`) bierze najpierw pionowe, a gdy nie ma żadnego - dowolne (wyjazd nie może zostać bez miniatury). Stare panoramy w bazie zostają - kafelek w Eksploracji kadruje je do 9:16. Okładka hero (`cover_url`) bez ograniczeń.

**DNI W WYJEŹDZIE - trzy poprawki po testach (2026-09-16).** (1) **Pasek dni jest OD RAZU**, także w pustym wyjeździe: warunek `daysUsable` stracił `pins.length > 0`, przez który chipy pojawiały się dopiero po dodaniu pierwszego miejsca i wszystkie dni wskakiwały naraz. (2) **Liczba dni BEZ DAT** - `routes.day_number` (kolumna była, nieużywana) plus krokomierz w menu „…"; pozycja pokazuje się WYŁĄCZNIE gdy nie ma zakresu dat, bo przy datach liczbę dni wyznacza zakres i dwa źródła prawdy rozjechałyby się przy pierwszej zmianie terminu. Skrócenie przenosi osierocone miejsca na ostatni istniejący dzień - inaczej zostają w bazie z `day_index` poza zakresem i znikają zależnie od tego, czy user tapnął dzień, czy „Wszystkie". (3) **Usunięcie PUSTEGO dnia** - guzik stoi w stanie zero tego dnia (osobna pozycja w menu wymagałaby zgadnięcia, którego dnia dotyczy), miejsca z dni powyżej przesuwają się w dół, a wyjazd skraca się o jeden dzień; przy wyjeździe z datami to także `end_date - 1`, więc potwierdzenie mówi o tym WPROST. **Galeria też filtruje się dniem:** pasek chipów żyje w JEDNEJ zmiennej `dayStrip` i renderuje się w zakładce Miejsca ORAZ Galeria. ⚠️ Dzień zdjęcia bierze się z MIEJSCA, przy którym je wrzucono (`pin_photos` → `pins.day_index`) - nie jest nigdzie zapisywany osobno. Zdjęcia wrzucone do samego wyjazdu (`routes.review_photos`, płaska tablica adresów) dnia NIE MAJĄ i widać je tylko w „Wszystkie"; dlatego guzik dodawania stoi wyłącznie tam, a pusty dzień mówi, że zdjęcia dodaje się przy miejscu. Pełne „przypisz zdjęcie do dnia" wymagałoby schematu (osobna tabela albo tablica z dniem) - świadomie nie zrobione.

**GALERIA WYJAZDU: SERCE I GWIAZDKA TO DWIE ROZNE RZECZY (2026-09-17).**
- **Serce = polubienie, bez limitu.** ⛔ `BrandHeart`, NIE lucide `Heart`, i pomaranczem marki - czerwony `fill-red-500` byl tu jedynym kolorem spoza palety. Serce stoi teraz TEZ NA KAZDYM KAFELKU siatki i jest klikalne; to ZNOSI wczesniejsza regule „siatka zostaje czysta, lajkuje sie w podgladzie", bo najczestszy gest w galerii kosztowal przez nia dwa dodatkowe tapniecia.
- **PODWOJNE TAPNIECIE w podgladzie** polubia zdjecie i wyrzuca duze serce DOKLADNIE W MIEJSCU PALCA (wspolrzedne ze zdarzenia, element `fixed`). ⚠️ Zawsze POLUBIA, nigdy nie cofa (wzor z Instagrama): cofniecie przypadkowym dwuklikiem byloby strata, ktorej user nie zauwazy. Prog: 300 ms i 40 px - bez progu odleglosci dwa tapniecia w PRZECIWNE rogi liczylyby sie jako jedno.
- ⛔ **Podwojnego tapniecia NIE MA na kafelku siatki** i nie jest to ograniczenie Capacitora: kafelek otwiera zdjecie pojedynczym tapnieciem, wiec rozpoznanie dwuklika wymagaloby wstrzymania otwarcia o ~250 ms przy KAZDYM zdjeciu. Stad serce na kafelku zamiast gestu.
- **Gwiazdka = WYROZNIENIE z BUDZETEM 3 NA WYJAZD** (migracja `20260917d`, tabela `photo_stars`). Binarna, jak „topka" przy miejscu. ⛔ Swiadomie NIE skala 1-3 na jednym zdjeciu: gwiazdka dostalaby drugie znaczenie w produkcie, a skala czytalaby sie jak ocena - czyli to, czego produkt unika przy miejscach. Niedobor jest cala mechanika; bez limitu gwiazdka bylaby drugim polubieniem.
- **Wyroznia TYLKO GOSC.** Autor i uczestnik widza sam licznik - ta sama zasada, co przy sercu (`guard_like_not_own_trip`).
- ⚠️ Limit i zakaz stoja w BAZIE (`guard_photo_star`, bledy `star_budget` / `own_trip_star`), nie tylko w UI: user moze miec wyjazd otwarty na dwoch urzadzeniach i wyklikac szesc gwiazdek, zanim ktorykolwiek ekran sie odswiezy. Klient sprawdza budzet drugi raz tylko po to, zeby nie wysylac zapytania skazanego na blad. **Zmieniasz `STAR_BUDGET` - zmien tez `if v_used >= 3` w migracji.**
- ⚠️ `photo_stars.photo_ref` to KLUCZ PLIKU (`photoStorageKey`), a `photo_likes.photo_ref` to caly adres. Rozbieznosc jest SWIADOMA: przy polubieniu duplikat nic nie kosztuje, a przy gwiazdce ten sam plik zapisany na dwoch domenach Storage zjadlby dwie z trzech.
- ⛔ Gwiazdka dla nie-goscia to `span`, nie `disabled` guzik - klik w wylaczony guzik przechodzi na rodzica, a rodzicem w podgladzie jest nakladka zamykajaca zdjecie.


**OKLADKA WYJAZDU = KROK PUBLIKACJI, ZAWSZE (prosba Nat 2026-09-17).** Tapniecie „Opublikuj wyjazd" otwiera [TripCoverSheet](src/components/route/TripCoverSheet.tsx) z CALA galeria i wyborem okladki - takze wtedy, gdy okladka jest juz ustawiona. Wyjazd bez ani jednego zdjecia dostaje tam jeden duzy cel „Dodaj zdjecie na okladke".
- ⛔ **Zniknela stara sciana:** brak `list_cover_url` BLOKOWAL publikacje toastem „wybierz okladke" i odsylal do zakladki Galeria - user dostawal zadanie zamiast kroku w drodze. Nie przywracaj tej bramki.
- Okladke zapisujemy **PRZED** `publishTrip`: gdyby publikacja przeszla, a zapis okladki nie, wyjazd wyladowalby w eksploracji bez miniatury, ktorej wymaga bramka feedu.
- **„Dostosuj okladke" w stosie akcji** pojawia sie DOPIERO dla wyjazdu opublikowanego (wczesniej dublowalaby krok publikacji). Ten sam arkusz w trybie `adjust`, gdzie mozna wgrac zdjecie, ktore **NIE trafia do galerii** - okladka bywa kadrem zrobionym pod kafelek, a nie wspomnieniem. Wlasciciel zmienia okladke eksploracji, uczestnik swoja wlasna; obaj ida przez `setCoverFromGallery`, wiec galezi nie ma po co dublowac.
- ⚠️ Naglowek i guzik arkusza sa PRZYPIETE, przewija sie sama siatka - przy dwudziestu zdjeciach guzik na koncu strony oznaczalby, ze trzeba przewinac cala galerie, zeby opublikowac.
- ⚠️ Okladka musi byc PIONOWA (`isPortraitCover`) - kafelek w eksploracji kadruje do 9:16. Zdjecie wgrane w trybie publikacji zostaje w galerii nawet gdy sie nie nadaje (jako zdjecie wyjazdu jest w porzadku); w trybie `adjust` jest kasowane ze storage, bo mialo byc wylacznie okladka.

**„DODAJ NOWE MIEJSCE" JEST W DOLNYM PASKU, NIE W STOSIE FAB (prosba Nat 2026-09-17).** Guzik secondary (szary fill) obok „Opublikuj wyjazd". W stosie zostaje sam wariant GALERII („dodaj zdjecie"), bo tam „+" nie ma odpowiednika na dolnym pasku. ⚠️ Warunek renderu paska dostal `canEdit && planTab !== "galeria"` - bez tego opublikowany wyjazd (nie ma juz guzika publikacji) zostalby BEZ dodawania miejsc w ogole.

**AUTOR I UCZESTNIK NIE MOGA POLUBIC WLASNEGO WYJAZDU (decyzja Nat 2026-09-17, migracja `20260917c`).** Dla nich serce przy tytule jest LICZNIKIEM z wejsciem do arkusza [TripLikesSheet](src/components/route/TripLikesSheet.tsx) („Kto polubil"), a nie przelacznikiem - odzew na swoj wyjazd widza tam, gdzie gosc go zostawia. Serce pokazuje sie im tylko dla wyjazdu OPUBLIKOWANEGO.
- ⛔ Bramka stoi w BAZIE (trigger `guard_like_not_own_trip`, blad `own_trip_like`), nie tylko w UI - regula z audytow: filtr w obu miejscach. Wyjatkowo trigger, nie polityka: odrzucenie przez polityke wyszloby jako puste 0 wierszy bez powodu.
- ⛔ Do 17.09 warunkiem w UI bylo `!isOwner`, wiec UCZESTNIK wspolnego wyjazdu polubial go normalnie - stad 4 takie polubienia na prodzie. Teraz bramka to `canEdit`.
- ⚠️ 6 historycznych polubien (2 wlascicieli + 4 uczestnikow z 21) ZOSTALO w bazie: padly pod wczesniejsza regula, a ich skasowanie obnizyloby liczniki widoczne dla innych userow. Do sprzatniecia jednym DELETE, gdy Nat zdecyduje.


**SKRACANIE WYJAZDU PYTA, A POJEDYNCZY DZIEN USUWA SIE Z LISTY W ARKUSZU (2026-09-17, dwa pytania Nat).**
- **Nic nie ginie przy skracaniu, ale trzeba to POWIEDZIEC.** Zdjecia (`pin_photos`), notatki (`pin_ratings`) i gwiazdka (`pins.is_top`) wisza na PINIE, wiec skrocenie wyjazdu tylko zmienia mu `day_index` na ostatni istniejacy dzien. WARUNEK: do 17.09 krokomierz zapisywal od razu i user widzial wylacznie to, ze dzien 3 zniknal razem z cala trescia - **brak straty, ktorego nie widac, jest nie do odroznienia od straty**. Teraz `requestDayCount` liczy, ile pinow wypada poza zakres, i przy niezerowej liczbie pyta (`day.shorten_*`). NIE dawaj tu `bg-destructive` - to nie jest akcja niszczaca, guzik jest pomaranczowy, a czerwien klamalaby o skutku.
- **Kazdy dzien da sie usunac osobno, takze z miejscami.** Do 17.09 guzik stal wylacznie w stanie zero dnia, wiec dzien z choc jednym miejscem nie mial ZADNEGO wejscia - a krokomierz tu nie pomaga, bo skraca wyjazd **OD KONCA**: komu wypadl dzien 2 z pieciu, ten skasowalby nim dzien 5. Arkusz liczby dni ma teraz pod krokomierzem **liste dni z liczba miejsc i koszem przy kazdym** (tylko wlasciciel, tylko gdy `dayCount > 1`). Stary guzik w stanie zero dnia zostaje jako skrot.
- **Regula przesuniecia jest JEDNA:** miejsca usuwanego dnia ida na `max(1, D-1)`, a dni powyzej schodza o jeden. Dzien 1 nie ma poprzednika, wiec jego miejsca zostaja na miejscu, a dotychczasowy dzien 2 dosuwa sie do nich. Cele licz od STARYCH numerow, zanim cokolwiek zapiszesz, i wysylaj **jedno zapytanie na dzien docelowy** (`.in(ids)`), nie jedno na pin - dzien z dwudziestoma miejscami to inaczej dwadziescia zapisow i widoczne mruganie listy.
- Copy potwierdzenia mowi o **sasiednim dniu**, nie o numerze: dla dnia 1 „przeniesie sie na dzien 1" byloby nieprawda albo belkotem. Numer pojawia sie tylko przy skracaniu, gdzie cel jest jednoznaczny.
- Kosz w wierszu **zamyka arkusz i otwiera potwierdzenie w tym samym zapisie stanu** - nowa nakladka wchodzi od razu, wiec tapniecie nie ma jak spasc na wyjazd pod spodem (ta sama pulapka, co przy `onClose(); navigate()` w szufladzie powiadomien).

**„Dostosuj ilość dni" jest DOSTĘPNE ZAWSZE (2026-09-16, drugie zgłoszenie testerki).** Krokomierz w menu „…" pokazuje się także przy wyjeździe z datami - wtedy przesuwa `end_date`, a bez dat rusza `day_number`. ⛔ Nigdy oba naraz: to nadal jedno źródło prawdy na wyjazd, tylko inne w zależności od tego, czy termin jest ustalony. W KREATORZE wyjazdu doszedł wiersz **„Ile dni?" Z CHEVRONEM**, prowadzący na własny krok `tripDaysStep` z dużym krokomierzem - widoczny **tylko gdy nie wybrano daty** (przy zakresie liczbę dni wyznacza kalendarz i dwa sterowania obok siebie kazałyby zgadywać, które wygrywa); wartość leci do `createEmptyWyjazd` jako `dayCount`. ⛔ NIE wracaj do krokomierza wciętego inline w kalendarz - był tak zrobiony w pierwszym podejściu („to jedna liczba, po co osobny ekran") i Nat zgłosiła dwa razy, że chodziło o chevron: w tym arkuszu **każde** dodatkowe ustawienie ma taki sam wiersz z chevronem („Dodaj osoby"), więc wyjątek dla dni czytał się jak obcy element, a nie jak skrót.

**Kalendarz ma siatkę LAT i MIESIĘCY** ([FullCalendarPicker](src/components/plan-wizard/FullCalendarPicker.tsx), prośba testerki 2026-09-16 „taką samą tabelkę na miesiące"): tapnięcie nagłówka rozwija obie siatki pod sobą. ⚠️ Tapnięcie ROKU nie zamyka już panelu (zmienia tylko rok) - zamyka dopiero wybór miesiąca, bo to on kończy „skocz do konkretnego miejsca w kalendarzu". Miesiąc w całości przeszły jest wygaszony, gdy kalendarz nie dopuszcza przeszłości.

**LIMIT WSPOLTWORCOW PLANU = 10 osob POZA wlascicielem (decyzja Nat 2026-09-21, migracja `20260921b_trip_member_limit`):** trigger BEFORE INSERT na `group_session_members` (`member_limit`, hint `trip_members`; host nie liczy sie do limitu, liczone sa tez osoby niepotwierdzone), `add_member_to_session` oddaje `{ok:false, reason:'member_limit'}`. Klient: `MAX_TRIP_MEMBERS` w [placeLimits.ts](src/lib/placeLimits.ts) (ten sam `checkPlaceLimit`, toast `common:limits.trip_members`), sprawdzany w `inviteUsersToRoute` (liczy sklad sesji przed wysylka) i przy ZAZNACZANIU w `InviteFriendsSheet` / kreatorze (`togglePerson`). ⛔ `inviteUsersToRoute` przy `member_limit` oddaje `{ ok:false, error:"member_limit" }` i SAM pokazuje toast - callery nie dokladaja „Nie udalo sie zaprosic". Kolekcje limitu wspoltworcow NIE maja.

**LIMITY MIEJSC (decyzja Nat 2026-09-20): wyjazd max 100, kolekcja max 30** - stale w [placeLimits.ts](src/lib/placeLimits.ts), egzekwowane W BAZIE triggerami statement-level z transition table (migracja `20260920_place_limits`, blad `place_limit`, `hint` = rodzaj), wiec wsadowy insert 31 miejsc do pustej kolekcji tez pada w calosci. ⛔ Prywatna „Ogolne" (`to_visit`) BEZ limitu - to wishlista, na prodzie dwie mialy 37 i 41 pozycji. Klient sprawdza PRZED wysylka (`checkPlaceLimit` w `AddPlaceSheet` przez prop `limit`, w kreatorze kolekcji, przy kopiowaniu miejsc do innego wyjazdu; `addPlaceToList` sam oddaje `false` + toast z liczba wolnych miejsc) i mapuje blad bazy na ten sam toast (`isPlaceLimitError`). Zmieniasz liczbe - zmien `v_limit` w migracji. **Długość wyjazdu:** `MAX_TRIP_DAYS = 92` ([tripDays.ts](src/lib/tripDays.ts)) w kreatorze i przy zmianie dat - Nat zdjęła limit 14 dni (2026-09-13), 3 miesiące zostają jako bezpiecznik; kalendarz przy długim limicie nie pokazuje „max. N dni", tylko po przycięciu zakresu wyświetla `calendar.max_range`.

### Supabase

- Klient: `src/integrations/supabase/client.ts`
- Typy: `src/integrations/supabase/types.ts` — regenerowane przez CLI, NIE edytuj ręcznie
- Migracje: `supabase/migrations/` — zawsze twórz nową migrację, NIE edytuj starych
- RLS: każda tabela musi mieć włączone Row Level Security

### Vercel Edge Functions

- Runtime: `export const config = { runtime: "edge" }`
- Lokalizacja: `api/` (root), NIE `src/api/`
- Sekretne zmienne: Vercel Dashboard → Environment Variables (bez VITE_ prefix)

### Logowanie przez Apple - trzy miejsca, ktore trzeba trzymac razem

Sign in with Apple psuje sie samo, bo **sekret klienta wygasa maksymalnie po 6 miesiacach**
(Apple odrzuca dluzszy `exp`). Nie ma po tym zadnego alertu - po prostu przestaje dzialac.

- **Sekret generujesz sam:** `node scripts/apple-client-secret.mjs --p8 <klucz.p8> --key-id <10 zn.> --team-id J33M8H3SGZ --client-id travel.trasa.signin`. Wynik wklejasz w Supabase (Authentication → Providers → Apple → Secret Key). Skrypt wypisuje date waznosci - **wpisz ja w kalendarz**.
- **Trzy identyfikatory, ktore latwo pomylic:** `travel.trasa.app` to bundle id aplikacji, `travel.trasa.signin` to **Services ID** (i to jego uzywa `external_apple_client_id` w Supabase), a `J33M8H3SGZ` to Team ID.
- **Adres powrotu zalezy od custom domain Supabase**, nie od domeny strony: Supabase wysyla do Apple `redirect_uri = https://api.spontaway.com/auth/v1/callback`. Musi byc wpisany w Services ID → Configure → Return URLs.
- **Weryfikacja domeny wymaga dzialajacego `/.well-known/`.** Regula `"/(.*)"` w `vercel.json` przepisuje WSZYSTKO na powloke aplikacji, wiec `spontaway.com/.well-known/cokolwiek` zwraca HTML landingu zamiast pliku. Plik weryfikacyjny Apple kladziemy w `public/.well-known/` - Vite kopiuje kropkowane katalogi, a Vercel serwuje istniejacy plik ZANIM zadzialaja przepisania. Ta sama luka blokuje universal links (`apple-app-site-association`).
- **Diagnoza bez urzadzenia:** `GET <supabase>/auth/v1/authorize?provider=apple&redirect_to=...` i odczytaj naglowek `Location` - widzisz dokladnie `client_id` i `redirect_uri`, ktore leca do Apple. Logi auth (`analytics/endpoints/logs.all`, tabela `auth_logs`) pokazuja, czy callback w ogole wrocil: same wpisy `/authorize` bez `/callback` znacza, ze flow umarl po stronie Apple, nie u nas.

---

### ⛔ ForBusinessPage — ZAMROŻONA, nie ruszać (src/pages/ForBusinessPage.tsx)

**NIE edytuj tego pliku.** Strona `/dla-firm` jest zachowana do późniejszego wykorzystania. Nie przepisuj, nie refaktoruj, nie usuwaj. Nowy one-pager dla firm to osobny plik `src/pages/BusinessLanding.tsx` pod routem `/dla-firm/landing`.

**Sekcja „Prowadzisz lokal?" ma od 2026-09-20 DWA guziki (prośba Nat): primary „Załóż konto" → `/auth?business=true` (landing z rejestracją lokalu) i secondary „Skontaktuj się" (brąz marki) → formularz zapytania. To częściowo cofa decyzję z 15.09 niżej - lokal może założyć konto sam, bez rozmowy; link „Dla firm" w belce i `/dla-firm` nadal odlinkowane.** ⛔ Poprzednia reguła (2026-09-15, częściowo nieaktualna): Landing B2C NIE prowadzi do niczego B2B. Landingu dla
firm jeszcze nie ma, a trakcję zbieramy na samych użytkownikach, więc ze `SpontawayLanding`
zniknął link „Dla firm" w belce oraz guziki „Załóż konto" (`/biznes/start`) i „Dowiedz się
więcej" (`/dla-firm`). Została **jedna ścieżka**: guzik **„Skontaktuj się"** w sekcji
„Prowadzisz lokal?" otwiera formularz zapytania o ofertę - **modal na desktopie, arkusz dołem
na telefonie** (`BusinessInquirySheet` w tym samym pliku, `z-[70]`, bo pasek cookies stoi na
`z-[60]`). Zapytanie idzie przez funkcję brzegową **`business-inquiry`** (walidacja, limit
5/h na IP, mail do założycieli przez Resend z `reply_to` lokalu) do tabeli
**`business_inquiries`** - tabela NIE ma polityki INSERT dla anon, więc nie da się do niej
pisać z klienta. To co innego niż `lead_contacts` (tam lokale, które MY zaczepiamy).
Routy `/dla-firm` i `/biznes/start` żyją dalej - są tylko odlinkowane. Nie przywracaj wejść
B2B na landing, dopóki nie powstanie landing dla firm.

⚠️ **Pole formularza zapytania (`InquiryField`) ma `h-full justify-end`** (2026-09-17). W rzędzie
dwukolumnowym (miasto + osoba do kontaktu) etykiety mają różną liczbę linii i bez tego input
z dłuższą etykietą zjeżdżał niżej niż sąsiad (zgłoszenie Nat). Zmierzone w WebKit na zbudowanym
CSS: „Osoba do kontaktu (opcjonalnie)" potrzebuje **196,1 px, a kolumna ma 200 px**, więc zawija
się u każdego, kto ma choć odrobinę szerszą zastępczą czcionkę (Inter jest wczytywany BEZ wagi
600, więc `font-semibold` bywa syntezowane różnie). ⛔ Nie prostuj tego skracaniem copy ani
mniejszym „(opcjonalnie)" - zapas wychodzi najwyżej 10 px i pierwsze dłuższe tłumaczenie znowu
to złamie. `justify-end` dosuwa etykietę DO SWOJEGO pola, więc wolne miejsce zostaje NAD krótszą
etykietą. ⛔ Kolumny tego rzędu mają **`sm:flex-1`, a NIE `flex-1`**: na telefonie kontener jest
KOLUMNĄ, a w kolumnie `flex: 1 1 0%` steruje wysokością (ta sama pułapka, co przy polu email
w hero B2B).

---

### ⛔ BusinessDashboard — główny dashboard firm ZAMROŻONY (src/pages/BusinessDashboard.tsx)

**NIE wprowadzaj żadnych zmian** w głównym dashboardzie biznesowym (`/biznes/:id`) bez wyraźnej zgody użytkownika. Dotyczy to zarówno layoutu, logiki, jak i stylów. Każda zmiana wymaga explicit "możesz zmienić X w dashboardzie".

**Batch 2026-09-14 (za zgodą Nat, 6 punktów) - to jest teraz stan bazowy:**
- **Kolory:** panel zszedł z niebieskiego na markę (patrz „Identyfikacja B2B" wyżej).
- **Znak:** ekran ładowania (`DashboardLoadingScreen`) i belka boczna pokazują sygnet + **„spontaway"** (było „trasa" / „trasa.biznes"). W `bizdash.json` nie ma już marki „trasa"; dawny rzeczownik „trasa/route" w analityce przeszedł na **„wyjazd/trip"** (zgodnie z nazewnictwem apki).
- **Pola formularzy:** wszystkie inputy przez lokalny **`BizInput`** (owija shadcn `<Input>` klasą `BIZ_FIELD`: szare wypełnienie `bg-slate-50`, delikatny obrys, pomarańczowy focus). Textarea i pola własne mają tę samą klasę wprost. Nowe pole w panelu = `<BizInput>`, NIE `<Input>` - inaczej wyłamie się z jednolitości.
- **Miękki zapis:** lokal NIE klika „Zapisz zmiany" - paska zapisu już nie ma. `persistProfile({ silent })` leci 1,4 s po ostatniej zmianie (debounce na `fieldSig` = sygnaturze wszystkich zapisywanych pól), status „Zapisywanie…/Zapisano" w belce (na mobile pigułka u dołu). Przełączenie zakładki dopina zapis. Błąd zapisu ZAWSZE daje toast i zostawia `isDirty`. Geokodowanie pilnuje `lastGeoAddrRef`, żeby miękki zapis nie wołał płatnego Google w kółko. Szkice nadal mają lżejszy `autoSaveDraft`.
- **Sekcja „Wyróżnione" (2026-09-16, TYLKO wizytówka premium):** jedno zdanie „Wyróżnione przez N osób" z brandową gwiazdką - ile osób dało temu lokalowi gwiazdkę „topki". ⛔ Na wizytówce w STANIE ZERO tej sekcji NIE MA (decyzja Nat) - `PremiumBusinessCard` obsługuje oba przypadki, więc bez propa `premium={isPremiumBusiness}` wyciekłaby na każde miejsce. Zero też się nie renderuje. Liczy SECDEF RPC `place_star_count` (migracja `20260916i`): `pins.is_top` + `discovery_items.is_top`, tożsamość miejsca = `place_note_key`, **liczone są OSOBY, nie wiersze** (kto wyróżnił ten sam lokal w trzech kolekcjach, liczy się raz). ⚠️ Licznik obejmuje WYŁĄCZNIE treść publiczną (wyjazdy opublikowane, kolekcje publiczne), więc bywa MNIEJSZY niż suma gwiazdek, którą właściciel widzi u siebie na profilu - gwiazdka w prywatnym szkicu jest prywatna i sam fakt jej doliczenia zdradzałby, że ktoś ma ten lokal u siebie.
- **Sekcja „Od użytkowników"** (`activeSection === 'community'`): notki (`fetchPlaceNotes` po nazwie lokalu) + zdjęcia userów (`place_photos` po kluczu `nm:<nazwa>`), z autorem i przyciskiem **zgłoszenia do moderacji** (`content_reports`, `target_type` `place_photo`/`place_note` - migracja `20260914d`). Biznes NIE kasuje UGC sam (autentyczność opinii); `place_photos.photo_url` renderuj WPROST, nie przez `getPhotoUrl` (to proxy referencji Google).

**⛔ Personalizacja wizytówki biznesu WYCOFANA (2026-09-14, decyzja Nat):** wizytówka lokalu wygląda **dokładnie tak samo jak każda inna** - wszystkie kolory z marki. Do tego dnia lokal mógł ustawić `color_button` i np. fioletowy guzik „Dodaj" lądował pośrodku pomarańczowej aplikacji. Teraz: picker koloru usunięty z panelu, `PlaceSwiper` **nie czyta** `color_*` z `business_profiles` (przekazuje `undefined`), a podgląd w panelu ma kolory zapisane na sztywno (`#EE5307`). Kolumny `color_badge`/`color_card_bg`/`color_button`/`color_promo` zostają w bazie (historia, zapis ich nie kasuje), ale nic ich nie czyta. Nie przywracaj bez wyraźnej prośby.

**Podgląd wizytówki w panelu = 1:1 z aplikacją (2026-09-14):** mini-karta i widok „karta" w pełnym podglądzie mają **kolumnę akcji po prawej: zapisz (zakładka) + rozwiń (^)** w białych kółkach - tak jak `SwipeCard scrollMode` w zakładce Miejsca. Widok „szczegóły" ma CTA **„Zapisz to miejsce" (brandowa zakładka) + ŻÓŁTE kółko udostępniania z brązową ikoną** - jak `PlaceSwiperDetail`. ⛔ Rząd „Odrzuć / Dodaj" NIE jest domyślnym widokiem (to tryb dodawania miejsca do wyjazdu) - lokal nie widział przez niego guzika zapisu ani udostępniania. Zmieniasz kartę w apce → zmień też podgląd w panelu.

**Język panelu: POLSKI domyślnie (2026-09-14).** Globalny detektor i18n czyta `navigator.language`, więc lokal z angielskim systemem dostawał panel po angielsku. `applyBusinessDefaultLanguage()` ([businessLanguage.ts](src/lib/businessLanguage.ts)) wymusza `pl` przy wejściu do panelu i onboardingu, CHYBA że lokal sam przełączył PL/EN w belce - wtedy jego wybór (klucz `spontaway_biz_lang_choice`) wygrywa na stałe. Nie opieraj się na `i18nextLng` - detektor zapisuje tam też język ZGADNIĘTY.

---

### ⛔ PlaceSwiper — sizing karty 9:16 ZAMROŻONY (src/components/plan-wizard/PlaceSwiper.tsx)

**NIE zmieniaj sizingu, paddingu ani pozycji karty bez wyraźnej prośby użytkownika.** Layout został długo dobierany i działa na wszystkich rozmiarach iPhone'a (SE → 15 Pro Max) zarówno w HomeSwipe (`exploreMode`) jak i solo PlanWizard (`/plan`).

**Co jest zamrożone:**
- Karta: `aspect-[9/16]` strict + height-first sizing (width liczone z dostępnej wysokości, NIE odwrotnie)
- Width formula: `min(420px, calc(100vw - 48px), calc((100dvh - env(top) - [env(bottom)] - 200px) * 9 / 16))`
- Chrome subtraction: exploreMode 200px, solo 242px (solo ma dodatkowy tab bar 42px "Eksploruj | Dopasowania" w PlanWizard step 4)
- exploreMode: bez env(bottom) (BottomNav pb-safe absorbuje). Solo: z env(bottom) (CTA pb-safe-4 dodaje osobno)
- Wrapper: `flex-1 min-h-0 flex items-start justify-center w-full pt-2` (items-start, NIE items-center - karta przylega do gory zamiast byc centrowana)
- Root PlaceSwiper: `flex flex-col flex-1 min-h-0` BEZ explicit pb (chrome subtraction w dvh calc zalatwia bezpieczenstwo)
- NIE uzywac `maxHeight: 100%` lub `maxHeight: 100dvh - X` na karcie (parent-relative % zawodzi w iOS Capacitor WebView z flex-1 ancestrami)

**Historia rozwiazania:**
- 2026-05-28: Reset z parent-relative `maxHeight: 100%` (zawodzilo na iOS) na explicit dvh-based calc + height-first sizing zeby 9:16 ratio bylo strict niezaleznie od ekranu.

---

### ⛔ WaitlistPage — układ i animacja ZAMROŻONE (src/pages/WaitlistPage.tsx)

**NIE zmieniaj układu, z-indeksów ani logiki animacji.** Układ jest zatwierdzony i wymaga długiego debugowania — każda zmiana może go zepsuć.

**Co jest zamrożone:**
- Układ mobile: `"speed dating"` (shrink-0, z-5/60) → orba (w-14, z-50) → telefon (flex-1 min-h-0) → `"z miastem"` (shrink-0 mt-2, z-5/60)
- Outer container: `height: 100dvh` — NIE zmieniać na minHeight
- Sekcja content: `flex-1 min-h-0` — NIE dodawać overflow-hidden ani zmieniać flex
- `FullscreenIntroVideo`: `position: fixed`, `overflow: hidden`, `zIndex: 40` → rośnie do `60` przy shrink
- Animacja przejścia: spring shrink (stiffness 120, damping 20) do rect ekranu telefonu (inset 9px, borderRadius 34px), potem fade 0.25s
- `PhoneMockup` (compact): **width-based** sizing (`width: 60vw, maxWidth: 265px, aspectRatio: 9/19.5`) — NIE używaj height-based dvh (nie działa w Safari flex context)
- `phoneBodyRef` → przekazywany do `FullscreenIntroVideo` i `PhoneMockup ref=` — NIE usuwać
- `shrinking` state → podnosi telefon z z-1 do z-50 przy starcie animacji (żeby bezel był widoczny)
- `onShrinkStart` callback → dwa rAF frames przed startem spring (żeby React zdążył odmalować)

**Co MOŻNA zmieniać:**
- Pliki wideo wewnątrz mockupu telefonu (`src` w `PhaseA`, `PhaseE` itp.)
- Plik intro video (`/founders_intro.mp4` → `src` w `FullscreenIntroVideo`)
- Treść faz (tekst, karty demo w `PhaseB`, `PhaseC`, `PhaseE`)
- Sekcja bottom CTA (email capture, badges, link do `/dla-firm`)
- Desktop layout (`hidden lg:flex` — osobna sekcja, niezależna od mobile)

### DemoSession TopBar - zasady (src/pages/DemoSession.tsx)

**Wszystkie kroki musza miec identyczna wysokosc headera** (padding `pt-safe-4 pb-3`, jeden wiersz tekstu, brak subtitles).

- Swipe header: `text-sm` dla nazwy miasta, brak subtitle/numeracji rundy, brak awatarow, brak ikony wyszukiwania, brak ikony dodania uczestnika
- Badge "dla firm ->" (niebieski, `rounded-full`) renderuje sie TYLKO gdy `isBiznesDemo === true`
- `isBiznesDemo = searchParams.get("biznes") === "1"` - ustawiany z URL param
- Route `/biznes/demo` przekierowuje do `/demo?biznes=1` (w App.tsx)
- Biznes demo: drum scroll z TYLKO Warszawa (odblokowana), reszta miast zablokowana

---

## Gesty natywne (2026-08-28)

Apka ma zachowywać się jak natywna, więc gesty dotykowe są **wspólnymi prymitywami**, nie kopiowanym kodem per ekran. Dwa hooki:

- **[src/hooks/useDragToDismiss.ts](src/hooks/useDragToDismiss.ts)** - „przeciągnij w dół, żeby zamknąć" dla arkuszy/drawerów.
- **[src/hooks/useSwipeNav.ts](src/hooks/useSwipeNav.ts)** - „przeciągnij w bok", żeby zmienić zakładkę / krok / zdjęcie.
- **[src/hooks/useLongPress.ts](src/hooks/useLongPress.ts)** - „przytrzymaj", żeby wejść w tryb zaznaczania.
- **Haptyka** = obiekt `haptics` z [useHaptics.ts](src/hooks/useHaptics.ts). ⛔ Na iOS samo `Haptics.selectionChanged()` jest NO-OPem (plugin tworzy generator dopiero w `selectionStart()`), więc `haptics.selection()` robi pełną sekwencję start → changed → end (do 2026-09-13 ten „tick" nigdzie nie działał). Delikatne potwierdzenie = `haptics.light()`.
- **[src/hooks/useLongPressReorder.tsx](src/hooks/useLongPressReorder.tsx)** - „przytrzymaj i przestaw": zmiana kolejności kafelków palcem (okładki Wspomnień na profilu). Duch = klon DOM w portalu `fixed`, sąsiedzi = FLIP przez Web Animations API, na czas gestu NIEpasywny `touchmove` z `preventDefault` na dokumencie (React-owe `onTouchMove` jest pasywne, `touch-action` nie da się zmienić w trakcie gestu). Zjada `click` po puszczeniu (na kontenerze - po przeciągnięciu klik ląduje na wspólnym przodku) i zatrzymuje `touchend`, żeby `useSwipeNav` na rodzicu nie wziął przeciągnięcia za zmianę zakładki. Obrazki w kafelkach: `draggable={false}` + `-webkit-user-drag:none` (natywny podgląd przeciągania obrazka w WKWebView udawał tę funkcję - zgłoszenie Nat 2026-09-11).

**Arkusze dolne są PŁYWAJĄCE (2026-09-11, wzór FYI):** 8 px od lewej, prawej i dołu, zaokrąglone z każdej strony promieniem **40 px** (w przybliżeniu koncentrycznie z rogiem iPhone'a odsuniętym o 8 pt; 24 px „wchodziło" w róg ekranu) - daje to sam wariant `bottom` w [sheet.tsx](src/components/ui/sheet.tsx) (`inset-x-2 bottom-2` + `rounded-[40px]` dopisane NA KOŃCU className, więc `rounded-t-3xl` z callerów nie ma znaczenia); ręczne panele (`fixed inset-0 flex items-end`) mają `w-[calc(100%-16px)] mx-2 mb-2 rounded-[40px]`. Nowy panel rób tak samo, NIE `inset-x-0 bottom-0 rounded-t-3xl`.

⛔ **Panel wjezdzający od dołu NIE MOŻE mieć własnego `transform`** (zgłoszenie Nat 2026-09-17: arkusz wyjeżdżał z prawej strony zamiast z dołu). `tailwindcss-animate` ustawia na czas animacji `transform: translate3d(var(--tw-enter-translate-x,0), var(--tw-enter-translate-y,0), 0)`, czyli NADPISUJE `-translate-x-1/2` elementu - panel przez całe wejście siedzi o połowę swojej szerokości za daleko w prawo i dopiero na koniec wskakuje na środek. Centruj przez `inset-x-2 mx-auto max-w-lg`, tak jak `SheetContent side="bottom"`, który tego błędu nigdy nie miał. ⚠️ `DialogContent` z shadcn ma `translate-x-[-50%]` świadomie - tam animacją jest `slide-in-from-left-1/2`, która ten przesuw uwzględnia; to nie jest ten sam przypadek.

**Potwierdzenia (`AlertDialog`, np. „Na pewno chcesz usunąć…") wyglądają jak arkusz** (2026-09-13): [alert-dialog.tsx](src/components/ui/alert-dialog.tsx) rysuje pływający panel dołem (`inset-x-2 bottom-2`, promień 40 px, `px-6 pt-8`), tytuł i opis wyśrodkowane, akcja NAD „Anuluj", oba guziki pełnej szerokości `rounded-full h-12` (akcja destrukcyjna = `bg-destructive` z callera, Anuluj = szary secondary). NIE nadpisuj `rounded-*`/`max-w-*` na `AlertDialogContent` w callerach.

**Arkusze na `<SheetContent side="bottom">` mają gest w dół WBUDOWANY** ([src/components/ui/sheet.tsx](src/components/ui/sheet.tsx)) - nie dodawaj tam nic ręcznie. Zamknięcie idzie przez ukryty `SheetPrimitive.Close`, więc działa z każdym `open/onOpenChange`. Wyjątek awaryjny: prop `disableDragToDismiss`. Panel składany ręcznie (fixed overlay + `rounded-t-3xl`) podpinasz sam: `const { dragProps } = useDragToDismiss({ onDismiss })` i `<div {...dragProps} style={{ ...dragProps.style, maxHeight: "82dvh" }}>`.

**Nowy bottom sheet buduj na `Sheet`, nie na własnym `fixed inset-0`** - dostajesz animację wejścia/wyjścia i gest za darmo (tak przepisany został `NotificationsDrawer`).

**Zasady kolizji (ważne):**
- Gest w dół startuje tylko gdy najbliższy scrollowany rodzic jest na górze (`scrollTop <= 0`); ruch w bok albo w górę oddaje gest treści.
- Gest w bok wymaga przewagi poziomu nad pionem i odpada w kontenerze scrollowanym poziomo (pigułki, karuzele).
- **`data-no-drag`** wyłącza zamykanie przeciągnięciem w swoim poddrzewie, **`data-no-swipe`** wyłącza zmianę zakładki. Mapy ([RouteMap](src/components/RouteMap.tsx), mapa w `RouteMapSheet`) mają oba - pan po mapie nie może zamykać arkusza ani przeskakiwać zakładek. Dodawaj je do każdego nowego elementu z własnym gestem (mapa, slider, canvas).
- Podczas gestu i domykania hook wyłącza animacje CSS (`animation: none`) - inaczej `animate-in/animate-out` nadpisuje transform i panel „nie klei się" do palca.

**TAPNIĘCIE W GÓRNĄ BELKĘ = POWRÓT NA GÓRĘ** ([scrollTop.ts](src/lib/scrollTop.ts), prośba Nat 2026-09-16 - odruch z natywnego iOS). Działa w: `TabTopBar` (Eksploracja, Miejsca), `TabHeader` (własny profil), belce profilu publicznego oraz żółtej/kolorowej belce wyjazdu i kolekcji.

⛔ **Bez kontekstu i bez rejestru scrollerów.** Ekran OZNACZA swój główny scroller atrybutem `data-scroll-main`, a belka pyta o niego DOM (`scrollTopTapProps()`). Nowy ekran włącza to JEDNYM atrybutem - bez podpinania się pod providera i bez ryzyka, że ref zostanie po odmontowaniu. `PullToRefresh` ma ten atrybut wbudowany, więc każdy ekran na nim zbudowany dostaje zachowanie za darmo.

Trzy rzeczy, które ten helper załatwia i których nie wolno zgubić przy refaktorze:
- **Guziki w belce działają normalnie** - `closest(INTERACTIVE)` odpuszcza kliknięcia w `button/a/input/label/[role=button]`, także gdy tapnięto ikonę WEWNĄTRZ guzika. Bez tego dzwonek i „wstecz" miałyby efekt uboczny.
- **Widoczność sprawdza `getClientRects()`, NIE `offsetParent`** - ten drugi zwraca `null` również dla `position: fixed` i odrzuciłby scroller, który jest na ekranie. Jeden ekran miewa kilka scrollerów w DOM (profil: feed ORAZ wyniki szukania); nieaktywny jest `display:none` i odpada.
- **Brak haptyki, gdy nie było czego przewijać** (`scrollMainToTop()` oddaje `false`) - inaczej tapnięcie w belkę na górze listy udawałoby akcję.

Sprawdzone renderem w WebKit: wybiera widoczny scroller, klik w ikonę w guziku nie przewija, klik w puste miejsce belki przewija do zera, klik przy już przewiniętym na górę nic nie robi.

⚠️ **W Eksploracji i w Miejscach belka jest w CAŁOŚCI wypełniona polem wyszukiwania i dzwonkiem** - `scrollTopTapProps` nie ma tam czego złapać. Dlatego [AppLayout](src/components/layout/AppLayout.tsx) renderuje dodatkowo **pas nad belką (obszar paska statusu)** jako cel tapnięcia: dokładnie to miejsce, w które stuka się w natywnym iOS. ⛔ Tylko na trasach `hideTopBar` (Eksploracja, Miejsca, Profil, `/home`) - tam `pt-safe` gwarantuje, że pod insetem nic nie ma; na pozostałych ekranach treść sięga samej góry (pasek „masz zaproszenie" nad belką wyjazdu) i pas przechwytywałby jej guziki. `z-20`: wyżej niż tło, ale dużo niżej niż toasty (są u góry ekranu) i arkusze. Na webie `env(safe-area-inset-top)` = 0, więc element ma zerową wysokość i nie istnieje dla palca. **Zakładka Miejsca ma scroller** - w trybie eksploracji `PlaceSwiper` to pionowa lista kart ze snapem, więc powrót „na górę" wraca na pierwszą wizytówkę.

**Cofanie gestem od krawędzi** ([useEdgeSwipeBack](src/hooks/useEdgeSwipeBack.ts), montowany raz w `App.tsx`): przeciągnięcie od **lewej krawędzi** (strefa 24px) w prawo = `navigate(-1)`, jak w natywnym iOS - WKWebView nie daje tego dla tras SPA. Gest jest ignorowany, gdy otwarty jest modal (tam zamyka się gestem w dół), gdy start jest dalej niż 24px od krawędzi (żeby nie gryzł się z gestami treści) oraz gdy nie ma historii w aplikacji (`history.state.idx === 0`) - wtedy NIC się nie dzieje, bez skoku na ekran zapasowy.

**Gdzie gesty już są:** zakładki profilu (Listy ↔ Wyjazdy, własny i publiczny), zakładki wyjazdu (Miejsca/Galeria/Mapa w `SharedRoute` i `ReviewSummary`), zakładki listy (Miejsca/Galeria), galeria fullscreen wyjazdu, hero wizytówki i karuzela wydarzeń, wszystkie bottom sheety + ręczne drawery.

---

## Dual-platform conventions (iOS native vs Web/PWA)

Trasa działa równolegle jako natywna aplikacja iOS (Capacitor 8, WebView) i web/PWA (Vercel). Jeden codebase, dwa cele wdrożenia. Niektóre zachowania powinny się różnić - poniżej obowiązujący wzorzec.

### Detection — zawsze przez `src/lib/platform.ts`

```ts
import { isNative, isWeb, platform, capabilities } from "@/lib/platform";
```

- `isNative` — `true` dla iOS/Android Capacitor WebView
- `isWeb` — `true` dla zwykłej przeglądarki (web + PWA)
- `platform` — `"ios" | "android" | "web"` (raw)
- `capabilities.*` — jawne flagi: `webShare`, `nativeShare`, `haptics`, `pushNotifications`, `serviceWorker`, `installablePWA`, `vercelAnalytics`

**NIE używaj `Capacitor.isNativePlatform()` bezpośrednio w kodzie.** Jedyne miejsce z tym importem to `platform.ts`. Wszędzie indziej importuj nazwane flagi.

### Inline branching — preferowany wzorzec

Małe różnice UI/UX trzymamy `{isNative ? A : B}` w komponencie, **w jednym pliku**. Bez konwencji `.native.tsx` / `.web.tsx`.

```tsx
{isNative ? "Wróć" : "← Wróć do strony głównej"}
```

### Native APIs przez capability hooki w `src/hooks/`

Każde wywołanie natywnego API ma swój hook który decyduje co użyć:

- [src/hooks/useShare.ts](src/hooks/useShare.ts) — Capacitor Share na native, `navigator.share` lub clipboard na web
- [src/hooks/useHaptics.ts](src/hooks/useHaptics.ts) — Capacitor Haptics na native, no-op na web

Wywołanie z komponentu nie wie nic o platformie:

```tsx
const share = useShare();
const result = await share({ title, url });
// result.method: "native" | "webshare" | "clipboard"
```

Kolejne native features (push, camera, biometric) dodajemy w tym samym wzorcu: nowy hook w `src/hooks/use*.ts` z fallbackiem.

### Native-only / Web-only kod

```ts
if (isNative) { /* ten kod wykonuje się tylko w iOS/Android */ }
if (isWeb) { /* ten kod wykonuje się tylko w przeglądarce */ }
```

Vite tree-shaking nie eliminuje tych branchy statycznie (`isNative` to runtime stała), ale runtime guard wystarcza i jest jasny dla developera.

### ⚠️ Natywka startuje z `app.html`, NIE z `index.html`

Build **celowo zamienia pliki miejscami** (`scripts/inject-landing-snapshot.mjs`): landing
z wklejoną treścią ląduje w `dist/index.html` (Vercel serwuje z niego `/`), a powłoka
aplikacji przenosi się do `dist/app.html`. Capacitor o tym nie wie - WebView zawsze otwiera
`index.html`, więc natywka dostawała **landing z gotową treścią** i pokazywała go przez
ułamek sekundy, zanim React się zamontował (zgłoszenie Nat 2026-09-08).

`npm run native:shell` (`scripts/native-shell.mjs`) podmienia `index.html` w projektach
natywnych na powłokę. Leci **PO `cap sync`**, bo sync nadpisuje `public/` świeżym `dist/`.
Jest już wpięty w `check:both` i `check:native`.

⛔ **Hook NIGDY pod `return`.** `npm run hooks:check` (w `check:both`) sprawdza teraz DWA ksztalty tego bledu: hook poza komponentem / w callbacku (od 2026-09-07) **oraz hook PONIZEJ early-returnu w ciele komponentu** (od 2026-09-15). Ten drugi wywalil widok kolekcji zaraz po jej utworzeniu („Coś się zacięło", zgloszenie Nat) - skladnia jest poprawna, wiec ani `tsc`, ani build tego nie widza, a React przy pierwszym renderze liczy mniej hookow niz przy drugim. Rozszerzona bramka od razu znalazla drugie takie miejsce w `TravelerProfile`. Nowy `useQuery`/`useEffect` w pliku z `if (isLoading) return …` wstawiaj NAD tym returnem.

⛔ **Po ręcznym `npx cap sync ios` ZAWSZE odpal `npm run native:shell`** - inaczej landing
wraca do natywki po cichu. Skrypt sam sprawdza, czy `app.html` ma pusty `<div id="root">`
i przerywa, gdyby build przestał ją opróżniać.

### Workflow przed git push

1. `npm run check:both` — buduje dist + robi `cap sync ios`. Musi przejść bez błędów.
2. Sprawdź w przeglądarce na `localhost:8080` (jeśli ruszałaś UI)
3. Cmd+R w Xcode w simulatorze (jeśli ruszałaś UI)

Jeśli zmiana dotyczy obu platform, przetestuj na obu **zanim** wypchniesz.

### Anti-patterns (NIE rób)

- ❌ Osobne pliki `.native.tsx` / `.web.tsx` (zdecydowaliśmy: inline branching)
- ❌ User-agent sniffing (`navigator.userAgent.match(...)`)
- ❌ Sprawdzanie `window.cordova` lub innych proxy hacków
- ❌ Hardcoded `if (window.location.hostname === ...)` w logice biznesowej
- ❌ Osobne build flagi per platforma (`--mode ios`) — mamy jeden build, jeden `dist/`
- ❌ Duplikowanie komponentów żeby zrobić "wersję na iOS" — zawsze inline if-em

---

## Znane problemy do naprawy

- [ ] `photo_url` w tabeli `places` jest null dla większości wpisów → potrzebne ręczne uzupełnienie lub skrypt migracyjny
- [x] Google Photos nie działa w `DemoSession` na etapie kart swipe — naprawione: `skipGoogleFetch=false` w DemoSwiper
- [x] Google Photos nie działa przy tworzeniu trasy — `skipGoogleFetch` domyślnie `false` w SwipeCard/PlaceSwiper
- [x] `PlaceDetailSheet` — sprawdzone, używa `getPhotoUrl()` poprawnie przez proxy

---

## Legacy / Do usunięcia

Poniższe elementy wyglądają na pozostałości po poprzednich pivotach:

**Strony:**
- `src/pages/Onboarding.tsx` — przekierowany do `/`, można usunąć
- `src/pages/SwipeHistory.tsx` — śledzi stare reakcje "swipe" z poprzedniego flow
- `src/pages/CreateRoute.tsx` — zastąpiony przez PlanWizard

**Komponenty:**
- `src/components/discover/` — stary flow odkrywania (SwipeCard, SwipeDiscovery)
  - ⚠️ Uwaga: `SwipeCard.tsx` w `discover/` vs `plan-wizard/PlaceSwiper.tsx` — sprawdź co jest aktualnie używane

**Zależności NPM (nieużywane):**
- ~~`qrcode.react`~~ — UŻYWANE od 2026-09-10 (kod QR do TestFlight w [ReferralCard](src/components/profile/ReferralCard.tsx)), NIE usuwać
- `canvas-confetti` — 0 użyć w kodzie
- `recharts` (poza ikonką z lucide) — komponent chart.tsx istnieje ale nikt go nie importuje
- `embla-carousel-react` — carousel.tsx istnieje ale nie jest używany na stronach
- `react-resizable-panels` — resizable.tsx istnieje ale nie jest używany

---

## Konwencje kodowania

- **Język UI:** Polski (komunikaty, etykiety, placeholdery)
- **Język kodu:** Angielski (zmienne, funkcje, komentarze)
- **Styl komponentów:** Tailwind CSS, bez CSS Modules ani styled-components
- **Ikony:** Lucide React (`lucide-react`) - bez innych bibliotek ikon. **Brandowe SVG** (`public/Ikona_*.svg`, rysunki Nat) rysuj przez [BrandIcon](src/components/BrandIcon.tsx) (CSS mask + `currentColor`, kolor z klasy tekstu jak w lucide). Zestaw (2026-09-11): `Ikona_Home`, `Ikona_Eksploracja`, `Ikona_Miejsca`, `Ikona_Profil` (dolny pasek), `Ikona_Trasy` (znak wyjazdu/placeholder; w warstwach ANIMOWANYCH rysuj go przez [BrandTripMark](src/components/BrandTripMark.tsx) - inline svg zamiast maski), `Ikona_Zapisane`, `Ikona_Gwiazdka` (**gwiazdka „topki"** przy miejscu, w menu i w animacji - NIE lucide `Star`), `Ikona_Listy_Listy` (**wypełniona ikona list** = `LIST_ICON`: zakładka Listy na profilu, kafelki list w eksploracji, kategoria „Listy" w wyszukiwarce - NIE lucide `FileText`/`LayoutGrid`), `Ikona__*` (kategorie miejsc). **Ikony AKCJI od Nat (2026-09-18): `Ikona_Mapa`, `Ikona_Galeria`, `Ikona_Udostepnij`, `Ikona_Czat`, `Ikona_Kalendarz`, `Ikona_Blokuj`, `Ikona_Flaga`, `Ikona_Kosz`** **+ druga paczka: `Ikona_Lupka`, `Ikona_Ustawienia`, `Ikona_Tarcza`, `Ikona_Dzwonek`, `Ikona_Klodka`; + trzecia: `Ikona_Check`, `Ikona_Globus`, `Ikona_Info`, `Ikona_Olowek`; + czwarta: `Ikona_Pomoc`, `Ikona_Notka`, `Ikona_DodajUzytkownika`; + piata: `Ikona_SchowajKlawiature` (guzik nad klawiatura), `Ikona_Paleta` („Tlo" w menu kolekcji), `Ikona_Uzytkownicy` (TYLKO „Osoby w kolekcji" w menu kolekcji - lucide `Users` w innych miejscach zostaje, decyzja Nat)** - w kodzie jako `BrandMap` / `BrandGallery` / `BrandShare` / `BrandChat` / `BrandCalendar` / `BrandBlock` / `BrandFlag` / `BrandTrash` / `BrandSearch` / `BrandSettings` / `BrandShield` / `BrandBell` / `BrandLock` / `BrandCheck` / `BrandGlobe` / `BrandInfo` / `BrandPencil` / `BrandHelp` / `BrandNote` / `BrandUserPlus` / `BrandPin` / `BrandKeyboardHide` / `BrandPalette` / `BrandUsers` z [BrandIcon.tsx](src/components/BrandIcon.tsx) (ten sam interfejs co lucide: `className` = rozmiar i kolor). ⛔ NIE uzywaj juz lucide `Map`, `Image`, `Share2`, `MessageCircle`, `Calendar*`, `UserX`/`Ban`, `Flag`, `Trash2`, `Search`, `Settings`, `Shield`, `Bell`, `Lock`, `Check`, `Globe`/`Globe2`, `Info`, `Pencil`, `LifeBuoy`, `UserPlus`, `MapPin` w apce B2C - podmienione w ~110 plikach (takze prymitywy shadcn `ui/checkbox`, `ui/select`, `ui/dropdown-menu`: ptaszek w checkboxie to `BrandCheck`; `CheckCheck` w pigulce odwiedzone ZOSTAJE lucide - podwojny ptaszek nie ma brandowego odpowiednika) (`swap_icons.py` w scratchpadzie sesji: zamienia tylko znaczniki JSX i `icon:`/`Icon:`, nie gole identyfikatory, bo `Map`/`Image` to tez globale JS); lucide zostaje w zamrozonym panelu lokalu, panelu ops i w `shareTargets` (tam `MessageCircle` oznacza aplikacje Wiadomosci, nie czat). **`BrandPin` = `Ikona_Miejsca.svg`, ten sam pin co zakladka „Miejsca" w dolnym pasku** - od 2026-09-18 zastepuje lucide `MapPin` w calej apce (chipy liczby miejsc, wiersze adresow, kafelki „Utworz wyjazd" pod „+", puste stany); Nat: „podmien wszedzie ikonke pina". `Ikona_Dzwonek.svg` podmieniony tego samego dnia na nowszy rysunek (bez `<g opacity>` z oryginalu - krycie ma isc z klasy, nie z pliku). **`BrandNote` to ikona POJECIA notki, nie akcji edycji**: stoi przy wpisach „Dodaj / Edytuj notke" w menu miejsca (wyjazd i kolekcja), przy „Dodaj / Edytuj opis" wyjazdu i na guziku edytora notki; `BrandPencil` zostaje tam, gdzie chodzi o zmiane nazwy / edycje planu. Pliki SVG sa bez metadanych C2PA (oryginaly od Nat mialy 90 % objetosci w manifescie).
- **Kategoria miejsca z Google = typ KONKRETNY przed OGOLNYM (2026-09-20).** Legacy Places API oddaje `types` ALFABETYCZNIE, nie „glowny pierwszy": sklep z butami Aplug Lodz przychodzil jako `['establishment','food','point_of_interest','shoe_store','store']`, a `categoryFromGoogleTypes` ([placeCategoryIcon.ts](src/lib/placeCategoryIcon.ts)) bral pierwsze trafienie w mapie, czyli `food` → „Restauracja" (zgloszenie Nat). Teraz typy z `GENERIC_GOOGLE_TYPES` (`food`, `point_of_interest`, `establishment`, `store`, `tourist_attraction`) licza sie dopiero, gdy zaden konkretny typ nie pasuje. ⚠️ Bledna kategoria PROPAGUJE sie do katalogu: `ensure_place_from_photo` (migracja `20260906`) zaklada wiersz `places` z kategoria skopiowana z `pins`/`discovery_items` po NAZWIE, wiec jeden zly pin = zla wizytowka dla wszystkich. Stare wiersze NIE sa przeliczone (wymagaloby to platnych zapytan do Google) - poprawka reczna w `places.category` + `pins.category`.
- **⛔ ZAKAZ długich myślników (—, em dash):** Nigdy nie używaj znaku `—` w żadnym tekście UI (banery, hinty, placeholdery, etykiety, komunikaty). Zamiast tego używaj zwykłego myślnika `-`, dwukropka `:`, przecinka lub przeformułuj zdanie.
- **⛔ ZAKAZ EMOJI w UI:** Nigdy nie dodawaj emoji (🍽️ ☕ 🌳 🏰 📍 itp.) do żadnego elementu interfejsu: placeholderów zdjęć/miniaturek, badge'ów kategorii, kart, list, etykiet, komunikatów, pustych stanów. Zamiast emoji kategorii używaj **ikon SVG** (`public/Ikona__*.svg` przez helper `categoryIconSrc()` z `src/lib/placeCategoryIcon.ts`) na peachy tle `#fcede3` (fallback zdjęcia miejsca = `PlacePhoto`). Dotyczy to nowych i istniejących widoków. Wyjątek: emoji w flagach/oznaczeniach czysto tekstowych bez alternatywy (np. 🇵🇱 w tej dokumentacji) - ale w UI aplikacji NIE.
- **🇵🇱 Polskie sieroty (typography rule, OBOWIĄZKOWE):** Pojedyncze litery `a, i, o, u, w, z` (oraz krótkie `do, na, po, za, ze, od` jeśli to możliwe) NIGDY nie mogą kończyć linii. Po nich zawsze musi być **twarda spacja** (non-breaking space, ` ` / NBSP / U+00A0), żeby przeniosły się do następnej linii razem z kolejnym słowem.
  - **Dotyczy:** wszystkie teksty UI w aplikacji i na stronach marketingowych (nagłówki, opisy, etykiety, placeholdery, treści maili, modale, toasty).
  - **W JSX:** używaj template literal z escape, np. `` {`treść z przyjaciółmi i grupą`} ``, albo inline `{" "}` w miejscach z elementami HTML w środku.
  - **Jak rozpoznać:** czytaj zdanie i znajduj pojedyncze litery przed spacją - tam wstaw NBSP. Przykład: `"z miastem"` → `"z miastem"`, `"i wam"` → `"i wam"`.
- **🌍 DWA JĘZYKI OD RAZU (2026-09-06, OBOWIĄZKOWE):** Idziemy z apką globalnie, więc **każdy nowy widok powstaje po polsku I po angielsku w tym samym commicie**. Napisy żyją w `src/locales/pl/*.json` + `src/locales/en/*.json` i lecą przez `t()`. ⛔ **ZAKAZ** polskiego tekstu wprost w JSX oraz zapasowego polskiego w wywołaniu (`t("klucz", "Polski")` / `defaultValue: "Polski"`) - to działa po polsku, a po angielsku pokazuje polski, bo `fallbackLng` to `pl`. Bramka: `npm run i18n:check` (wpięta w `check:both`) pilnuje parzystości kluczy, form liczby mnogiej i braku polskiego na sztywno. Pliki z długiem sprzed bramki są w `scripts/i18n-baseline.json` - **ta lista może tylko maleć**, dopisanie nowego pliku to błąd.
  - **Liczba mnoga:** polski ma `_one` / `_few` / `_many`, angielski `_one` / `_other`. Nie kopiuj polskich sufiksów do `en/*.json` - `_few` i `_many` nigdy się po angielsku nie dopasują.
  - **Twarde spacje (sieroty) to reguła POLSKA** - tekstów EN nie NBSP-ujemy.
  - **Treści użytkowników** (nazwy tras, notki, tytuły list) NIE są tłumaczone - to co człowiek napisał, zostaje jak napisał. **Wyjątek NA ŻĄDANIE (decyzja Nat 2026-09-18):** notki przy miejscach (cudze, w wyjeździe i kolekcji), „Od użytkowników" na wizytówce, opis wyjazdu i opis kolekcji renderuje [TranslatableText](src/components/TranslatableText.tsx) - guzik „Przetłumacz" pojawia się TYLKO, gdy zgadnięty język treści (`guessLang` w [translate.ts](src/lib/translate.ts), bez sieci) różni się od języka apki; po tapnięciu tłumaczenie staje W MIEJSCU oryginału z wierszem „Przetłumaczono · Pokaż oryginał". Silnik: funkcja brzegowa `translate-text` = **Claude Haiku 4.5 BEZPOŚREDNIO przez API Anthropica** (sekret `ANTHROPIC_API_KEY`), ⛔ NIE przez bramkę Lovable, przez którą idą pozostałe funkcje AI - Nat świadomie: słownictwo apki + własny klucz. Cache w tabeli `translations` (sha256 treści + język; trafienie nie zużywa limitu), **limity w `claim_translation_quota`** (migracja `20260918c`): 30/h i 150/dobę na usera, 20 000 znaków/dobę na usera, 3 000/dobę globalnie; przekroczenie = 429 i toast „Limit tłumaczeń na dziś wyczerpany". Własnej notki (edytor) nie tłumaczymy. Sprawdzone na prodzie: PL→EN z zachowaniem emoji i nazw lokali, cache 0,5 s vs 3,5 s, anon = 401, limit godzinowy odcina 31. wywołanie.
  - **Panel ops (`src/admin/**`) zostaje po polsku** - widzą go tylko founderzy (decyzja Nat 2026-09-06).
- **Ładowanie = [BrandSpinner](src/components/BrandSpinner.tsx)** (2026-09-17): trzy kropki marki krążące po okręgu, wzór Pinteresta. ⛔ Zastąpił trzy kropki SKACZĄCE jedna po drugiej (`animate-bounce` z `animationDelay`) - tamte czytały się jak „ktoś pisze" w komunikatorze, a nie jak ładowanie ekranu. Obraca się CAŁY kontener wbudowanym `animate-spin`, a kropki stoją nieruchomo na okręgu: jedna animacja transformu zamiast trzech, czyli najtańszy wariant akurat w momencie przejścia między ekranami. ⚠️ Kropki mają malejące krycie (1 / .55 / .28) - bez tego obracający się trójkąt równoboczny wygląda, jakby stał w miejscu, bo oko nie ma czego śledzić.
- **Toast:** Sonner (`import { toast } from "sonner"`). **U GÓRY ekranu jako BIAŁA pigułka** (2026-09-11, wzór Pinterest), akcja (`action: { label: "Cofnij" | "Zobacz" }`) = mała CIEMNA pigułka po prawej. ⛔ Wariant ŻÓŁTY (tło `#FDF184`, brązowy guzik) był próbowany 2026-09-15 i odrzucony tego samego dnia - nie proponuj go ponownie bez wyraźnej prośby. NIE ustawiaj `position` per wywołanie. ⚠️ Na żółtym piszemy WYŁĄCZNIE brązem (10:1) - pomarańcz marki ma na nim 3,08:1 i nadaje się tylko do dużych nagłówków. ⚠️ Kolor jest WSPÓLNY dla wszystkich toastów, także `toast.error` - błędy są żółte, nie czerwone; jeśli błąd ma się odcinać, dołóż osobny wariant, nie zmieniaj tego. Wszystko żyje w [sonner.tsx](src/components/ui/sonner.tsx).
- **Formularze:** React Hook Form + Zod gdy złożone; prosty `useState` gdy 1-2 pola
- **Data fetching:** Supabase client direct lub `useQuery` z TanStack Query
- **Routing:** React Router v6, `useNavigate()` hook
- **Nie używaj:** `any` bez komentarza wyjaśniającego czemu; `console.log` bez prefiksu `[module-name]`

---

## Struktura repo

```
/
├── api/                    # Vercel Edge Functions (photo proxy, demo-places)
├── src/
│   ├── components/
│   │   ├── business/       # Komponenty dashboardu B2B
│   │   ├── home/           # Komponenty strony głównej (feed, karty tras)
│   │   ├── layout/         # AppLayout, BottomNav, OrbOverlay
│   │   ├── plan-wizard/    # Główny flow planowania (CityPicker → Swiper → Wyniki)
│   │   ├── route/          # Edytor trasy, timeline, chat AI
│   │   ├── social/         # Feed społecznościowy
│   │   └── ui/             # shadcn/ui primitives
│   ├── lib/
│   │   ├── placePhotos.ts  # getPhotoUrl() — CORE, nie ruszaj
│   │   ├── googleMaps.ts   # Geocoding utilities
│   │   └── mockPlaces.ts   # Mock data (używany jako fallback)
│   ├── pages/              # Route components (jeden plik = jedna strona)
│   └── integrations/supabase/  # Wygenerowane typy + klient
├── supabase/
│   ├── functions/          # Edge Functions (google-places-proxy, AI chat, etc.)
│   └── migrations/         # SQL migracje (NIE edytuj istniejących)
└── CLAUDE.md               # Ten plik
```
